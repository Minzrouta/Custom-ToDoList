---
phase: 01-bootstrap-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - postcss.config.mjs
  - .eslintrc.json
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/components/providers/ThemeProvider.tsx
  - src/lib/utils.ts
autonomous: true
requirements:
  - NFR-01
  - NFR-03

must_haves:
  truths:
    - "`npm run dev` démarre sans erreur sur le port 3000"
    - "`npm run build` se termine sans erreur TypeScript ni ESLint"
    - "La page d'accueil affiche un placeholder français dans le navigateur"
    - "Le dark mode s'active via la classe `dark` sur `<html>` grâce à next-themes"
    - "TypeScript strict est activé et ne produit aucune erreur de compilation"
  artifacts:
    - path: "package.json"
      provides: "Déclaration des dépendances Next.js 15, next-themes, tailwindcss"
      contains: "next-themes"
    - path: "src/app/layout.tsx"
      provides: "Root layout avec ThemeProvider et dark mode"
      exports: ["default RootLayout"]
    - path: "src/app/page.tsx"
      provides: "Page d'accueil placeholder"
      exports: ["default Home"]
    - path: "src/components/providers/ThemeProvider.tsx"
      provides: "Wrapper next-themes pour dark mode"
      exports: ["ThemeProvider"]
    - path: "tailwind.config.ts"
      provides: "Config Tailwind avec darkMode: 'class'"
      contains: "darkMode"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/providers/ThemeProvider.tsx"
      via: "import et wrapping du children"
      pattern: "ThemeProvider"
    - from: "tailwind.config.ts"
      to: "dark mode"
      via: "darkMode: 'class'"
      pattern: "darkMode.*class"
---

<objective>
Initialiser le projet Next.js 15 App Router avec TypeScript strict, Tailwind CSS, ESLint, et support dark mode via next-themes. Le résultat est une base compilable et testable localement, avec une page d'accueil placeholder en français.

Purpose: Toutes les phases suivantes s'appuient sur cette fondation. Elle doit compiler proprement, supporter le dark mode dès le départ, et établir la structure de dossiers App Router.
Output: Projet Next.js 15 fonctionnel avec `npm run dev` et `npm run build` qui passent, dark mode configuré, page placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialiser Next.js 15 avec TypeScript, Tailwind et ESLint</name>
  <read_first>
    - .planning/PROJECT.md (stack et contraintes)
    - .planning/ROADMAP.md (Phase 1 goal)
  </read_first>
  <files>
    package.json, tsconfig.json, next.config.ts, tailwind.config.ts,
    postcss.config.mjs, .eslintrc.json, src/app/globals.css
  </files>
  <action>
Depuis le répertoire /home/ubuntu/Custom-ToDoList, initialiser le projet avec la commande suivante (répondre non-interactivement via les flags) :

```bash
npx create-next-app@latest . \
  --typescript \
  --tailwind \
  --eslint \
  --app \
  --src-dir \
  --import-alias "@/*" \
  --no-git \
  --yes
```

Ensuite installer next-themes :
```bash
npm install next-themes
```

Modifier `tailwind.config.ts` pour activer le dark mode par classe :
```typescript
import type { Config } from "tailwindcss";

const config: Config = {
  darkMode: "class",
  content: [
    "./src/pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/components/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
export default config;
```

Modifier `tsconfig.json` pour activer le mode strict (il doit déjà être strict avec create-next-app, vérifier que `"strict": true` est présent dans `compilerOptions`).

Modifier `src/app/globals.css` pour inclure les directives Tailwind et des variables CSS de base :
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --background: #ffffff;
  --foreground: #171717;
}

@media (prefers-color-scheme: dark) {
  :root {
    --background: #0a0a0a;
    --foreground: #ededed;
  }
}

body {
  color: var(--foreground);
  background: var(--background);
  font-family: Arial, Helvetica, sans-serif;
}
```

Modifier `next.config.ts` pour garder une config minimale propre :
```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "standalone",
};

export default nextConfig;
```

Note: `output: "standalone"` est requis pour que le build Docker fonctionne correctement (plan 01-03).
  </action>
  <verify>
    <automated>cd /home/ubuntu/Custom-ToDoList && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contient `"next"` avec une version >= 15 et `"next-themes"`
    - `tailwind.config.ts` contient `darkMode: "class"`
    - `tsconfig.json` contient `"strict": true` dans `compilerOptions`
    - `next.config.ts` contient `output: "standalone"`
    - `npm run build` se termine avec "✓ Compiled successfully" ou équivalent (exit code 0)
    - Le dossier `.next/standalone` est créé après le build
  </acceptance_criteria>
  <done>Projet Next.js 15 initialisé, dark mode Tailwind configuré, build standalone propre.</done>
</task>

<task type="auto">
  <name>Task 2: Créer ThemeProvider, layout root et page d'accueil placeholder</name>
  <read_first>
    - src/app/layout.tsx (généré par create-next-app, à remplacer)
    - src/app/page.tsx (généré par create-next-app, à remplacer)
    - package.json (vérifier que next-themes est installé)
  </read_first>
  <files>
    src/components/providers/ThemeProvider.tsx,
    src/app/layout.tsx,
    src/app/page.tsx,
    src/lib/utils.ts
  </files>
  <action>
Créer le dossier `src/components/providers/` et le fichier `ThemeProvider.tsx` :
```typescript
// src/components/providers/ThemeProvider.tsx
"use client";

import { ThemeProvider as NextThemesProvider } from "next-themes";
import { type ThemeProviderProps } from "next-themes";

export function ThemeProvider({ children, ...props }: ThemeProviderProps) {
  return <NextThemesProvider {...props}>{children}</NextThemesProvider>;
}
```

Créer le dossier `src/lib/` et le fichier `utils.ts` avec une fonction utilitaire de base :
```typescript
// src/lib/utils.ts
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

Installer les dépendances utilitaires :
```bash
npm install clsx tailwind-merge
```

Remplacer `src/app/layout.tsx` par :
```typescript
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/components/providers/ThemeProvider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Gestionnaire de tâches",
  description: "Application de gestion de tâches multi-utilisateur",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="fr" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-white dark:bg-gray-950 text-gray-900 dark:text-gray-100`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
```

Remplacer `src/app/page.tsx` par :
```typescript
export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-24">
      <div className="text-center">
        <h1 className="text-4xl font-bold mb-4">Gestionnaire de tâches</h1>
        <p className="text-lg text-gray-600 dark:text-gray-400 mb-8">
          Application de gestion de tâches — déploiement en cours
        </p>
        <div className="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-green-100 dark:bg-green-900 text-green-700 dark:text-green-300 text-sm font-medium">
          <span className="w-2 h-2 rounded-full bg-green-500 animate-pulse"></span>
          Phase 1 — Infrastructure prête
        </div>
      </div>
    </main>
  );
}
```

Note : `suppressHydrationWarning` sur `<html>` est requis pour éviter l'erreur d'hydratation React quand next-themes change la classe `dark` côté client.
  </action>
  <verify>
    <automated>cd /home/ubuntu/Custom-ToDoList && npm run build 2>&1 | grep -E "error|Error|compiled" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/providers/ThemeProvider.tsx` existe et exporte `ThemeProvider`
    - `src/lib/utils.ts` existe et exporte la fonction `cn`
    - `src/app/layout.tsx` contient `ThemeProvider` avec `attribute="class"` et `suppressHydrationWarning`
    - `src/app/page.tsx` contient le texte "Gestionnaire de tâches" en français
    - `npm run build` passe sans erreur TypeScript (exit code 0)
    - Vérification grep : `grep -c "ThemeProvider" src/app/layout.tsx` retourne >= 1
    - Vérification grep : `grep -v '^//' src/app/layout.tsx | grep -c "suppressHydrationWarning"` retourne >= 1
    - Vérification grep : `grep -c "darkMode.*class" tailwind.config.ts` retourne 1
  </acceptance_criteria>
  <done>Layout root avec ThemeProvider opérationnel, page d'accueil placeholder en français, dark mode fonctionnel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → runtime | Variables NEXT_PUBLIC_* baked au build — toute fuite de secrets via ces vars est permanente jusqu'au rebuild |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | NEXT_PUBLIC_* vars | mitigate | Ne jamais mettre de secrets dans les vars NEXT_PUBLIC_* — uniquement des valeurs publiques (URL d'app, etc.) |
| T-01-02 | Information Disclosure | .env local | accept | .env est dans .gitignore, pas de secret committé en phase 1 |
</threat_model>

<verification>
```bash
# Build propre
cd /home/ubuntu/Custom-ToDoList && npm run build

# TypeScript strict
cd /home/ubuntu/Custom-ToDoList && npx tsc --noEmit

# ESLint propre
cd /home/ubuntu/Custom-ToDoList && npm run lint

# Vérifier dark mode configuré
grep -c "darkMode.*class" /home/ubuntu/Custom-ToDoList/tailwind.config.ts

# Vérifier ThemeProvider dans le layout
grep -c "ThemeProvider" /home/ubuntu/Custom-ToDoList/src/app/layout.tsx

# Vérifier output standalone
grep -c "standalone" /home/ubuntu/Custom-ToDoList/next.config.ts
```
</verification>

<success_criteria>
- `npm run build` exit code 0, dossier `.next/standalone` présent
- `npx tsc --noEmit` sans erreur
- `npm run lint` sans erreur
- `tailwind.config.ts` a `darkMode: "class"`
- `next.config.ts` a `output: "standalone"`
- `src/app/layout.tsx` wrappe les enfants dans `ThemeProvider`
- Page d'accueil en français avec texte "Gestionnaire de tâches"
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-bootstrap-infrastructure/01-01-SUMMARY.md`
</output>
