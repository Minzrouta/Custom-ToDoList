---
phase: 01-bootstrap-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - prisma/schema.prisma
  - src/lib/prisma.ts
  - src/app/api/health/route.ts
  - .env.example
autonomous: true
requirements:
  - NFR-01
  - NFR-03

must_haves:
  truths:
    - "Prisma est installé et `prisma generate` s'exécute sans erreur"
    - "Le schema contient les modèles User, Workspace, WorkspaceMember"
    - "Le client Prisma est exporté depuis `src/lib/prisma.ts` en singleton"
    - "L'endpoint GET `/api/health` répond JSON `{ status: 'ok', db: 'connected' }` avec code HTTP 200"
    - "`npm run build` passe toujours après ajout Prisma"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Schema DB avec User, Workspace, WorkspaceMember"
      contains: "model User"
    - path: "src/lib/prisma.ts"
      provides: "Client Prisma singleton"
      exports: ["prisma"]
    - path: "src/app/api/health/route.ts"
      provides: "Health check endpoint"
      exports: ["GET"]
    - path: ".env.example"
      provides: "Template des variables d'environnement requises"
      contains: "DATABASE_URL"
  key_links:
    - from: "src/app/api/health/route.ts"
      to: "src/lib/prisma.ts"
      via: "import prisma + prisma.$queryRaw"
      pattern: "prisma.*queryRaw"
    - from: "src/lib/prisma.ts"
      to: "prisma/schema.prisma"
      via: "PrismaClient généré"
      pattern: "PrismaClient"
---

<objective>
Installer et configurer Prisma ORM avec PostgreSQL. Créer le schema initial avec les modèles User, Workspace et WorkspaceMember anticipant la phase 2 Auth. Exposer l'endpoint `/api/health` qui vérifie la connectivité DB. Créer `.env.example` documentant les variables requises.

Purpose: La base de données et le client ORM sont nécessaires pour toutes les phases fonctionnelles. Le health check est requis par NFR-03 pour vérifier le bon fonctionnement du déploiement Coolify.
Output: Prisma configuré avec schema initial, client singleton, health check API route, fichier .env.example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bootstrap-infrastructure/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Contrats établis par le plan 01-01 que ce plan consomme -->

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From next.config.ts:
```typescript
// output: "standalone" activé — le build produit .next/standalone/
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Installer Prisma et créer le schema initial</name>
  <read_first>
    - .planning/REQUIREMENTS.md (FR-01, FR-02 pour comprendre les modèles Phase 2)
    - .planning/ROADMAP.md (Phase 2 schema requirements)
    - package.json (dépendances actuelles)
  </read_first>
  <files>
    prisma/schema.prisma, src/lib/prisma.ts, .env.example
  </files>
  <action>
Installer Prisma :
```bash
cd /home/ubuntu/Custom-ToDoList
npm install prisma @prisma/client
npx prisma init --datasource-provider postgresql
```

Cela crée `prisma/schema.prisma` et `.env` (qui sera dans .gitignore).

Remplacer le contenu de `prisma/schema.prisma` par le schema complet :

```prisma
// prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id            String    @id @default(cuid())
  name          String?
  email         String    @unique
  emailVerified DateTime?
  image         String?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  workspaces WorkspaceMember[]

  @@map("users")
}

model Workspace {
  id          String   @id @default(cuid())
  name        String
  description String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  members WorkspaceMember[]

  @@map("workspaces")
}

model WorkspaceMember {
  id          String   @id @default(cuid())
  role        Role     @default(MEMBER)
  joinedAt    DateTime @default(now())

  userId      String
  workspaceId String

  user      User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  workspace Workspace @relation(fields: [workspaceId], references: [id], onDelete: Cascade)

  @@unique([userId, workspaceId])
  @@map("workspace_members")
}

enum Role {
  OWNER
  MEMBER
}
```

Créer le client Prisma singleton dans `src/lib/prisma.ts` :
```typescript
// src/lib/prisma.ts
import { PrismaClient } from "@prisma/client";

const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined;
};

export const prisma =
  globalForPrisma.prisma ??
  new PrismaClient({
    log: process.env.NODE_ENV === "development" ? ["query", "error", "warn"] : ["error"],
  });

if (process.env.NODE_ENV !== "production") globalForPrisma.prisma = prisma;
```

Ce pattern singleton évite la création de multiples connexions PrismaClient en développement (Next.js hot reload).

Générer le client Prisma :
```bash
cd /home/ubuntu/Custom-ToDoList && npx prisma generate
```

Créer `.env.example` (jamais committé, sert de documentation) :
```
# .env.example — Copier en .env pour le développement local
# Ces variables sont injectées par Coolify en production (ne pas committer .env)

# Base de données PostgreSQL
DATABASE_URL="postgresql://postgres:password@localhost:5432/custom_todolist"

# Next.js (optionnel en dev)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="changeme-generate-with-openssl-rand-base64-32"
```

S'assurer que `.env` est dans `.gitignore` (create-next-app le fait normalement, vérifier).
  </action>
  <verify>
    <automated>cd /home/ubuntu/Custom-ToDoList && npx prisma generate 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` existe et contient les trois modèles : `grep -c "^model" prisma/schema.prisma` retourne 3
    - `prisma/schema.prisma` contient l'enum Role : `grep -c "enum Role" prisma/schema.prisma` retourne 1
    - `prisma/schema.prisma` contient `@@unique([userId, workspaceId])` dans WorkspaceMember
    - `src/lib/prisma.ts` existe et contient `globalForPrisma` (pattern singleton)
    - `npx prisma generate` s'exécute sans erreur (exit code 0)
    - `.env.example` existe et contient `DATABASE_URL`
    - `.gitignore` contient `.env` (vérifier avec `grep -c "^\.env$" .gitignore`)
  </acceptance_criteria>
  <done>Schema Prisma créé avec User/Workspace/WorkspaceMember, client singleton généré, .env.example documenté.</done>
</task>

<task type="auto">
  <name>Task 2: Créer l'endpoint health check `/api/health`</name>
  <read_first>
    - src/lib/prisma.ts (client Prisma à importer)
    - .planning/REQUIREMENTS.md (NFR-03 : health check `/api/health` requis)
  </read_first>
  <files>
    src/app/api/health/route.ts
  </files>
  <action>
Créer le dossier `src/app/api/health/` et le fichier `route.ts` :

```typescript
// src/app/api/health/route.ts
import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";

export async function GET() {
  try {
    // Vérifier la connectivité DB avec une requête légère
    await prisma.$queryRaw`SELECT 1`;

    return NextResponse.json(
      {
        status: "ok",
        db: "connected",
        timestamp: new Date().toISOString(),
      },
      { status: 200 }
    );
  } catch (error) {
    console.error("[health] DB connection failed:", error);

    return NextResponse.json(
      {
        status: "error",
        db: "disconnected",
        timestamp: new Date().toISOString(),
      },
      { status: 503 }
    );
  }
}
```

Note : En phase 1, sans DB connectée en local, cette route retournera 503. C'est le comportement attendu — elle retournera 200 une fois le Docker Compose déployé (plan 01-03). Le build Next.js ne nécessite pas de DB active.

Vérifier que le build passe toujours après ajout de la route :
```bash
cd /home/ubuntu/Custom-ToDoList && npm run build
```
  </action>
  <verify>
    <automated>cd /home/ubuntu/Custom-ToDoList && npm run build 2>&1 | grep -E "Route \(app\)|Error|error" | head -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/health/route.ts` existe et exporte une fonction `GET`
    - Le fichier contient `prisma.$queryRaw` pour vérifier la DB
    - Le fichier retourne `{ status: "ok", db: "connected" }` en cas de succès
    - Le fichier retourne HTTP 503 en cas d'échec DB (pas de crash non géré)
    - `npm run build` passe sans erreur (exit code 0) — la route apparaît dans la liste des routes compilées
    - Vérification : `grep -c "queryRaw" src/app/api/health/route.ts` retourne 1
    - Vérification : `grep -c "503" src/app/api/health/route.ts` retourne 1
  </acceptance_criteria>
  <done>Health check `/api/health` retourne JSON avec statut DB, build propre.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → PostgreSQL | Connexion DB via DATABASE_URL — credentials doivent être secrets |
| internet → /api/health | Endpoint public exposant des informations système |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | /api/health | mitigate | Retourner uniquement "ok/error" — ne jamais exposer le message d'erreur DB complet dans la réponse JSON (déjà géré : `console.error` mais pas dans la réponse) |
| T-01-05 | Information Disclosure | DATABASE_URL | mitigate | Injecté via Coolify environment (jamais committé) — .env dans .gitignore confirmé |
| T-01-06 | Denial of Service | /api/health (DB query) | accept | Requête `SELECT 1` est négligeable — rate limiting Traefik protège si nécessaire en phase ultérieure |
</threat_model>

<verification>
```bash
# Build propre avec Prisma
cd /home/ubuntu/Custom-ToDoList && npm run build

# Schema valide
cd /home/ubuntu/Custom-ToDoList && npx prisma validate

# Vérifier les 3 modèles
grep -c "^model" /home/ubuntu/Custom-ToDoList/prisma/schema.prisma

# Vérifier singleton Prisma
grep -c "globalForPrisma" /home/ubuntu/Custom-ToDoList/src/lib/prisma.ts

# Vérifier health check
grep -c "queryRaw" /home/ubuntu/Custom-ToDoList/src/app/api/health/route.ts

# Vérifier .env non committé
grep -c "^\.env$" /home/ubuntu/Custom-ToDoList/.gitignore
```
</verification>

<success_criteria>
- `npx prisma validate` passe sans erreur
- `npm run build` passe sans erreur, la route `/api/health` apparaît dans le build output
- Schema contient exactement 3 modèles (User, Workspace, WorkspaceMember) + 1 enum (Role)
- `.env.example` existe avec DATABASE_URL documenté
- `.env` est dans `.gitignore`
- `src/lib/prisma.ts` utilise le pattern singleton avec `globalThis`
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-bootstrap-infrastructure/01-02-SUMMARY.md`
</output>
