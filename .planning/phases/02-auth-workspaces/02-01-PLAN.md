---
phase: 02-auth-workspaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/auth.ts
  - src/app/api/auth/[...nextauth]/route.ts
  - src/types/next-auth.d.ts
autonomous: true
requirements:
  - FR-01
  - NFR-02

must_haves:
  truths:
    - "Un utilisateur peut se connecter via Google OAuth et obtenir une session"
    - "Un utilisateur peut se connecter via GitHub OAuth et obtenir une session"
    - "Les modèles NextAuth (Account, Session, VerificationToken) existent en base"
    - "La session expose session.user.id utilisable dans les Server Components et API routes"
    - "Les workspaces Boulot/Ecole/Perso sont créés automatiquement au premier login"
  artifacts:
    - path: "src/auth.ts"
      provides: "Configuration NextAuth v5 avec providers Google/GitHub et callback workspaces par défaut"
      exports: ["handlers", "signIn", "signOut", "auth"]
    - path: "prisma/schema.prisma"
      provides: "Modèles Account, Session, VerificationToken + relations User.accounts/sessions"
      contains: "model Account"
    - path: "src/app/api/auth/[...nextauth]/route.ts"
      provides: "Route handler OAuth (GET + POST)"
      exports: ["GET", "POST"]
    - path: "src/types/next-auth.d.ts"
      provides: "Augmentation du type Session pour inclure user.id"
  key_links:
    - from: "src/auth.ts"
      to: "prisma/schema.prisma"
      via: "PrismaAdapter"
      pattern: "PrismaAdapter\\(prisma\\)"
    - from: "src/app/api/auth/[...nextauth]/route.ts"
      to: "src/auth.ts"
      via: "re-export handlers"
      pattern: "export const \\{ GET, POST \\} = handlers"
    - from: "src/auth.ts callback signIn"
      to: "prisma.workspace.create"
      via: "création workspaces par défaut"
      pattern: "prisma\\.workspace\\.create"

user_setup:
  - service: google-oauth
    why: "Login OAuth Google"
    env_vars:
      - name: GOOGLE_CLIENT_ID
        source: "Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client"
      - name: GOOGLE_CLIENT_SECRET
        source: "Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client"
    dashboard_config:
      - task: "Ajouter l'URI de redirection autorisée"
        location: "Google Cloud Console → Credentials → Authorized redirect URIs"
        value: "https://tasks.bantou.me/api/auth/callback/google (prod) + http://localhost:3000/api/auth/callback/google (dev)"
  - service: github-oauth
    why: "Login OAuth GitHub"
    env_vars:
      - name: GITHUB_ID
        source: "GitHub → Settings → Developer settings → OAuth Apps → Client ID"
      - name: GITHUB_SECRET
        source: "GitHub → Settings → Developer settings → OAuth Apps → Client secret"
    dashboard_config:
      - task: "Créer une OAuth App GitHub"
        location: "GitHub → Settings → Developer settings → OAuth Apps → New OAuth App"
        value: "Homepage URL: https://tasks.bantou.me, Callback URL: https://tasks.bantou.me/api/auth/callback/github"
  - service: nextauth
    why: "Signature des sessions NextAuth"
    env_vars:
      - name: NEXTAUTH_SECRET
        source: "Générer avec: openssl rand -base64 32"
      - name: NEXTAUTH_URL
        source: "https://tasks.bantou.me (prod) ou http://localhost:3000 (dev)"
---

<objective>
Installer NextAuth.js v5 avec providers OAuth Google et GitHub, étendre le schema Prisma avec les modèles requis par @auth/prisma-adapter, configurer le callback signIn pour créer les workspaces par défaut au premier login, et exposer la route handler `/api/auth/[...nextauth]`.

Purpose: Fournir la couche d'identité complète sur laquelle toutes les phases suivantes s'appuient. Après ce plan, n'importe quel Server Component ou API route peut appeler `auth()` pour obtenir la session de l'utilisateur connecté.

Output: `src/auth.ts` (config NextAuth), `prisma/schema.prisma` étendu, `src/app/api/auth/[...nextauth]/route.ts`, `src/types/next-auth.d.ts`.
</objective>

<execution_context>
@/home/ubuntu/Custom-ToDoList/node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md
</execution_context>

<context>
@/home/ubuntu/Custom-ToDoList/.planning/ROADMAP.md
@/home/ubuntu/Custom-ToDoList/.planning/phases/02-auth-workspaces/02-CONTEXT.md
@/home/ubuntu/Custom-ToDoList/.planning/phases/02-auth-workspaces/02-PATTERNS.md

<interfaces>
<!-- Fichiers existants que ce plan consomme -->

From prisma/schema.prisma (état actuel — Phase 1):
```prisma
model User {
  id            String    @id @default(cuid())
  name          String?
  email         String    @unique
  emailVerified DateTime?
  image         String?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  workspaces WorkspaceMember[]
  // Phase 2 ajoutera: accounts Account[] et sessions Session[]

  @@map("users")
}

model Workspace {
  id          String   @id @default(cuid())
  name        String
  description String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  members WorkspaceMember[]

  @@map("workspaces")
}

model WorkspaceMember {
  id       String   @id @default(cuid())
  role     Role     @default(MEMBER)
  joinedAt DateTime @default(now())

  userId      String
  workspaceId String

  user      User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  workspace Workspace @relation(fields: [workspaceId], references: [id], onDelete: Cascade)

  @@unique([userId, workspaceId])
  @@map("workspace_members")
}

enum Role {
  OWNER
  MEMBER
}
```

From src/lib/prisma.ts:
```typescript
export const prisma: PrismaClient  // singleton
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : Installer next-auth et étendre le schema Prisma</name>
  <files>prisma/schema.prisma</files>
  <action>
**Étape 1 — Installer les packages :**
```bash
cd /home/ubuntu/Custom-ToDoList && npm install next-auth@beta @auth/prisma-adapter
```

**Étape 2 — Étendre `prisma/schema.prisma` :**

Ajouter dans le modèle `User` (après `workspaces WorkspaceMember[]`) :
```prisma
  accounts Account[]
  sessions Session[]
```

Ajouter après l'enum `Role`, les trois modèles NextAuth requis par `@auth/prisma-adapter` :

```prisma
model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String? @db.Text
  access_token      String? @db.Text
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String? @db.Text
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
  @@map("accounts")
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("sessions")
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
  @@map("verification_tokens")
}
```

**Étape 3 — [BLOCKING] Push du schema en base :**
```bash
cd /home/ubuntu/Custom-ToDoList && npx prisma db push --accept-data-loss
```

Cette commande doit réussir avant toute implémentation des API routes. Si elle échoue, vérifier que `DATABASE_URL` est défini et que PostgreSQL est accessible.

**Étape 4 — Régénérer le client Prisma :**
```bash
cd /home/ubuntu/Custom-ToDoList && npx prisma generate
```
  </action>
  <verify>
    <automated>cd /home/ubuntu/Custom-ToDoList && npx prisma validate && node -e "const {PrismaClient}=require('@prisma/client');const p=new PrismaClient();p.account.findFirst().then(()=>{console.log('OK');p.\$disconnect()}).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <done>
    - `npm install` réussi, `next-auth` et `@auth/prisma-adapter` présents dans `node_modules`
    - `prisma validate` ne retourne aucune erreur
    - `prisma db push` a créé les tables `accounts`, `sessions`, `verification_tokens` en base
    - `prisma generate` a régénéré le client avec les nouveaux modèles
    - `prisma.account.findFirst()` s'exécute sans erreur (table existe)
  </done>
</task>

<task type="auto">
  <name>Tâche 2 : Configurer NextAuth v5 — auth.ts, route handler et types</name>
  <files>
    src/auth.ts
    src/app/api/auth/[...nextauth]/route.ts
    src/types/next-auth.d.ts
  </files>
  <action>
**Fichier 1 — `src/auth.ts` (configuration NextAuth v5) :**

```typescript
// src/auth.ts
import NextAuth from "next-auth";
import { PrismaAdapter } from "@auth/prisma-adapter";
import Google from "next-auth/providers/google";
import GitHub from "next-auth/providers/github";
import { prisma } from "@/lib/prisma";

export const { handlers, signIn, signOut, auth } = NextAuth({
  adapter: PrismaAdapter(prisma),
  providers: [
    Google({
      clientId: process.env.GOOGLE_CLIENT_ID!,
      clientSecret: process.env.GOOGLE_CLIENT_SECRET!,
    }),
    GitHub({
      clientId: process.env.GITHUB_ID!,
      clientSecret: process.env.GITHUB_SECRET!,
    }),
  ],
  session: { strategy: "database" },
  callbacks: {
    async signIn({ user }) {
      // Créer les workspaces par défaut au premier login
      if (user.id) {
        const existing = await prisma.workspaceMember.findFirst({
          where: { userId: user.id },
        });
        if (!existing) {
          const defaults = ["Boulot", "Ecole", "Perso"];
          for (const name of defaults) {
            const workspace = await prisma.workspace.create({
              data: { name },
            });
            await prisma.workspaceMember.create({
              data: {
                userId: user.id,
                workspaceId: workspace.id,
                role: "OWNER",
              },
            });
          }
        }
      }
      return true;
    },
    async session({ session, user }) {
      // Exposer l'ID utilisateur dans la session
      session.user.id = user.id;
      return session;
    },
  },
  pages: {
    signIn: "/auth/signin",
  },
});
```

**Fichier 2 — `src/app/api/auth/[...nextauth]/route.ts` (re-export handlers) :**

Créer le répertoire `src/app/api/auth/[...nextauth]/` puis le fichier :

```typescript
// src/app/api/auth/[...nextauth]/route.ts
import { handlers } from "@/auth";

export const { GET, POST } = handlers;
```

**Fichier 3 — `src/types/next-auth.d.ts` (augmentation des types) :**

Créer le répertoire `src/types/` si absent :

```typescript
// src/types/next-auth.d.ts
import type { DefaultSession } from "next-auth";

declare module "next-auth" {
  interface Session {
    user: {
      id: string;
    } & DefaultSession["user"];
  }
}
```

Ce fichier est essentiel pour que TypeScript accepte `session.user.id` sans erreur dans tous les Server Components et API routes.
  </action>
  <verify>
    <automated>cd /home/ubuntu/Custom-ToDoList && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - `src/auth.ts` exporte `{ handlers, signIn, signOut, auth }` depuis next-auth
    - `src/app/api/auth/[...nextauth]/route.ts` exporte `{ GET, POST }` depuis handlers
    - `src/types/next-auth.d.ts` augmente le type Session avec `user.id: string`
    - `npx tsc --noEmit` ne retourne aucune erreur TypeScript sur ces fichiers
    - `npm run build` passe sans erreur (les providers OAuth échoueront à l'exécution sans les env vars, mais la compilation doit réussir)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/auth/* | Callbacks OAuth — données venant du provider externe |
| OAuth Provider → NextAuth | Tokens OAuth reçus de Google/GitHub |
| NextAuth callback → Prisma | Écriture session/account en base |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | OAuth callback `/api/auth/callback/*` | mitigate | NextAuth valide le `state` CSRF et le `code` OAuth côté provider — ne pas exposer le NEXTAUTH_SECRET |
| T-02-02 | Tampering | `src/auth.ts` callback `signIn` | mitigate | La création de workspaces par défaut utilise `user.id` fourni par NextAuth après validation OAuth — pas d'input utilisateur direct |
| T-02-03 | Information Disclosure | Session côté client | mitigate | `session.user.id` exposé uniquement via `session()` de next-auth — jamais de token brut dans le client. NEXTAUTH_SECRET stocké en env var Coolify uniquement |
| T-02-04 | Elevation of Privilege | Modèles Account/Session | accept | Tables gérées exclusivement par NextAuth adapter — aucune API route n'écrit directement dedans |
</threat_model>

<verification>
Après exécution complète du plan :

1. `npx prisma validate` — aucune erreur de schema
2. `npx prisma db push --accept-data-loss` — tables `accounts`, `sessions`, `verification_tokens` créées
3. `npx tsc --noEmit` — aucune erreur TypeScript
4. `npm run build` — build Next.js réussi
5. `curl http://localhost:3000/api/auth/providers` (après `npm run dev`) — retourne JSON avec `google` et `github` comme providers
</verification>

<success_criteria>
- Packages `next-auth@beta` et `@auth/prisma-adapter` installés
- Schema Prisma étendu avec Account, Session, VerificationToken (db push réussi)
- `auth()` appelable depuis n'importe quel Server Component ou API route
- `session.user.id` typé en TypeScript sans erreur
- Callback signIn crée 3 workspaces par défaut (Boulot/Ecole/Perso) au premier login d'un nouvel utilisateur
- Route `/api/auth/[...nextauth]` opérationnelle pour les callbacks OAuth Google et GitHub
</success_criteria>

<output>
Après complétion, créer `.planning/phases/02-auth-workspaces/02-01-SUMMARY.md` avec :
- Packages installés et leurs versions
- Modèles Prisma ajoutés
- Fichiers créés
- Variables d'environnement requises pour le déploiement
</output>
