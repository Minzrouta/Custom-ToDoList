---
phase: 02-auth-workspaces
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01-PLAN.md
files_modified:
  - src/proxy.ts
  - src/app/api/workspaces/route.ts
  - src/app/api/workspaces/[id]/route.ts
  - src/app/api/workspaces/[id]/members/route.ts
autonomous: true
requirements:
  - FR-02
  - NFR-02

must_haves:
  truths:
    - "Les routes protégées redirigent vers /auth/signin si l'utilisateur n'est pas connecté"
    - "GET /api/workspaces retourne uniquement les workspaces dont l'user est membre"
    - "POST /api/workspaces crée un workspace avec l'user comme OWNER"
    - "PATCH/DELETE /api/workspaces/[id] vérifie que l'user est OWNER avant modification"
    - "POST /api/workspaces/[id]/members invite un user par email (OWNER uniquement)"
    - "Un user ne peut jamais accéder aux données d'un workspace dont il n'est pas membre (403)"
  artifacts:
    - path: "src/proxy.ts"
      provides: "Protection des routes — redirection /auth/signin si non authentifié"
      exports: ["proxy", "config"]
    - path: "src/app/api/workspaces/route.ts"
      provides: "CRUD workspaces (GET liste + POST création)"
      exports: ["GET", "POST"]
    - path: "src/app/api/workspaces/[id]/route.ts"
      provides: "CRUD workspace individuel (GET + PATCH + DELETE)"
      exports: ["GET", "PATCH", "DELETE"]
    - path: "src/app/api/workspaces/[id]/members/route.ts"
      provides: "Gestion membres (GET liste + POST invitation par email)"
      exports: ["GET", "POST"]
  key_links:
    - from: "src/proxy.ts"
      to: "src/auth.ts"
      via: "import auth pour vérifier la session"
      pattern: "import.*auth.*from.*@/auth"
    - from: "src/app/api/workspaces/[id]/route.ts"
      to: "prisma.workspaceMember.findUnique"
      via: "fonction requireMembership — isolation workspace"
      pattern: "userId_workspaceId.*userId.*workspaceId"
    - from: "src/app/api/workspaces/[id]/members/route.ts"
      to: "prisma.user.findUnique"
      via: "lookup user par email pour invitation"
      pattern: "prisma\\.user\\.findUnique.*email"
---

<objective>
Créer `src/proxy.ts` (protection des routes Next.js 16), les API routes CRUD pour les workspaces, et l'endpoint d'invitation par email. L'isolation workspace est garantie par une vérification de membership dans chaque handler.

Purpose: Fournir la couche sécurité et API qui isole les données par workspace — aucun utilisateur ne peut accéder à des données d'un workspace dont il n'est pas membre.

Output: `src/proxy.ts`, `src/app/api/workspaces/route.ts`, `src/app/api/workspaces/[id]/route.ts`, `src/app/api/workspaces/[id]/members/route.ts`.
</objective>

<execution_context>
@/home/ubuntu/Custom-ToDoList/node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md
</execution_context>

<context>
@/home/ubuntu/Custom-ToDoList/.planning/ROADMAP.md
@/home/ubuntu/Custom-ToDoList/.planning/phases/02-auth-workspaces/02-CONTEXT.md
@/home/ubuntu/Custom-ToDoList/.planning/phases/02-auth-workspaces/02-PATTERNS.md

<interfaces>
<!-- Types et exports produits par 02-01-PLAN.md — utiliser directement, sans exploration -->

From src/auth.ts (créé en 02-01):
```typescript
export async function auth(): Promise<Session | null>
export { handlers, signIn, signOut }
// Session.user.id est typé grâce à src/types/next-auth.d.ts
```

From prisma/schema.prisma (état après 02-01):
```prisma
model WorkspaceMember {
  // Clé composite unique: @@unique([userId, workspaceId])
  // Accessible via: prisma.workspaceMember.findUnique({ where: { userId_workspaceId: { userId, workspaceId } } })
  role     Role  // "OWNER" | "MEMBER"
}

model User {
  id    String @id
  email String @unique
  name  String?
  image String?
}

model Workspace {
  id   String @id
  name String
}
```

From src/lib/prisma.ts:
```typescript
export const prisma: PrismaClient
```

From src/app/api/health/route.ts (pattern exact à reproduire):
```typescript
// Pattern error handling — toujours try/catch + Response.json() (pas NextResponse.json())
return Response.json({ data: result }, { status: 200 });
return Response.json({ error: "message" }, { status: 4XX });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : Créer src/proxy.ts — protection des routes (Next.js 16)</name>
  <files>src/proxy.ts</files>
  <action>
**CRITIQUE : Next.js 16 utilise `proxy.ts` (pas `middleware.ts`). La fonction exportée s'appelle `proxy`, pas `middleware`.**

Créer `src/proxy.ts` à la racine du dossier `src/` :

```typescript
// src/proxy.ts
// IMPORTANT: Next.js 16 — ce fichier remplace middleware.ts (déprecié)
// La fonction exportée DOIT s'appeler "proxy" (pas "middleware")
import { NextResponse } from "next/server";
import type { NextRequest } from "next/server";
import { auth } from "@/auth";

export async function proxy(request: NextRequest) {
  const session = await auth();
  const { pathname } = request.nextUrl;

  // Routes publiques — laisser passer sans authentification
  const publicPaths = ["/", "/auth/signin", "/api/auth"];
  const isPublic = publicPaths.some(
    (path) => pathname === path || pathname.startsWith(path + "/")
  );

  if (!session && !isPublic) {
    return NextResponse.redirect(new URL("/auth/signin", request.url));
  }

  return NextResponse.next();
}

export const config = {
  matcher: [
    /*
     * Exclure les ressources statiques Next.js et les fichiers binaires
     * Appliquer le proxy à toutes les autres routes
     */
    "/((?!_next/static|_next/image|favicon.ico|.*\\.png$|.*\\.jpg$|.*\\.svg$).*)",
  ],
};
```

Ne pas créer de fichier `middleware.ts` — il est déprecié en Next.js 16 et serait ignoré.
  </action>
  <verify>
    <automated>cd /home/ubuntu/Custom-ToDoList && npx tsc --noEmit 2>&1 | grep -i "proxy" || echo "No proxy errors"</automated>
  </verify>
  <done>
    - `src/proxy.ts` existe et exporte une fonction nommée `proxy` (pas `middleware`)
    - Fichier situé dans `src/` (même niveau que `app/`)
    - `export const config` avec un `matcher` défini
    - `npx tsc --noEmit` sans erreur sur `src/proxy.ts`
    - Aucun `middleware.ts` créé (fichier déprecié)
  </done>
</task>

<task type="auto">
  <name>Tâche 2 : API routes workspaces — CRUD complet avec isolation workspace</name>
  <files>
    src/app/api/workspaces/route.ts
    src/app/api/workspaces/[id]/route.ts
    src/app/api/workspaces/[id]/members/route.ts
  </files>
  <action>
**Fichier 1 — `src/app/api/workspaces/route.ts` (liste + création) :**

```typescript
// src/app/api/workspaces/route.ts
import { prisma } from "@/lib/prisma";
import { auth } from "@/auth";

// GET /api/workspaces — lister les workspaces de l'utilisateur connecté
export async function GET() {
  const session = await auth();
  if (!session?.user?.id) {
    return Response.json({ error: "Non authentifié" }, { status: 401 });
  }

  try {
    const memberships = await prisma.workspaceMember.findMany({
      where: { userId: session.user.id },
      include: {
        workspace: true,
      },
      orderBy: { joinedAt: "asc" },
    });
    return Response.json({
      data: memberships.map((m) => ({
        ...m.workspace,
        role: m.role,
      })),
    });
  } catch (error) {
    console.error("[workspaces:GET]", error);
    return Response.json({ error: "Erreur interne" }, { status: 500 });
  }
}

// POST /api/workspaces — créer un workspace
export async function POST(request: Request) {
  const session = await auth();
  if (!session?.user?.id) {
    return Response.json({ error: "Non authentifié" }, { status: 401 });
  }

  try {
    const body = await request.json();
    const { name } = body;

    if (!name || typeof name !== "string" || name.trim().length === 0) {
      return Response.json({ error: "Le nom du workspace est requis" }, { status: 400 });
    }

    if (name.trim().length > 100) {
      return Response.json({ error: "Le nom ne peut pas dépasser 100 caractères" }, { status: 400 });
    }

    const workspace = await prisma.workspace.create({
      data: {
        name: name.trim(),
        members: {
          create: { userId: session.user.id, role: "OWNER" },
        },
      },
    });
    return Response.json({ data: { ...workspace, role: "OWNER" } }, { status: 201 });
  } catch (error) {
    console.error("[workspaces:POST]", error);
    return Response.json({ error: "Erreur interne" }, { status: 500 });
  }
}
```

**Fichier 2 — `src/app/api/workspaces/[id]/route.ts` (détail + modification + suppression) :**

```typescript
// src/app/api/workspaces/[id]/route.ts
import { prisma } from "@/lib/prisma";
import { auth } from "@/auth";

// Helper : vérifier que l'utilisateur est membre du workspace (isolation)
async function requireMembership(workspaceId: string, userId: string) {
  return prisma.workspaceMember.findUnique({
    where: { userId_workspaceId: { userId, workspaceId } },
  });
}

// GET /api/workspaces/[id] — détail d'un workspace
export async function GET(
  _request: Request,
  { params }: { params: Promise<{ id: string }> }
) {
  const session = await auth();
  if (!session?.user?.id) {
    return Response.json({ error: "Non authentifié" }, { status: 401 });
  }

  try {
    const { id } = await params;
    const member = await requireMembership(id, session.user.id);
    if (!member) {
      return Response.json({ error: "Accès refusé" }, { status: 403 });
    }

    const workspace = await prisma.workspace.findUnique({
      where: { id },
      include: {
        members: {
          include: {
            user: { select: { id: true, name: true, email: true, image: true } },
          },
        },
      },
    });

    if (!workspace) {
      return Response.json({ error: "Workspace introuvable" }, { status: 404 });
    }

    return Response.json({ data: { ...workspace, role: member.role } });
  } catch (error) {
    console.error("[workspace:GET]", error);
    return Response.json({ error: "Erreur interne" }, { status: 500 });
  }
}

// PATCH /api/workspaces/[id] — renommer un workspace (OWNER uniquement)
export async function PATCH(
  request: Request,
  { params }: { params: Promise<{ id: string }> }
) {
  const session = await auth();
  if (!session?.user?.id) {
    return Response.json({ error: "Non authentifié" }, { status: 401 });
  }

  try {
    const { id } = await params;
    const member = await requireMembership(id, session.user.id);
    if (!member || member.role !== "OWNER") {
      return Response.json({ error: "Accès refusé — OWNER requis" }, { status: 403 });
    }

    const body = await request.json();
    const { name } = body;

    if (!name || typeof name !== "string" || name.trim().length === 0) {
      return Response.json({ error: "Le nom est requis" }, { status: 400 });
    }

    if (name.trim().length > 100) {
      return Response.json({ error: "Le nom ne peut pas dépasser 100 caractères" }, { status: 400 });
    }

    const workspace = await prisma.workspace.update({
      where: { id },
      data: { name: name.trim() },
    });

    return Response.json({ data: { ...workspace, role: member.role } });
  } catch (error) {
    console.error("[workspace:PATCH]", error);
    return Response.json({ error: "Erreur interne" }, { status: 500 });
  }
}

// DELETE /api/workspaces/[id] — supprimer un workspace (OWNER uniquement)
export async function DELETE(
  _request: Request,
  { params }: { params: Promise<{ id: string }> }
) {
  const session = await auth();
  if (!session?.user?.id) {
    return Response.json({ error: "Non authentifié" }, { status: 401 });
  }

  try {
    const { id } = await params;
    const member = await requireMembership(id, session.user.id);
    if (!member || member.role !== "OWNER") {
      return Response.json({ error: "Accès refusé — OWNER requis" }, { status: 403 });
    }

    await prisma.workspace.delete({ where: { id } });
    return Response.json({ success: true });
  } catch (error) {
    console.error("[workspace:DELETE]", error);
    return Response.json({ error: "Erreur interne" }, { status: 500 });
  }
}
```

**Fichier 3 — `src/app/api/workspaces/[id]/members/route.ts` (liste membres + invitation) :**

```typescript
// src/app/api/workspaces/[id]/members/route.ts
import { prisma } from "@/lib/prisma";
import { auth } from "@/auth";

// GET /api/workspaces/[id]/members — lister les membres du workspace
export async function GET(
  _request: Request,
  { params }: { params: Promise<{ id: string }> }
) {
  const session = await auth();
  if (!session?.user?.id) {
    return Response.json({ error: "Non authentifié" }, { status: 401 });
  }

  try {
    const { id } = await params;

    // Vérifier que l'user est membre avant d'exposer la liste
    const self = await prisma.workspaceMember.findUnique({
      where: { userId_workspaceId: { userId: session.user.id, workspaceId: id } },
    });
    if (!self) {
      return Response.json({ error: "Accès refusé" }, { status: 403 });
    }

    const members = await prisma.workspaceMember.findMany({
      where: { workspaceId: id },
      include: {
        user: { select: { id: true, name: true, email: true, image: true } },
      },
      orderBy: { joinedAt: "asc" },
    });

    return Response.json({ data: members });
  } catch (error) {
    console.error("[members:GET]", error);
    return Response.json({ error: "Erreur interne" }, { status: 500 });
  }
}

// POST /api/workspaces/[id]/members — inviter un utilisateur par email (OWNER uniquement)
export async function POST(
  request: Request,
  { params }: { params: Promise<{ id: string }> }
) {
  const session = await auth();
  if (!session?.user?.id) {
    return Response.json({ error: "Non authentifié" }, { status: 401 });
  }

  try {
    const { id } = await params;

    // Seul l'OWNER peut inviter
    const self = await prisma.workspaceMember.findUnique({
      where: { userId_workspaceId: { userId: session.user.id, workspaceId: id } },
    });
    if (!self || self.role !== "OWNER") {
      return Response.json({ error: "Accès refusé — OWNER requis pour inviter" }, { status: 403 });
    }

    const body = await request.json();
    const { email } = body;

    if (!email || typeof email !== "string" || !email.includes("@")) {
      return Response.json({ error: "Email invalide" }, { status: 400 });
    }

    // Trouver l'utilisateur invité (doit déjà avoir un compte)
    const targetUser = await prisma.user.findUnique({ where: { email: email.toLowerCase().trim() } });
    if (!targetUser) {
      return Response.json(
        { error: "Aucun compte trouvé pour cet email. L'utilisateur doit se connecter une première fois." },
        { status: 404 }
      );
    }

    // Vérifier que l'utilisateur n'est pas déjà membre
    const alreadyMember = await prisma.workspaceMember.findUnique({
      where: { userId_workspaceId: { userId: targetUser.id, workspaceId: id } },
    });
    if (alreadyMember) {
      return Response.json({ error: "Cet utilisateur est déjà membre du workspace" }, { status: 409 });
    }

    const member = await prisma.workspaceMember.create({
      data: { userId: targetUser.id, workspaceId: id, role: "MEMBER" },
      include: {
        user: { select: { id: true, name: true, email: true, image: true } },
      },
    });

    return Response.json({ data: member }, { status: 201 });
  } catch (error) {
    console.error("[members:POST]", error);
    return Response.json({ error: "Erreur interne" }, { status: 500 });
  }
}
```

**Règle critique pour tous les fichiers :** `params` est une `Promise` en Next.js 15+ — toujours `await params` avant d'en extraire les valeurs. Toujours utiliser `Response.json()` (Web API), jamais `NextResponse.json()`.
  </action>
  <verify>
    <automated>cd /home/ubuntu/Custom-ToDoList && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <done>
    - `src/app/api/workspaces/route.ts` : GET et POST fonctionnels avec auth check
    - `src/app/api/workspaces/[id]/route.ts` : GET, PATCH, DELETE avec vérification membership et rôle OWNER
    - `src/app/api/workspaces/[id]/members/route.ts` : GET membres et POST invitation par email
    - Toutes les routes vérifient l'auth en premier (401 si non connecté)
    - Toutes les routes `[id]` vérifient le membership (403 si non membre)
    - PATCH et DELETE vérifient le rôle OWNER (403 si MEMBER)
    - `await params` utilisé dans toutes les routes dynamiques
    - `Response.json()` utilisé partout (pas `NextResponse.json()`)
    - `npx tsc --noEmit` sans erreur
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API routes `/api/workspaces/*` | Requêtes authentifiées — cookie de session validé par NextAuth |
| API route → Prisma | Requêtes DB — toujours filtrées par userId vérifié en session |
| Invitation → email lookup | L'email fourni doit correspondre à un compte existant — pas de création de compte implicite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Spoofing | `src/proxy.ts` | mitigate | Vérification de session via `auth()` NextAuth avant chaque route protégée — session validée par NEXTAUTH_SECRET |
| T-02-06 | Elevation of Privilege | `PATCH /api/workspaces/[id]` | mitigate | Vérification `member.role === "OWNER"` avant toute modification — un MEMBER ne peut pas renommer ou supprimer |
| T-02-07 | Information Disclosure | `GET /api/workspaces/[id]` | mitigate | `requireMembership()` vérifié avant de retourner les données — 403 immédiat si non membre |
| T-02-08 | Tampering | `POST /api/workspaces/[id]/members` (invitation) | mitigate | Seul l'OWNER peut inviter, vérification `self.role !== "OWNER"`. L'invité doit avoir un compte existant — pas de création implicite |
| T-02-09 | Information Disclosure | `GET /api/workspaces/[id]/members` | mitigate | Liste membres exposée uniquement aux membres du workspace (vérification self membership) |
| T-02-10 | Denial of Service | Validation des inputs | mitigate | Validation longueur du nom workspace (max 100 chars), email validé via `includes("@")` |
</threat_model>

<verification>
Après exécution complète du plan :

1. `npx tsc --noEmit` — aucune erreur TypeScript
2. `npm run build` — build réussi
3. Démarrer `npm run dev` et vérifier :
   - `curl http://localhost:3000/dashboard` → redirige vers `/auth/signin` (proxy fonctionne)
   - `curl http://localhost:3000/api/workspaces` → `{ "error": "Non authentifié" }` (401)
   - `curl http://localhost:3000/api/auth/providers` → JSON avec `google` et `github`
4. Vérifier que `src/proxy.ts` existe (pas `src/middleware.ts`)
</verification>

<success_criteria>
- `src/proxy.ts` protège toutes les routes sauf `/`, `/auth/signin`, `/api/auth/*`
- `GET /api/workspaces` retourne les workspaces de l'user connecté uniquement
- `POST /api/workspaces` crée un workspace avec l'user comme OWNER
- `GET/PATCH/DELETE /api/workspaces/[id]` refusent l'accès si l'user n'est pas membre (403)
- `PATCH/DELETE` refusent si l'user n'est pas OWNER (403)
- `POST /api/workspaces/[id]/members` invite par email (OWNER requis)
- Isolation totale : aucune fuite de données cross-workspace
</success_criteria>

<output>
Après complétion, créer `.planning/phases/02-auth-workspaces/02-02-SUMMARY.md` avec :
- Fichiers créés et leur rôle
- Endpoints API disponibles (méthodes + URLs)
- Sécurité : isolation workspace vérifiée
</output>
