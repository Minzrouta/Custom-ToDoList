---
phase: 03-core-task-management
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/components/tasks/KanbanColumn.tsx
  - src/components/tasks/KanbanBoard.tsx
  - src/app/workspace/[id]/kanban/page.tsx
autonomous: true
requirements:
  - FR-06

must_haves:
  truths:
    - "La page /workspace/[id]/kanban affiche 4 colonnes (À faire, En cours, Terminé, Annulé)"
    - "Chaque colonne affiche les tâches correspondant à son statut"
    - "Glisser une tâche vers une autre colonne met à jour son statut via PATCH /api/workspaces/[id]/tasks/[taskId]"
    - "Mise à jour optimiste : la tâche change de colonne immédiatement avant confirmation serveur"
    - "En cas d'erreur réseau, la tâche revient dans sa colonne d'origine (rollback)"
    - "router.refresh() appelé après succès pour synchroniser les données server"
    - "Un bouton '+ Nouvelle tâche' est visible (wiring TaskModal en plan 04)"
  artifacts:
    - path: "src/components/tasks/KanbanColumn.tsx"
      provides: "Colonne droppable avec liste de TaskCards"
      exports: ["KanbanColumn"]
    - path: "src/components/tasks/KanbanBoard.tsx"
      provides: "Board client avec DndContext, gestion drag end, état optimiste"
      exports: ["KanbanBoard"]
    - path: "src/app/workspace/[id]/kanban/page.tsx"
      provides: "Page server protégée chargeant les tâches groupées par statut"
      contains: "WorkspaceKanbanPage"
  key_links:
    - from: "src/components/tasks/KanbanBoard.tsx"
      to: "/api/workspaces/[id]/tasks/[taskId]"
      via: "PATCH fetch avec { status: newStatus }"
      pattern: "fetch.*PATCH.*tasks"
    - from: "src/components/tasks/KanbanColumn.tsx"
      to: "@dnd-kit/core"
      via: "useDroppable({ id: status })"
      pattern: "useDroppable"
    - from: "src/components/tasks/KanbanBoard.tsx"
      to: "src/components/tasks/KanbanColumn.tsx"
      via: "COLUMNS.map"
      pattern: "KanbanColumn"
---

<objective>
Vue Kanban drag & drop : installation de @dnd-kit, composant KanbanBoard client avec
DndContext, colonnes droppables (KanbanColumn), cards draggables (TaskCard), et
mise à jour optimiste du statut via API.

Purpose: Second mode d'affichage des tâches — vue visuelle par statut avec drag & drop natif React.
Output: Page /workspace/[id]/kanban avec Kanban fonctionnel, drag between columns, PATCH statut.
</objective>

<execution_context>
@/home/ubuntu/Custom-ToDoList/.planning/phases/03-core-task-management/03-CONTEXT.md
@/home/ubuntu/Custom-ToDoList/.planning/phases/03-core-task-management/03-PATTERNS.md
@/home/ubuntu/Custom-ToDoList/.planning/phases/03-core-task-management/03-01-SUMMARY.md
</execution_context>

<context>
@/home/ubuntu/Custom-ToDoList/.planning/ROADMAP.md
@/home/ubuntu/Custom-ToDoList/src/app/workspace/[id]/page.tsx
@/home/ubuntu/Custom-ToDoList/src/components/ui/WorkspaceActions.tsx
@/home/ubuntu/Custom-ToDoList/src/components/tasks/TaskCard.tsx

<interfaces>
<!-- dnd-kit API à utiliser -->

Installation :
```bash
npm install @dnd-kit/core @dnd-kit/sortable
```

Imports depuis @dnd-kit/core :
```typescript
import {
  DndContext,
  DragEndEvent,
  DragOverlay,
  PointerSensor,
  useSensor,
  useSensors,
} from "@dnd-kit/core";
import { useDroppable } from "@dnd-kit/core";
import { useDraggable } from "@dnd-kit/core";
```

Pattern DndContext (dans KanbanBoard) :
```typescript
const sensors = useSensors(useSensor(PointerSensor, { activationConstraint: { distance: 8 } }));

<DndContext sensors={sensors} onDragEnd={handleDragEnd}>
  {COLUMNS.map((col) => (
    <KanbanColumn key={col.status} status={col.status} label={col.label} tasks={tasks[col.status] ?? []} onTaskClick={...} />
  ))}
</DndContext>
```

Pattern useDroppable (dans KanbanColumn) :
```typescript
const { isOver, setNodeRef } = useDroppable({ id: status });
```

Pattern useDraggable (dans wrapper de TaskCard) :
```typescript
const { attributes, listeners, setNodeRef, transform, isDragging } = useDraggable({
  id: task.id,
  data: { status: task.status },
});
const style = transform
  ? { transform: `translate(${transform.x}px, ${transform.y}px)` }
  : undefined;
```

Pattern DragEndEvent :
```typescript
async function handleDragEnd(event: DragEndEvent) {
  const { active, over } = event;
  if (!over) return;
  const taskId = active.id as string;
  const oldStatus = active.data.current?.status as string;
  const newStatus = over.id as string;
  if (oldStatus === newStatus) return;
  // ... optimistic update + PATCH
}
```

<!-- Type TaskCardData depuis plan 02 -->
```typescript
import { TaskCardData } from "@/components/tasks/TaskCard";
type TaskStatus = "todo" | "in_progress" | "done" | "cancelled";
```

<!-- Pattern PATCH API depuis plan 01 -->
```
PATCH /api/workspaces/[id]/tasks/[taskId]
Body: { status: "in_progress" }
Response: { data: Task }
```

<!-- Pattern router.refresh() -->
```typescript
import { useRouter } from "next/navigation";
const router = useRouter();
// après succès API :
router.refresh();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : Installer dnd-kit et créer KanbanColumn + KanbanBoard</name>
  <files>
    src/components/tasks/KanbanColumn.tsx,
    src/components/tasks/KanbanBoard.tsx
  </files>
  <action>
**Étape A — Installer les dépendances dnd-kit**

```bash
npm install @dnd-kit/core @dnd-kit/sortable
```

Vérifier que les deux packages apparaissent dans package.json avant de continuer.

---

**Étape B — Créer `src/components/tasks/KanbanColumn.tsx`**

```typescript
// src/components/tasks/KanbanColumn.tsx
"use client";

import { useDroppable } from "@dnd-kit/core";
import { useDraggable } from "@dnd-kit/core";
import { cn } from "@/lib/utils";
import { TaskCard, TaskCardData } from "@/components/tasks/TaskCard";

// Wrapper draggable pour chaque TaskCard
function DraggableTaskCard({
  task,
  onTaskClick,
}: {
  task: TaskCardData;
  onTaskClick: (task: TaskCardData) => void;
}) {
  const { attributes, listeners, setNodeRef, transform, isDragging } = useDraggable({
    id: task.id,
    data: { status: task.status },
  });

  const style = transform
    ? { transform: `translate(${transform.x}px, ${transform.y}px)` }
    : undefined;

  return (
    <div
      ref={setNodeRef}
      style={style}
      {...listeners}
      {...attributes}
      className={cn("touch-none", isDragging && "opacity-50 z-50")}
    >
      <TaskCard task={task} onClick={() => onTaskClick(task)} />
    </div>
  );
}

type TaskStatus = "todo" | "in_progress" | "done" | "cancelled";

interface KanbanColumnProps {
  status: TaskStatus;
  label: string;
  color: string;
  tasks: TaskCardData[];
  onTaskClick: (task: TaskCardData | null) => void;
}

export function KanbanColumn({ status, label, color, tasks, onTaskClick }: KanbanColumnProps) {
  const { isOver, setNodeRef } = useDroppable({ id: status });

  return (
    <div className="flex flex-col min-w-[280px] max-w-[320px] flex-1">
      {/* Header de colonne */}
      <div className="flex items-center justify-between mb-3">
        <div className="flex items-center gap-2">
          <span
            className="w-3 h-3 rounded-full flex-shrink-0"
            style={{ backgroundColor: color }}
          />
          <h3 className="text-sm font-semibold text-gray-700 dark:text-gray-300">{label}</h3>
        </div>
        <span className="text-xs text-gray-400 dark:text-gray-600 bg-gray-100 dark:bg-gray-800 px-2 py-0.5 rounded-full">
          {tasks.length}
        </span>
      </div>

      {/* Zone droppable */}
      <div
        ref={setNodeRef}
        className={cn(
          "flex-1 rounded-xl border-2 border-dashed p-2 space-y-2 min-h-[120px] transition-colors duration-150",
          isOver
            ? "border-blue-400 dark:border-blue-500 bg-blue-50 dark:bg-blue-900/20"
            : "border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-900/50"
        )}
      >
        {tasks.map((task) => (
          <DraggableTaskCard key={task.id} task={task} onTaskClick={onTaskClick} />
        ))}
        {tasks.length === 0 && (
          <div className="flex items-center justify-center h-16 text-xs text-gray-400 dark:text-gray-600">
            Déposez une tâche ici
          </div>
        )}
      </div>
    </div>
  );
}
```

---

**Étape C — Créer `src/components/tasks/KanbanBoard.tsx`**

```typescript
// src/components/tasks/KanbanBoard.tsx
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import {
  DndContext,
  DragEndEvent,
  PointerSensor,
  useSensor,
  useSensors,
} from "@dnd-kit/core";
import { KanbanColumn } from "@/components/tasks/KanbanColumn";
import { TaskCardData } from "@/components/tasks/TaskCard";
import { cn } from "@/lib/utils";

type TaskStatus = "todo" | "in_progress" | "done" | "cancelled";

const COLUMNS: { status: TaskStatus; label: string; color: string }[] = [
  { status: "todo", label: "À faire", color: "#6b7280" },
  { status: "in_progress", label: "En cours", color: "#f59e0b" },
  { status: "done", label: "Terminé", color: "#10b981" },
  { status: "cancelled", label: "Annulé", color: "#ef4444" },
];

type TasksByStatus = Record<TaskStatus, TaskCardData[]>;

interface KanbanBoardProps {
  workspaceId: string;
  initialTasks: TasksByStatus;
  onOpenTask: (task: TaskCardData | null) => void;
}

export function KanbanBoard({ workspaceId, initialTasks, onOpenTask }: KanbanBoardProps) {
  const [tasks, setTasks] = useState<TasksByStatus>(initialTasks);
  const router = useRouter();

  const sensors = useSensors(
    useSensor(PointerSensor, { activationConstraint: { distance: 8 } })
  );

  async function handleDragEnd(event: DragEndEvent) {
    const { active, over } = event;
    if (!over) return;

    const taskId = active.id as string;
    const oldStatus = active.data.current?.status as TaskStatus;
    const newStatus = over.id as TaskStatus;

    if (oldStatus === newStatus) return;

    // Trouver la tâche
    const taskToMove = tasks[oldStatus]?.find((t) => t.id === taskId);
    if (!taskToMove) return;

    // Mise à jour optimiste immédiate
    const updatedTask = { ...taskToMove, status: newStatus };
    setTasks((prev) => ({
      ...prev,
      [oldStatus]: prev[oldStatus].filter((t) => t.id !== taskId),
      [newStatus]: [...(prev[newStatus] ?? []), updatedTask],
    }));

    // PATCH vers l'API
    try {
      const res = await fetch(
        `/api/workspaces/${workspaceId}/tasks/${taskId}`,
        {
          method: "PATCH",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ status: newStatus }),
        }
      );

      if (!res.ok) {
        throw new Error(`PATCH failed: ${res.status}`);
      }

      // Synchroniser les données server
      router.refresh();
    } catch (error) {
      console.error("[kanban:drag]", error);
      // Rollback : restaurer l'état initial
      setTasks(initialTasks);
    }
  }

  const totalTasks = Object.values(tasks).reduce((acc, col) => acc + col.length, 0);

  return (
    <div>
      {/* Barre d'actions */}
      <div className="flex items-center justify-between mb-6">
        <p className="text-sm text-gray-500 dark:text-gray-400">
          {totalTasks} tâche{totalTasks !== 1 ? "s" : ""}
        </p>
        <button
          type="button"
          onClick={() => onOpenTask(null)}
          className={cn(
            "px-4 py-2 text-sm rounded-lg bg-blue-600 hover:bg-blue-700",
            "text-white font-medium transition-colors"
          )}
        >
          + Nouvelle tâche
        </button>
      </div>

      {/* Colonnes Kanban */}
      <DndContext sensors={sensors} onDragEnd={handleDragEnd}>
        <div className="flex gap-4 overflow-x-auto pb-4">
          {COLUMNS.map((col) => (
            <KanbanColumn
              key={col.status}
              status={col.status}
              label={col.label}
              color={col.color}
              tasks={tasks[col.status] ?? []}
              onTaskClick={onOpenTask}
            />
          ))}
        </div>
      </DndContext>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>cd /home/ubuntu/Custom-ToDoList && grep -q '"@dnd-kit/core"' package.json && echo "dnd-kit installé" && npx tsc --noEmit 2>&1 | grep -E "KanbanBoard|KanbanColumn" | wc -l | xargs -I{} test {} -eq 0 && echo "Pas d'erreurs TS Kanban"</automated>
  </verify>
  <done>
    - @dnd-kit/core et @dnd-kit/sortable dans package.json
    - src/components/tasks/KanbanColumn.tsx créé avec useDroppable + DraggableTaskCard
    - src/components/tasks/KanbanBoard.tsx créé avec DndContext, handleDragEnd, mise à jour optimiste + rollback
    - Pas d'erreur TypeScript sur ces deux fichiers
  </done>
</task>

<task type="auto">
  <name>Tâche 2 : Page /workspace/[id]/kanban</name>
  <files>src/app/workspace/[id]/kanban/page.tsx</files>
  <action>
Créer `src/app/workspace/[id]/kanban/page.tsx`. Page server qui charge les tâches groupées par statut et les passe au composant client KanbanBoard. Même pattern auth/membership que la page list.

```typescript
// src/app/workspace/[id]/kanban/page.tsx
import { auth } from "@/auth";
import { redirect, notFound } from "next/navigation";
import { prisma } from "@/lib/prisma";
import { Header } from "@/components/ui/Header";
import { KanbanBoard } from "@/components/tasks/KanbanBoard";
import Link from "next/link";

type TaskStatus = "todo" | "in_progress" | "done" | "cancelled";

export default async function WorkspaceKanbanPage({
  params,
}: {
  params: Promise<{ id: string }>;
}) {
  const session = await auth();
  if (!session?.user?.id) redirect("/auth/signin");

  const { id } = await params;

  const membership = await prisma.workspaceMember.findUnique({
    where: { userId_workspaceId: { userId: session.user.id, workspaceId: id } },
    include: { workspace: true },
  });

  if (!membership) notFound();

  // Charger toutes les tâches du workspace avec relations nécessaires
  const tasks = await prisma.task.findMany({
    where: { workspaceId: id },
    include: {
      category: { select: { id: true, name: true, color: true } },
      assignee: { select: { id: true, name: true, image: true } },
      tags: { include: { tag: { select: { id: true, name: true } } } },
      subtasks: { select: { completed: true } },
      _count: { select: { comments: true } },
    },
    orderBy: [{ priority: "desc" }, { createdAt: "desc" }],
  });

  // Grouper par statut pour le Kanban
  const tasksByStatus = {
    todo: tasks.filter((t) => t.status === "todo"),
    in_progress: tasks.filter((t) => t.status === "in_progress"),
    done: tasks.filter((t) => t.status === "done"),
    cancelled: tasks.filter((t) => t.status === "cancelled"),
  } satisfies Record<TaskStatus, typeof tasks>;

  return (
    <div className="min-h-screen bg-gray-50 dark:bg-gray-950">
      <Header user={session.user} />
      <main className="max-w-[1400px] mx-auto px-6 py-10">
        {/* Breadcrumb */}
        <nav className="text-sm text-gray-500 dark:text-gray-400 mb-6">
          <Link href="/dashboard" className="hover:text-blue-600 dark:hover:text-blue-400">
            Mes workspaces
          </Link>
          {" / "}
          <Link href={`/workspace/${id}`} className="hover:text-blue-600 dark:hover:text-blue-400">
            {membership.workspace.name}
          </Link>
          {" / "}
          <span>Kanban</span>
        </nav>

        <div className="flex items-center justify-between mb-6">
          <h1 className="text-2xl font-bold text-gray-900 dark:text-gray-100">
            {membership.workspace.name} — Kanban
          </h1>
          <Link
            href={`/workspace/${id}/list`}
            className="text-sm px-3 py-2 rounded-lg border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-800 transition-colors"
          >
            Vue liste
          </Link>
        </div>

        <KanbanBoard
          workspaceId={id}
          initialTasks={tasksByStatus}
          onOpenTask={() => {
            // TaskModal wiring effectué en plan 04
            // Remplacé par KanbanPageClient en plan 04
          }}
        />
      </main>
    </div>
  );
}
```

**Note sur onOpenTask** : Même approche que la page list (plan 02). Si le compilateur TypeScript refuse une fonction vide dans un Server Component pour ce prop, créer `src/components/tasks/KanbanPageClient.tsx` :

```typescript
// src/components/tasks/KanbanPageClient.tsx — uniquement si nécessaire pour le build
"use client";

import { useState } from "react";
import { KanbanBoard } from "@/components/tasks/KanbanBoard";
import { TaskCardData } from "@/components/tasks/TaskCard";

type TaskStatus = "todo" | "in_progress" | "done" | "cancelled";

interface KanbanPageClientProps {
  workspaceId: string;
  initialTasks: Record<TaskStatus, TaskCardData[]>;
}

export function KanbanPageClient({ workspaceId, initialTasks }: KanbanPageClientProps) {
  const [selectedTask, setSelectedTask] = useState<TaskCardData | null | undefined>(undefined);

  return (
    <KanbanBoard
      workspaceId={workspaceId}
      initialTasks={initialTasks}
      onOpenTask={(task) => setSelectedTask(task ?? null)}
    />
    // TaskModal sera ajouté ici en plan 04
  );
}
```

Puis remplacer `<KanbanBoard ... />` dans la page server par `<KanbanPageClient ... />`. Plan 04 injectera le TaskModal dans ce composant.
  </action>
  <verify>
    <automated>cd /home/ubuntu/Custom-ToDoList && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - src/app/workspace/[id]/kanban/page.tsx créé
    - Page server protégée par auth + membership check
    - Tâches chargées depuis Prisma et groupées par statut (todo/in_progress/done/cancelled)
    - KanbanBoard reçoit les tâches groupées en initialTasks
    - Breadcrumb et lien "Vue liste" présents
    - `npm run build` passe sans erreur
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (drag event) → API PATCH | Le taskId et newStatus viennent du drag event côté client — validés côté serveur |
| Server Component → Client KanbanBoard | Les données Prisma sont sérialisées sans objets non-sérialisables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | handleDragEnd PATCH | mitigate | L'API PATCH (plan 01) vérifie requireMembership et que taskId appartient au workspace via `where: { id: taskId, workspaceId: id }` |
| T-03-11 | Information Disclosure | WorkspaceKanbanPage (server) | mitigate | membership check avant tout fetch — isolation workspace garantie |
| T-03-12 | Denial of Service | DragEnd rapid fire | accept | Pas de debounce en v1 (app auto-hébergée, surface limitée) — chaque drag déclenche un PATCH unique |
| T-03-13 | Tampering | Optimistic update rollback | mitigate | Si PATCH échoue, setTasks(initialTasks) restaure l'état serveur |
</threat_model>

<verification>
1. `npm run build` — aucune erreur
2. `grep -q "@dnd-kit/core" package.json` — confirme l'installation
3. Visiter `/workspace/[id]/kanban` — 4 colonnes visibles (À faire, En cours, Terminé, Annulé)
4. Si des tâches existent (après avoir testé l'API manuellement), elles apparaissent dans la bonne colonne
5. Drag d'une carte vers une autre colonne — la carte se déplace visuellement (hors browser-test automatisé)
6. Le lien "Vue liste" pointe vers `/workspace/[id]/list`
</verification>

<success_criteria>
- @dnd-kit/core et @dnd-kit/sortable installés
- KanbanColumn utilise useDroppable pour accepter les drops
- KanbanBoard gère DndContext, handleDragEnd avec optimistic update + rollback
- Page /workspace/[id]/kanban protégée, charge les données, passe au KanbanBoard
- `npm run build` passe sans erreur
- Architecture prête pour l'injection du TaskModal en plan 04
</success_criteria>

<output>
Après completion, créer `.planning/phases/03-core-task-management/03-03-SUMMARY.md` avec :
- Packages installés : @dnd-kit/core, @dnd-kit/sortable
- Composants créés : KanbanColumn (useDroppable + DraggableTaskCard), KanbanBoard (DndContext + optimistic update)
- Page créée : /workspace/[id]/kanban
- Note sur KanbanPageClient si créé (pour que plan 04 sache où injecter le TaskModal)
- État du build
</output>
