---
phase: 04-discord-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - bot/package.json
  - bot/tsconfig.json
  - bot/Dockerfile
  - bot/.dockerignore
  - bot/src/index.ts
  - bot/src/prisma.ts
  - bot/src/commands/index.ts
  - bot/src/register.ts
  - bot/src/notify-server.ts
  - docker-compose.yml
  - .env.example
autonomous: false
requirements:
  - FR-07
must_haves:
  truths:
    - "Le schema Prisma expose discordId sur User et discordGuildId/discordChannelId sur Workspace"
    - "Le dossier bot/ contient un service Node.js TypeScript indépendant avec son propre package.json"
    - "Le bot Discord se connecte avec discord.js v14 et logue 'Bot ready' au démarrage"
    - "Un script register.ts publie les slash commands /task auprès de Discord"
    - "Un mini-serveur HTTP interne tourne sur le port 8080 avec un endpoint POST /notify"
    - "docker-compose.yml démarre un nouveau service discord-bot connecté au réseau coolify"
    - "Le schema est appliqué en base via prisma db push --accept-data-loss"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Champs Discord ajoutés (discordId, discordGuildId, discordChannelId)"
      contains: "discordGuildId"
    - path: "bot/package.json"
      provides: "Manifeste npm du bot Discord (discord.js v14, prisma client, tsx)"
      contains: "discord.js"
    - path: "bot/tsconfig.json"
      provides: "Config TypeScript du bot (strict, target ES2022, NodeNext)"
      contains: "strict"
    - path: "bot/src/index.ts"
      provides: "Point d'entrée du bot — initialise Client discord.js + serveur HTTP /notify"
      min_lines: 30
    - path: "bot/src/prisma.ts"
      provides: "Singleton PrismaClient pour le bot"
      contains: "PrismaClient"
    - path: "bot/src/commands/index.ts"
      provides: "Définitions SlashCommandBuilder pour /task (squelette + handler stub)"
      contains: "SlashCommandBuilder"
    - path: "bot/src/register.ts"
      provides: "Script d'enregistrement REST PUT des slash commands"
      contains: "Routes.applicationCommands"
    - path: "bot/src/notify-server.ts"
      provides: "Mini serveur HTTP node:http port 8080 endpoint POST /notify"
      contains: "/notify"
    - path: "bot/Dockerfile"
      provides: "Image Docker du bot — multi-stage avec prisma generate"
      contains: "FROM node:20-alpine"
    - path: "docker-compose.yml"
      provides: "Service discord-bot ajouté au compose existant"
      contains: "discord-bot"
  key_links:
    - from: "bot/src/index.ts"
      to: "bot/src/commands/index.ts"
      via: "import + Client.on('interactionCreate')"
      pattern: "interactionCreate"
    - from: "bot/src/index.ts"
      to: "bot/src/notify-server.ts"
      via: "startNotifyServer(client)"
      pattern: "startNotifyServer"
    - from: "bot/src/prisma.ts"
      to: "prisma/schema.prisma"
      via: "import PrismaClient depuis ../node_modules/@prisma/client"
      pattern: "@prisma/client"
    - from: "docker-compose.yml"
      to: "bot/Dockerfile"
      via: "build context: ./bot"
      pattern: "context: ./bot"
---

<objective>
Mettre en place toute la fondation du bot Discord : extension du schema Prisma (3 nouvelles colonnes), structure du dossier `bot/` séparé du Next.js, client discord.js v14 prêt à recevoir des interactions, script d'enregistrement des slash commands, mini-serveur HTTP interne pour les notifications, et nouveau service `discord-bot` dans `docker-compose.yml`.

Purpose: Une fois ce plan complet, le bot peut démarrer (en dev local et en Docker), les commandes peuvent être enregistrées chez Discord, et la base de données accepte les liaisons workspace ↔ guild Discord. Les plans 04-02 et 04-03 viendront greffer la logique métier (commandes + notifications) sur cette fondation.

Output: Schema Prisma à jour (db push appliqué), arborescence `bot/` complète et compilable, image Docker buildable, service compose ajouté.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-discord-integration/04-CONTEXT.md
@.planning/phases/02-auth-workspaces/02-PATTERNS.md
@.planning/phases/03-core-task-management/03-PATTERNS.md
@prisma/schema.prisma
@docker-compose.yml
@Dockerfile
@.env.example
@package.json

<interfaces>
<!-- Schema Prisma actuel — à étendre, NON à réécrire -->
<!-- Modèles à modifier : User (ajouter discordId), Workspace (ajouter discordGuildId, discordChannelId) -->
<!-- Tous les autres modèles (Task, Category, Tag, SubTask, Comment, Account, Session...) doivent rester strictement identiques -->

User (extrait actuel) :
```prisma
model User {
  id            String    @id @default(cuid())
  name          String?
  email         String    @unique
  emailVerified DateTime?
  image         String?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  workspaces   WorkspaceMember[]
  accounts     Account[]
  sessions     Session[]
  tasks        Task[]    @relation("AssignedTasks")
  createdTasks Task[]    @relation("CreatedTasks")
  comments     Comment[]

  @@map("users")
}
```

Workspace (extrait actuel) :
```prisma
model Workspace {
  id          String   @id @default(cuid())
  name        String
  description String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  members    WorkspaceMember[]
  tasks      Task[]
  categories Category[]
  tags       Tag[]

  @@map("workspaces")
}
```

docker-compose.yml — service `web` existant (NE PAS modifier sa logique, juste ajouter le service `discord-bot` à côté). Le réseau `coolify` est déjà déclaré comme external.

Dockerfile racine — multi-stage avec deps/builder/runner. Le bot aura SON PROPRE Dockerfile (image séparée), n'altère pas le Dockerfile racine.

Le client Prisma est généré dans `node_modules/@prisma/client` à la racine. Le bot, étant dans `bot/`, devra avoir accès au schema (montage volume ou copie au build).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Étendre le schema Prisma avec les champs Discord</name>
  <files>prisma/schema.prisma</files>
  <action>
Modifier `prisma/schema.prisma` pour ajouter les champs Discord aux modèles User et Workspace, sans toucher aux autres modèles.

**Ajouts précis (utiliser Edit tool, ne pas réécrire le fichier) :**

1. Dans `model User`, après la ligne `image String?` et avant `createdAt`, ajouter :
```prisma
  discordId     String?   @unique
```

2. Dans `model Workspace`, après la ligne `description String?` et avant `createdAt`, ajouter :
```prisma
  discordGuildId   String?  @unique
  discordChannelId String?
```

**Règles à respecter :**
- Ne pas modifier les autres modèles (Task, Category, Tag, SubTask, Comment, Account, Session, VerificationToken, WorkspaceMember).
- Ne pas modifier les enums.
- Conserver les `@@map` existants.
- `discordId` et `discordGuildId` sont `@unique` (un user app = un user Discord, un workspace = un guild Discord).
- `discordChannelId` n'est PAS unique (plusieurs workspaces pourraient théoriquement utiliser le même channel — mais la contrainte forte est sur guildId).

**Après modification du schema, exécuter :**
```bash
npx prisma generate
```
pour régénérer le client TypeScript et permettre la compilation des plans suivants.
  </action>
  <verify>
    <automated>grep -q "discordId\s*String?\s*@unique" prisma/schema.prisma && grep -q "discordGuildId\s*String?\s*@unique" prisma/schema.prisma && grep -q "discordChannelId\s*String?" prisma/schema.prisma && npx prisma validate</automated>
  </verify>
  <done>Schema valide, `npx prisma validate` retourne success, `npx prisma generate` produit un client incluant les nouveaux champs.</done>
</task>

<task type="auto">
  <name>Task 2: Créer la structure du dossier bot/ (package.json, tsconfig, Dockerfile, .dockerignore, prisma singleton)</name>
  <files>bot/package.json, bot/tsconfig.json, bot/Dockerfile, bot/.dockerignore, bot/src/prisma.ts</files>
  <action>
Créer le dossier `bot/` à la racine du repo (frère de `src/`) avec un package npm autonome utilisant discord.js v14.

**1. `bot/package.json` — manifeste autonome :**
```json
{
  "name": "custom-todolist-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "register": "tsx src/register.ts",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "discord.js": "^14.16.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.19.2",
    "typescript": "^5"
  }
}
```

**2. `bot/tsconfig.json` — config strict TypeScript ES modules :**
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "noEmit": true,
    "isolatedModules": true,
    "lib": ["ES2022"]
  },
  "include": ["src/**/*"]
}
```

**3. `bot/Dockerfile` — image multi-stage, partage le schema Prisma :**
```dockerfile
# bot/Dockerfile
# Build context = repo racine (voir docker-compose.yml: build.context: .)
# Permet d'accéder au prisma/schema.prisma partagé.

FROM node:20-alpine AS deps
RUN apk add --no-cache libc6-compat openssl
WORKDIR /app

COPY bot/package.json bot/package-lock.json* ./
RUN npm install --no-audit --no-fund

FROM node:20-alpine AS runner
RUN apk add --no-cache openssl
WORKDIR /app

ENV NODE_ENV=production

# Copier dépendances installées
COPY --from=deps /app/node_modules ./node_modules

# Copier le schema Prisma partagé (depuis la racine du repo)
COPY prisma ./prisma

# Générer le client Prisma à l'intérieur du conteneur
RUN npx --yes prisma@5.22.0 generate --schema=./prisma/schema.prisma

# Copier le code du bot
COPY bot/tsconfig.json ./tsconfig.json
COPY bot/src ./src

# Port HTTP interne pour les notifications (non exposé publiquement)
EXPOSE 8080

CMD ["npx", "--yes", "tsx", "src/index.ts"]
```

**4. `bot/.dockerignore` :**
```
node_modules
npm-debug.log
.env
.env.local
dist
*.log
```

**5. `bot/src/prisma.ts` — singleton PrismaClient (pattern repris de src/lib/prisma.ts) :**
```typescript
// bot/src/prisma.ts
import { PrismaClient } from "@prisma/client";

const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined;
};

export const prisma =
  globalForPrisma.prisma ??
  new PrismaClient({
    log: process.env.NODE_ENV === "development" ? ["query", "error", "warn"] : ["error"],
  });

if (process.env.NODE_ENV !== "production") globalForPrisma.prisma = prisma;
```

**Notes critiques :**
- Le bot utilise ES modules (`"type": "module"` + `"module": "NodeNext"`) — pas de `require()`.
- Le bot ne committe PAS son `node_modules` ni son `package-lock.json` séparé (npm install au build Docker).
- Le bot importe `@prisma/client` qui sera régénéré à l'intérieur du conteneur via `prisma generate` (le schema est copié depuis la racine).
- En dev local, l'utilisateur lancera `npm install` dans `bot/` puis `npm run register` puis `npm run dev`.
  </action>
  <verify>
    <automated>test -f bot/package.json && test -f bot/tsconfig.json && test -f bot/Dockerfile && test -f bot/.dockerignore && test -f bot/src/prisma.ts && grep -q '"discord.js"' bot/package.json && grep -q '"strict": true' bot/tsconfig.json && grep -q "prisma generate" bot/Dockerfile && grep -q "PrismaClient" bot/src/prisma.ts</automated>
  </verify>
  <done>Dossier `bot/` créé avec les 5 fichiers fondateurs. Le `bot/package.json` déclare discord.js v14, @prisma/client, tsx. Le Dockerfile copie le schema racine et exécute prisma generate.</done>
</task>

<task type="auto">
  <name>Task 3: Créer le client discord.js, les définitions de slash commands (squelettes), le serveur HTTP /notify, et le script register</name>
  <files>bot/src/index.ts, bot/src/commands/index.ts, bot/src/register.ts, bot/src/notify-server.ts</files>
  <action>
Créer le squelette complet du bot avec discord.js v14. Les handlers de commande sont des STUBS dans ce plan — la logique métier (création/listing/done de tâches + notifications) est livrée par les plans 04-02 et 04-03. Ici on garantit juste que le bot démarre, écoute les interactions, et expose /notify.

**1. `bot/src/commands/index.ts` — définitions des slash commands :**
```typescript
// bot/src/commands/index.ts
import {
  SlashCommandBuilder,
  type ChatInputCommandInteraction,
} from "discord.js";

/**
 * Slash command unique /task avec 3 sous-commandes : add, list, done.
 * Les handlers sont des stubs ici — implémentation réelle en plan 04-02.
 */
export const taskCommand = new SlashCommandBuilder()
  .setName("task")
  .setDescription("Gérer les tâches du workspace lié à ce serveur Discord")
  .addSubcommand((sub) =>
    sub
      .setName("add")
      .setDescription("Créer une nouvelle tâche")
      .addStringOption((opt) =>
        opt.setName("titre").setDescription("Titre de la tâche").setRequired(true),
      )
      .addStringOption((opt) =>
        opt
          .setName("priorite")
          .setDescription("Priorité de la tâche")
          .addChoices(
            { name: "low", value: "low" },
            { name: "medium", value: "medium" },
            { name: "high", value: "high" },
            { name: "urgent", value: "urgent" },
          ),
      )
      .addStringOption((opt) =>
        opt.setName("categorie").setDescription("Nom d'une catégorie existante"),
      ),
  )
  .addSubcommand((sub) =>
    sub
      .setName("list")
      .setDescription("Lister les tâches en cours")
      .addStringOption((opt) =>
        opt
          .setName("statut")
          .setDescription("Filtrer par statut")
          .addChoices(
            { name: "todo", value: "todo" },
            { name: "in_progress", value: "in_progress" },
          ),
      ),
  )
  .addSubcommand((sub) =>
    sub
      .setName("done")
      .setDescription("Marquer une tâche comme terminée")
      .addStringOption((opt) =>
        opt
          .setName("id")
          .setDescription("ID (ou préfixe) de la tâche")
          .setRequired(true),
      ),
  );

export const allCommands = [taskCommand];

/**
 * Dispatcher principal — appelé depuis index.ts sur chaque interaction.
 * En 04-01, retourne un message éphémère "Pas encore implémenté".
 * En 04-02, ce stub sera remplacé par les vrais handlers.
 */
export async function handleTaskInteraction(
  interaction: ChatInputCommandInteraction,
): Promise<void> {
  const sub = interaction.options.getSubcommand();
  await interaction.reply({
    content: `[stub 04-01] /task ${sub} reçu — implémentation en plan 04-02.`,
    ephemeral: true,
  });
}
```

**2. `bot/src/notify-server.ts` — mini serveur HTTP interne port 8080 :**
```typescript
// bot/src/notify-server.ts
import { createServer, type IncomingMessage, type ServerResponse } from "node:http";
import type { Client } from "discord.js";

/**
 * Serveur HTTP interne (réseau Docker) — l'app Next.js POST /notify pour
 * envoyer un message dans un channel Discord configuré.
 * Port 8080 NON exposé publiquement (pas de port mapping dans compose).
 *
 * En 04-01 : reçoit la requête, log, répond 202 Accepted (stub).
 * En 04-03 : route vers le bon channel et envoie un embed.
 */
export function startNotifyServer(client: Client): void {
  const port = Number(process.env.NOTIFY_PORT ?? 8080);

  const server = createServer(async (req: IncomingMessage, res: ServerResponse) => {
    if (req.method === "POST" && req.url === "/notify") {
      let body = "";
      req.on("data", (chunk) => {
        body += chunk;
      });
      req.on("end", async () => {
        try {
          const payload = body ? JSON.parse(body) : {};
          console.log("[notify] payload reçu:", payload);
          // Implémentation réelle en 04-03 — pour l'instant on accepte sans agir
          void client; // évite warning unused — sera utilisé en 04-03
          res.writeHead(202, { "Content-Type": "application/json" });
          res.end(JSON.stringify({ status: "accepted" }));
        } catch (err) {
          console.error("[notify] payload invalide:", err);
          res.writeHead(400, { "Content-Type": "application/json" });
          res.end(JSON.stringify({ error: "Invalid JSON" }));
        }
      });
      return;
    }

    if (req.method === "GET" && req.url === "/health") {
      res.writeHead(200, { "Content-Type": "application/json" });
      res.end(JSON.stringify({ status: "ok", botReady: client.isReady() }));
      return;
    }

    res.writeHead(404, { "Content-Type": "application/json" });
    res.end(JSON.stringify({ error: "Not found" }));
  });

  server.listen(port, "0.0.0.0", () => {
    console.log(`[notify] Server listening on :${port}`);
  });
}
```

**3. `bot/src/index.ts` — point d'entrée, client Discord + serveur HTTP :**
```typescript
// bot/src/index.ts
import { Client, GatewayIntentBits, Events, Interaction } from "discord.js";
import { handleTaskInteraction } from "./commands/index.js";
import { startNotifyServer } from "./notify-server.js";

const token = process.env.DISCORD_TOKEN;
if (!token) {
  console.error("[bot] DISCORD_TOKEN manquant dans l'environnement");
  process.exit(1);
}

const client = new Client({
  intents: [GatewayIntentBits.Guilds],
});

client.once(Events.ClientReady, (c) => {
  console.log(`[bot] Ready — connecté en tant que ${c.user.tag}`);
});

client.on(Events.InteractionCreate, async (interaction: Interaction) => {
  if (!interaction.isChatInputCommand()) return;
  if (interaction.commandName !== "task") return;

  try {
    await handleTaskInteraction(interaction);
  } catch (err) {
    console.error("[bot] Erreur dans le handler:", err);
    if (interaction.deferred || interaction.replied) {
      await interaction.followUp({ content: "Erreur interne", ephemeral: true });
    } else {
      await interaction.reply({ content: "Erreur interne", ephemeral: true });
    }
  }
});

// Démarrer le serveur HTTP /notify AVANT le login Discord
startNotifyServer(client);

client.login(token).catch((err) => {
  console.error("[bot] Login Discord échoué:", err);
  process.exit(1);
});
```

**4. `bot/src/register.ts` — enregistrement REST des slash commands :**
```typescript
// bot/src/register.ts
// Usage : npm run register
//   - Si DISCORD_GUILD_ID est défini, enregistre dans ce guild (instantané, pour dev)
//   - Sinon enregistre globalement (peut prendre jusqu'à 1h pour propager)
import { REST, Routes } from "discord.js";
import { allCommands } from "./commands/index.js";

const token = process.env.DISCORD_TOKEN;
const clientId = process.env.DISCORD_CLIENT_ID;
const guildId = process.env.DISCORD_GUILD_ID;

if (!token || !clientId) {
  console.error("[register] DISCORD_TOKEN et DISCORD_CLIENT_ID requis");
  process.exit(1);
}

const rest = new REST({ version: "10" }).setToken(token);
const body = allCommands.map((c) => c.toJSON());

(async () => {
  try {
    if (guildId) {
      console.log(`[register] Enregistrement guild ${guildId}...`);
      await rest.put(Routes.applicationGuildCommands(clientId, guildId), { body });
      console.log("[register] Commandes enregistrées dans le guild.");
    } else {
      console.log("[register] Enregistrement global...");
      await rest.put(Routes.applicationCommands(clientId), { body });
      console.log("[register] Commandes enregistrées globalement (propagation jusqu'à 1h).");
    }
  } catch (err) {
    console.error("[register] Erreur:", err);
    process.exit(1);
  }
})();
```

**Notes critiques :**
- Imports avec extension `.js` (NodeNext ESM resolution requiert l'extension explicite, même pour des sources `.ts`).
- Intent `Guilds` suffit pour les slash commands (pas besoin de `MessageContent`).
- Le serveur HTTP démarre AVANT `client.login()` pour que le healthcheck Docker puisse passer même si Discord est lent.
- `ephemeral: true` partout : les réponses sont privées au user qui a invoqué la commande.
  </action>
  <verify>
    <automated>test -f bot/src/index.ts && test -f bot/src/commands/index.ts && test -f bot/src/register.ts && test -f bot/src/notify-server.ts && grep -q "GatewayIntentBits.Guilds" bot/src/index.ts && grep -q "SlashCommandBuilder" bot/src/commands/index.ts && grep -q "Routes.applicationCommands" bot/src/register.ts && grep -q '"/notify"' bot/src/notify-server.ts && cd bot && npm install --no-audit --no-fund 2>&1 | tail -5 && npx tsc --noEmit</automated>
  </verify>
  <done>Les 4 fichiers sources existent, le `npm install` dans `bot/` réussit, `tsc --noEmit` ne retourne aucune erreur, le bot pourrait théoriquement démarrer (token requis pour test live, voir tâche 5).</done>
</task>

<task type="auto">
  <name>Task 4: Ajouter le service discord-bot dans docker-compose.yml et étendre .env.example</name>
  <files>docker-compose.yml, .env.example</files>
  <action>
Ajouter un service `discord-bot` à `docker-compose.yml` (ne pas réécrire les services existants `web` et `database`) et compléter `.env.example` avec les variables Discord.

**1. Modifier `docker-compose.yml` — ajouter le service `discord-bot` après `database` et avant `volumes:` :**

Le service doit :
- Builder depuis le contexte racine (`.`) avec `dockerfile: bot/Dockerfile` (pour accéder à `prisma/schema.prisma`).
- Lire `DATABASE_URL`, `DISCORD_TOKEN`, `DISCORD_CLIENT_ID`, `DISCORD_GUILD_ID` (optionnel) depuis l'environnement Coolify.
- Dépendre de `database` (healthy) — il a besoin de la DB pour fonctionner.
- Être connecté au réseau `coolify`.
- Exposer en INTERNE le port 8080 (pas de port mapping public — l'app Next.js l'appelle via `discord-bot:8080`).
- Avoir un healthcheck sur `/health`.
- Restart unless-stopped.
- Label `traefik.enable=false` (le bot n'est PAS routé par Traefik).

Bloc à insérer (à ajouter avec Edit, sans toucher au reste) :
```yaml
  discord-bot:
    build:
      context: .
      dockerfile: bot/Dockerfile
    environment:
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      DISCORD_TOKEN: ${DISCORD_TOKEN}
      DISCORD_CLIENT_ID: ${DISCORD_CLIENT_ID}
      DISCORD_GUILD_ID: ${DISCORD_GUILD_ID:-}
      NOTIFY_PORT: 8080
    depends_on:
      database:
        condition: service_healthy
    networks:
      - coolify
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    labels:
      # Le bot n'est PAS exposé publiquement — désactive Traefik pour ce service
      - "traefik.enable=false"
    restart: unless-stopped
```

**Important :**
- Ne PAS ajouter de `ports:` mapping pour 8080 — accès interne au réseau Docker uniquement.
- Le service `web` parlera à `http://discord-bot:8080/notify` (DNS Docker résout le nom de service).
- Conserver le commentaire d'en-tête, les services existants, le bloc `volumes:` et `networks:` à la fin.
- Le healthcheck utilise `wget` (présent dans `node:20-alpine`).

**2. Modifier `.env.example` — ajouter à la fin du fichier :**
```bash

# Discord bot (service discord-bot)
DISCORD_TOKEN="your-bot-token-from-discord-developer-portal"
DISCORD_CLIENT_ID="your-application-id"
# DISCORD_GUILD_ID est optionnel — si défini, register.ts enregistre les commandes dans ce guild uniquement (instantané, dev)
DISCORD_GUILD_ID=""
```
  </action>
  <verify>
    <automated>grep -q "discord-bot:" docker-compose.yml && grep -q "dockerfile: bot/Dockerfile" docker-compose.yml && grep -q "NOTIFY_PORT: 8080" docker-compose.yml && grep -q "traefik.enable=false" docker-compose.yml && ! grep -A2 "discord-bot:" docker-compose.yml | grep -q "ports:" && grep -q "DISCORD_TOKEN" .env.example && grep -q "DISCORD_CLIENT_ID" .env.example</automated>
  </verify>
  <done>`docker-compose.yml` contient le service `discord-bot` avec build context racine, sans port mapping public, avec healthcheck et `traefik.enable=false`. Le service `web` et `database` restent inchangés. `.env.example` documente les 3 variables Discord.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: [BLOCKING] Appliquer le schema Prisma à la base de données</name>
  <what-built>
Les tâches 1-4 ont étendu le schema Prisma (3 nouvelles colonnes Discord) et créé toute l'infrastructure du bot. Le schema TypeScript (client Prisma régénéré) compile, mais la base PostgreSQL n'a PAS encore les nouvelles colonnes — toute requête utilisant `discordId`, `discordGuildId` ou `discordChannelId` planterait à l'exécution.

Cette étape applique le schema à la DB. **Bloquant** : sans elle, les plans 04-02 et 04-03 échoueront à runtime.
  </what-built>
  <how-to-verify>
1. **Vérifier que `DATABASE_URL` est joignable.** En local : la DB Docker doit tourner (`docker compose up database -d` ou équivalent). En CI/prod : Coolify gère.

2. **Lancer la commande de push (depuis la racine du repo) :**
```bash
npx prisma db push --accept-data-loss
```
- `--accept-data-loss` est ici sans risque : on AJOUTE des colonnes nullables, pas de drop ni de cast.

3. **Sortie attendue :**
```
Datasource "db": PostgreSQL database "custom_todolist" ...
🚀 Your database is now in sync with your Prisma schema. Done in XXXms
✔ Generated Prisma Client (vX.Y.Z) ...
```

4. **Validation rapide :**
```bash
npx prisma db pull --print 2>&1 | grep -E "discordId|discordGuildId|discordChannelId"
```
Doit afficher les 3 lignes correspondant aux nouvelles colonnes.

5. **Si la DB locale n'est pas accessible** (cas connu — voir STATE.md "db push bloqué localhost:5432 inaccessible"), confirmer que vous comprenez que les plans 04-02/04-03 nécessiteront ce push avant exécution. Dans ce cas répondre "DB inaccessible — push différé" et le push sera tenté en début de plan 04-02.
  </how-to-verify>
  <resume-signal>Tapez "schema-pushed" si la commande a réussi, "DB inaccessible — push différé" si la DB locale n'est pas joignable (plans 04-02/04-03 retenteront), ou décrivez l'erreur rencontrée.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord ↔ bot | Discord envoie des interactions signées via WebSocket — discord.js valide la signature |
| app Next.js ↔ bot | App POST sur `discord-bot:8080/notify` via réseau Docker interne (non exposé publiquement) |
| bot ↔ DB | Bot lit/écrit Prisma — partage la DB avec l'app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | I (Information disclosure) | bot/src/notify-server.ts port 8080 | mitigate | Pas de port mapping public dans compose — accès UNIQUEMENT via réseau Docker `coolify`. `traefik.enable=false` empêche un routage accidentel. |
| T-04-02 | T (Tampering) | DISCORD_TOKEN dans env | mitigate | Token JAMAIS committé. `.env.example` contient un placeholder. Coolify injecte la vraie valeur via env vars. |
| T-04-03 | E (Elevation) | bot lit la DB avec les mêmes credentials que l'app | accept | Le bot a besoin d'accès lecture/écriture aux Workspace/Task/User. Isolation logique via discordGuildId comme clé de filtre — implémentée en plan 04-02. |
| T-04-04 | D (DoS) | endpoint /notify sans auth | mitigate (partielle) | Réseau Docker interne uniquement (T-04-01). Plan 04-03 peut ajouter un secret partagé `BOT_NOTIFY_SECRET` si besoin. v1 : la mitigation réseau suffit. |
| T-04-05 | S (Spoofing) | Tout user Discord peut invoquer /task | mitigate (logique métier) | Implémentée en 04-02 : interaction.guildId DOIT correspondre à un Workspace.discordGuildId, et interaction.user.id DOIT être lié à un User.discordId. |
</threat_model>

<verification>
**Niveau plan 04-01 (vérification automatique en fin de plan) :**
1. `npx prisma validate` retourne success.
2. `npx prisma generate` produit un client incluant `discordId`, `discordGuildId`, `discordChannelId`.
3. `cd bot && npm install && npx tsc --noEmit` passe sans erreur.
4. `docker compose config` parse sans erreur le compose modifié.
5. `grep -c "^services:" docker-compose.yml` retourne `1` (section services unique).
6. Les 3 services `web`, `database`, `discord-bot` sont déclarés dans le compose.
7. Le checkpoint humain Task 5 confirme l'application du schema (ou marque le report explicite).

**Vérification fonctionnelle (différée — non bloquante pour ce plan) :**
- Build de l'image Docker du bot : `docker compose build discord-bot` réussit.
- En setting un DISCORD_TOKEN valide en dev local : `cd bot && npm run dev` log "Bot ready" et "[notify] Server listening on :8080".
- `curl http://localhost:8080/health` (depuis l'intérieur du conteneur web ou en dev local sans Docker) retourne `{ "status": "ok", "botReady": true }`.
</verification>

<success_criteria>
1. `prisma/schema.prisma` étendu avec discordId (User), discordGuildId + discordChannelId (Workspace) — autres modèles intacts.
2. `bot/` est un module npm autonome avec discord.js v14, tsx, @prisma/client.
3. `bot/src/index.ts` initialise un Client discord.js avec intent Guilds, écoute `InteractionCreate` pour `/task`, et démarre le serveur /notify.
4. `bot/src/commands/index.ts` définit la slash command `/task` avec sous-commandes `add`, `list`, `done` (handler stub renvoyant "[stub 04-01]").
5. `bot/src/register.ts` enregistre les commandes via REST (guild ou global selon DISCORD_GUILD_ID).
6. `bot/src/notify-server.ts` expose POST /notify et GET /health sur port 8080 (interne).
7. `bot/Dockerfile` build une image fonctionnelle qui inclut le client Prisma régénéré.
8. `docker-compose.yml` déclare le service `discord-bot` connecté au réseau coolify, dépendant de `database` healthy, sans port mapping public, avec healthcheck.
9. `.env.example` documente DISCORD_TOKEN, DISCORD_CLIENT_ID, DISCORD_GUILD_ID.
10. Le schema est appliqué en base via `prisma db push --accept-data-loss` (ou report explicite consigné).
</success_criteria>

<output>
After completion, create `.planning/phases/04-discord-integration/04-01-SUMMARY.md` documenting :
- Schema diff appliqué (3 colonnes ajoutées)
- Commande exacte du db push exécutée + résultat (ou raison du report)
- Liste des fichiers créés dans `bot/`
- Service `discord-bot` ajouté au compose (rappel : pas de port mapping public)
- Variables d'environnement à provisionner dans Coolify avant le déploiement (DISCORD_TOKEN, DISCORD_CLIENT_ID, optionnel DISCORD_GUILD_ID)
- Note pour 04-02 : les handlers de slash commands sont des stubs, l'implémentation logique vient ensuite.
- Note pour 04-03 : `notify-server.ts` accepte les payloads mais ne route pas encore vers les channels — implémentation en 04-03.
</output>
</content>
</invoke>