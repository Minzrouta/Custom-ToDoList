---
phase: 04-discord-integration
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - bot/src/commands/index.ts
  - bot/src/commands/task-add.ts
  - bot/src/commands/task-list.ts
  - bot/src/commands/task-done.ts
  - bot/src/lib/resolve-context.ts
  - bot/src/lib/embeds.ts
autonomous: true
requirements:
  - FR-07
must_haves:
  truths:
    - "/task add <titre> crée une tâche dans le workspace lié au guild Discord et confirme avec un embed"
    - "/task list affiche jusqu'à 10 tâches en cours du workspace lié sous forme d'embed Discord"
    - "/task done <id> marque la tâche comme done si elle appartient au workspace lié"
    - "Si le guild Discord n'est lié à aucun Workspace, le bot répond avec un message d'erreur clair"
    - "Si l'user Discord n'est pas lié à un User app, le bot répond avec un message d'erreur clair (lien settings)"
    - "Les embeds utilisent une couleur selon priorité (urgent=rouge, high=orange, medium=bleu, low=gris)"
    - "L'argument id de /task done accepte un préfixe court (8 chars) via Prisma startsWith"
  artifacts:
    - path: "bot/src/commands/task-add.ts"
      provides: "Handler /task add — création de tâche via prisma"
      min_lines: 40
    - path: "bot/src/commands/task-list.ts"
      provides: "Handler /task list — liste des tâches en embed"
      min_lines: 40
    - path: "bot/src/commands/task-done.ts"
      provides: "Handler /task done — passage statut done"
      min_lines: 30
    - path: "bot/src/lib/resolve-context.ts"
      provides: "Helper qui resout (guildId, userId Discord) vers (workspace, user app) avec messages d'erreur normalises"
      contains: "discordGuildId"
    - path: "bot/src/lib/embeds.ts"
      provides: "Builders d'embeds Discord avec couleur selon priorite"
      contains: "EmbedBuilder"
  key_links:
    - from: "bot/src/commands/index.ts"
      to: "bot/src/commands/task-add.ts"
      via: "import + dispatch sur subcommand 'add'"
      pattern: "task-add"
    - from: "bot/src/commands/task-add.ts"
      to: "bot/src/lib/resolve-context.ts"
      via: "resolveContext(interaction)"
      pattern: "resolveContext"
    - from: "bot/src/commands/task-add.ts"
      to: "bot/src/prisma.ts"
      via: "prisma.task.create"
      pattern: "prisma.task.create"
    - from: "bot/src/commands/task-list.ts"
      to: "bot/src/lib/embeds.ts"
      via: "buildTaskListEmbed"
      pattern: "buildTaskListEmbed"
---

<objective>
Implementer la logique metier des 3 slash commands Discord (`/task add`, `/task list`, `/task done`) en remplacant les stubs livres par 04-01. Chaque commande resout d'abord le contexte (guild vers workspace, user Discord vers user app), execute son operation via Prisma, et repond a l'utilisateur avec un embed Discord stylise.

Purpose: A la fin de ce plan, un user dont le compte Discord est lie a un user app peut, depuis un serveur Discord lie a un workspace, creer / lister / cloturer des taches sans ouvrir l'application web. Les notifications (creation / completion vers channel) ne sont PAS dans ce plan — voir 04-03.

Output: 3 handlers complets + 2 helpers (resolution de contexte + builders d'embeds), dispatcher mis a jour dans `commands/index.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/STATE.md
@.planning/phases/04-discord-integration/04-CONTEXT.md
@.planning/phases/04-discord-integration/04-01-SUMMARY.md
@.planning/phases/03-core-task-management/03-PATTERNS.md
@bot/src/index.ts
@bot/src/commands/index.ts
@bot/src/prisma.ts
@prisma/schema.prisma

<interfaces>
Le bot importe le client Prisma regenere par 04-01. Champs disponibles :
User.discordId, Workspace.discordGuildId, Workspace.discordChannelId.
Modeles existants utilises : Task, Category, TaskStatus enum, Priority enum.

Enums Prisma (pour rappel — minuscules, alignes sur Kanban) :
```prisma
enum TaskStatus { todo  in_progress  done  cancelled }
enum Priority   { low   medium       high  urgent    }
```

Modele Task (existant — ne pas modifier) :
```prisma
model Task {
  id          String     @id @default(cuid())
  title       String
  description String?    @db.Text
  status      TaskStatus @default(todo)
  priority    Priority   @default(medium)
  dueDate     DateTime?
  workspaceId  String
  categoryId   String?
  assigneeId   String?
  createdById  String
}
```

discord.js v14 — types cles :
```typescript
import {
  ChatInputCommandInteraction,
  EmbedBuilder,
} from "discord.js";

interaction.options.getString("titre", true);   // required=true throw si absent
interaction.options.getString("priorite");       // optional string | null
interaction.options.getSubcommand();             // 'add' | 'list' | 'done'
interaction.guildId;                             // string | null (DM)
interaction.user.id;                             // string Discord user snowflake
```

Pattern Prisma createdById : la tache creee via Discord a `createdById = user.id` du user app resolu.
La creee via Discord n'a PAS d'assignee par defaut (assigneeId reste null).
La creee via Discord n'a PAS de categorie par defaut sauf si l'option `categorie` est fournie ET match un Category.name dans ce workspace.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Creer les helpers resolve-context et embeds</name>
  <files>bot/src/lib/resolve-context.ts, bot/src/lib/embeds.ts</files>
  <action>
Creer 2 helpers reutilises par les 3 handlers de commande.

**1. `bot/src/lib/resolve-context.ts` — resolution guild vers workspace + user Discord vers user app :**

```typescript
// bot/src/lib/resolve-context.ts
import type { ChatInputCommandInteraction } from "discord.js";
import { prisma } from "../prisma.js";

export interface ResolvedContext {
  workspace: { id: string; name: string; discordChannelId: string | null };
  user: { id: string; name: string | null };
}

export type ResolveError =
  | { kind: "no-guild" }
  | { kind: "guild-not-linked"; guildId: string }
  | { kind: "user-not-linked"; discordUserId: string };

export type ResolveResult =
  | { ok: true; ctx: ResolvedContext }
  | { ok: false; error: ResolveError };

export async function resolveContext(
  interaction: ChatInputCommandInteraction,
): Promise<ResolveResult> {
  if (!interaction.guildId) {
    return { ok: false, error: { kind: "no-guild" } };
  }

  const workspace = await prisma.workspace.findUnique({
    where: { discordGuildId: interaction.guildId },
    select: { id: true, name: true, discordChannelId: true },
  });

  if (!workspace) {
    return {
      ok: false,
      error: { kind: "guild-not-linked", guildId: interaction.guildId },
    };
  }

  const user = await prisma.user.findUnique({
    where: { discordId: interaction.user.id },
    select: { id: true, name: true },
  });

  if (!user) {
    return {
      ok: false,
      error: { kind: "user-not-linked", discordUserId: interaction.user.id },
    };
  }

  return { ok: true, ctx: { workspace, user } };
}

export function formatResolveError(error: ResolveError): string {
  switch (error.kind) {
    case "no-guild":
      return "Cette commande doit etre utilisee dans un serveur Discord, pas en message prive.";
    case "guild-not-linked":
      return (
        "Ce serveur Discord n'est lie a aucun workspace. " +
        "Configurez-le depuis les parametres du workspace dans l'application."
      );
    case "user-not-linked":
      return (
        "Votre compte Discord n'est pas lie a un compte de l'application. " +
        `Renseignez votre Discord ID (\`${error.discordUserId}\`) ` +
        "dans les parametres de votre profil dans l'application."
      );
  }
}
```

**2. `bot/src/lib/embeds.ts` — builders d'embeds avec couleur par priorite :**

```typescript
// bot/src/lib/embeds.ts
import { EmbedBuilder } from "discord.js";

type Priority = "low" | "medium" | "high" | "urgent";
type TaskStatus = "todo" | "in_progress" | "done" | "cancelled";

const PRIORITY_COLORS: Record<Priority, number> = {
  urgent: 0xef4444,
  high: 0xf97316,
  medium: 0x3b82f6,
  low: 0x9ca3af,
};

const STATUS_LABELS: Record<TaskStatus, string> = {
  todo: "A faire",
  in_progress: "En cours",
  done: "Terminee",
  cancelled: "Annulee",
};

const APP_URL = process.env.NEXT_PUBLIC_APP_URL ?? "https://tasks.bantou.me";

interface TaskLike {
  id: string;
  title: string;
  status: TaskStatus;
  priority: Priority;
  dueDate: Date | null;
  category: { name: string } | null;
}

export function buildTaskActionEmbed(opts: {
  title: string;
  task: TaskLike;
  workspaceId: string;
  workspaceName: string;
}): EmbedBuilder {
  const { title, task, workspaceId, workspaceName } = opts;
  const embed = new EmbedBuilder()
    .setTitle(title)
    .setDescription(`**${task.title}**`)
    .setColor(PRIORITY_COLORS[task.priority])
    .addFields(
      { name: "Statut", value: STATUS_LABELS[task.status], inline: true },
      { name: "Priorite", value: task.priority, inline: true },
    )
    .setFooter({ text: `Workspace : ${workspaceName} - ${task.id.slice(0, 8)}` })
    .setURL(`${APP_URL}/workspace/${workspaceId}/list`);

  if (task.category) {
    embed.addFields({ name: "Categorie", value: task.category.name, inline: true });
  }
  if (task.dueDate) {
    embed.addFields({
      name: "Echeance",
      value: task.dueDate.toISOString().slice(0, 10),
      inline: true,
    });
  }

  return embed;
}

export function buildTaskListEmbed(opts: {
  workspaceId: string;
  workspaceName: string;
  tasks: TaskLike[];
  totalCount: number;
}): EmbedBuilder {
  const { workspaceId, workspaceName, tasks, totalCount } = opts;

  if (tasks.length === 0) {
    return new EmbedBuilder()
      .setTitle(`Taches - ${workspaceName}`)
      .setDescription("Aucune tache en cours.")
      .setColor(0x6b7280)
      .setURL(`${APP_URL}/workspace/${workspaceId}/list`);
  }

  const lines = tasks.map((t) => {
    const idShort = t.id.slice(0, 8);
    const due = t.dueDate ? ` - ${t.dueDate.toISOString().slice(0, 10)}` : "";
    const cat = t.category ? ` [${t.category.name}]` : "";
    return `**\`${idShort}\`** ${t.title} - _${t.priority}_${cat}${due}`;
  });

  const description = lines.join("\n");
  const moreLine =
    totalCount > tasks.length
      ? `\n\n_${totalCount - tasks.length} tache(s) de plus non affichee(s) - voir l'app._`
      : "";

  return new EmbedBuilder()
    .setTitle(`Taches - ${workspaceName}`)
    .setDescription(description + moreLine)
    .setColor(0x3b82f6)
    .setFooter({ text: `${totalCount} tache(s) au total` })
    .setURL(`${APP_URL}/workspace/${workspaceId}/list`);
}
```

Notes :
- Les couleurs sont des entiers (format Discord), PAS des strings hex.
- L'URL de l'app est tiree de `NEXT_PUBLIC_APP_URL` (a ajouter a l'environnement du bot — voir compose, completer en tache 4 si manquant).
- Le prefixe court de l'ID (8 chars) est utilise pour `/task done` (humainement copiable depuis la liste).
  </action>
  <verify>
    <automated>test -f bot/src/lib/resolve-context.ts && test -f bot/src/lib/embeds.ts && grep -q "discordGuildId" bot/src/lib/resolve-context.ts && grep -q "discordId" bot/src/lib/resolve-context.ts && grep -q "PRIORITY_COLORS" bot/src/lib/embeds.ts && grep -q "EmbedBuilder" bot/src/lib/embeds.ts && cd bot && npx tsc --noEmit</automated>
  </verify>
  <done>Les 2 helpers existent, exportent les fonctions documentees, et passent `tsc --noEmit`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implementer les 3 handlers (task-add, task-list, task-done)</name>
  <files>bot/src/commands/task-add.ts, bot/src/commands/task-list.ts, bot/src/commands/task-done.ts</files>
  <action>
Creer 3 fichiers handlers, un par sous-commande. Chaque handler suit le meme squelette :
1. `await interaction.deferReply({ ephemeral: true })` (les operations DB peuvent depasser 3s)
2. Appel `resolveContext(interaction)` — early return avec `formatResolveError` si echec
3. Logique metier Prisma
4. `interaction.editReply({ embeds: [...] })`

**1. `bot/src/commands/task-add.ts` :**
```typescript
// bot/src/commands/task-add.ts
import type { ChatInputCommandInteraction } from "discord.js";
import { prisma } from "../prisma.js";
import { resolveContext, formatResolveError } from "../lib/resolve-context.js";
import { buildTaskActionEmbed } from "../lib/embeds.js";

type Priority = "low" | "medium" | "high" | "urgent";
const VALID_PRIORITIES: Priority[] = ["low", "medium", "high", "urgent"];

export async function handleTaskAdd(
  interaction: ChatInputCommandInteraction,
): Promise<void> {
  await interaction.deferReply({ ephemeral: true });

  const ctx = await resolveContext(interaction);
  if (!ctx.ok) {
    await interaction.editReply({ content: formatResolveError(ctx.error) });
    return;
  }

  const title = interaction.options.getString("titre", true).trim();
  const priorityInput = interaction.options.getString("priorite");
  const categoryName = interaction.options.getString("categorie")?.trim();

  if (title.length === 0 || title.length > 200) {
    await interaction.editReply({
      content: "Le titre doit faire entre 1 et 200 caracteres.",
    });
    return;
  }

  const priority: Priority = VALID_PRIORITIES.includes(priorityInput as Priority)
    ? (priorityInput as Priority)
    : "medium";

  let categoryId: string | null = null;
  if (categoryName) {
    const cat = await prisma.category.findUnique({
      where: {
        name_workspaceId: { name: categoryName, workspaceId: ctx.ctx.workspace.id },
      },
      select: { id: true },
    });
    if (!cat) {
      await interaction.editReply({
        content: `Categorie "${categoryName}" introuvable dans ce workspace.`,
      });
      return;
    }
    categoryId = cat.id;
  }

  try {
    const created = await prisma.task.create({
      data: {
        title,
        priority,
        status: "todo",
        workspaceId: ctx.ctx.workspace.id,
        createdById: ctx.ctx.user.id,
        categoryId,
      },
      select: {
        id: true,
        title: true,
        status: true,
        priority: true,
        dueDate: true,
        category: { select: { name: true } },
      },
    });

    const embed = buildTaskActionEmbed({
      title: "Tache creee",
      task: created,
      workspaceId: ctx.ctx.workspace.id,
      workspaceName: ctx.ctx.workspace.name,
    });

    await interaction.editReply({ embeds: [embed] });
  } catch (err) {
    console.error("[task-add]", err);
    await interaction.editReply({ content: "Erreur lors de la creation de la tache." });
  }
}
```

**2. `bot/src/commands/task-list.ts` :**
```typescript
// bot/src/commands/task-list.ts
import type { ChatInputCommandInteraction } from "discord.js";
import { prisma } from "../prisma.js";
import { resolveContext, formatResolveError } from "../lib/resolve-context.js";
import { buildTaskListEmbed } from "../lib/embeds.js";

type Status = "todo" | "in_progress";
const VALID_LIST_STATUSES: Status[] = ["todo", "in_progress"];
const MAX_DISPLAYED = 10;

export async function handleTaskList(
  interaction: ChatInputCommandInteraction,
): Promise<void> {
  await interaction.deferReply({ ephemeral: true });

  const ctx = await resolveContext(interaction);
  if (!ctx.ok) {
    await interaction.editReply({ content: formatResolveError(ctx.error) });
    return;
  }

  const statusInput = interaction.options.getString("statut");
  const statusFilter: Status | null = VALID_LIST_STATUSES.includes(statusInput as Status)
    ? (statusInput as Status)
    : null;

  const where = statusFilter
    ? { workspaceId: ctx.ctx.workspace.id, status: statusFilter }
    : {
        workspaceId: ctx.ctx.workspace.id,
        status: { in: ["todo", "in_progress"] as const },
      };

  try {
    const [tasks, totalCount] = await Promise.all([
      prisma.task.findMany({
        where,
        orderBy: [{ priority: "desc" }, { createdAt: "desc" }],
        take: MAX_DISPLAYED,
        select: {
          id: true,
          title: true,
          status: true,
          priority: true,
          dueDate: true,
          category: { select: { name: true } },
        },
      }),
      prisma.task.count({ where }),
    ]);

    const embed = buildTaskListEmbed({
      workspaceId: ctx.ctx.workspace.id,
      workspaceName: ctx.ctx.workspace.name,
      tasks,
      totalCount,
    });

    await interaction.editReply({ embeds: [embed] });
  } catch (err) {
    console.error("[task-list]", err);
    await interaction.editReply({ content: "Erreur lors du chargement des taches." });
  }
}
```

**3. `bot/src/commands/task-done.ts` :**
```typescript
// bot/src/commands/task-done.ts
import type { ChatInputCommandInteraction } from "discord.js";
import { prisma } from "../prisma.js";
import { resolveContext, formatResolveError } from "../lib/resolve-context.js";
import { buildTaskActionEmbed } from "../lib/embeds.js";

export async function handleTaskDone(
  interaction: ChatInputCommandInteraction,
): Promise<void> {
  await interaction.deferReply({ ephemeral: true });

  const ctx = await resolveContext(interaction);
  if (!ctx.ok) {
    await interaction.editReply({ content: formatResolveError(ctx.error) });
    return;
  }

  const idInput = interaction.options.getString("id", true).trim();

  if (idInput.length < 4) {
    await interaction.editReply({
      content: "L'ID fourni est trop court (minimum 4 caracteres).",
    });
    return;
  }

  try {
    const matches = await prisma.task.findMany({
      where: {
        workspaceId: ctx.ctx.workspace.id,
        id: { startsWith: idInput },
      },
      take: 2,
      select: {
        id: true,
        title: true,
        status: true,
        priority: true,
        dueDate: true,
        category: { select: { name: true } },
      },
    });

    if (matches.length === 0) {
      await interaction.editReply({
        content: `Aucune tache dont l'ID commence par \`${idInput}\` dans ce workspace.`,
      });
      return;
    }

    if (matches.length > 1) {
      await interaction.editReply({
        content: `Plusieurs taches correspondent a \`${idInput}\`. Precisez l'ID (8+ caracteres).`,
      });
      return;
    }

    const target = matches[0];
    if (target.status === "done") {
      await interaction.editReply({
        content: `La tache **${target.title}** est deja marquee comme terminee.`,
      });
      return;
    }

    const updated = await prisma.task.update({
      where: { id: target.id },
      data: { status: "done" },
      select: {
        id: true,
        title: true,
        status: true,
        priority: true,
        dueDate: true,
        category: { select: { name: true } },
      },
    });

    const embed = buildTaskActionEmbed({
      title: "Tache terminee",
      task: updated,
      workspaceId: ctx.ctx.workspace.id,
      workspaceName: ctx.ctx.workspace.name,
    });

    await interaction.editReply({ embeds: [embed] });
  } catch (err) {
    console.error("[task-done]", err);
    await interaction.editReply({ content: "Erreur lors de la mise a jour." });
  }
}
```

Notes critiques :
- `deferReply({ ephemeral: true })` AVANT toute operation DB — Discord coupe les interactions non repondues a 3s.
- Tous les imports utilisent l'extension `.js` (ESM NodeNext, voir 04-01).
- Les filtres Prisma utilisent `workspaceId` comme cle d'isolation forte — un user d'un autre workspace ne peut pas modifier ces taches via ces commandes.
- `prisma.task.update` ne necessite pas re-verification du workspaceId APRES le `findMany` parce que `findMany` filtre deja par `workspaceId`.
- Le `name_workspaceId` compose dans Category provient du `@@unique([name, workspaceId])` du schema (Phase 3).
  </action>
  <verify>
    <automated>test -f bot/src/commands/task-add.ts && test -f bot/src/commands/task-list.ts && test -f bot/src/commands/task-done.ts && grep -q "deferReply" bot/src/commands/task-add.ts && grep -q "startsWith: idInput" bot/src/commands/task-done.ts && grep -q "buildTaskListEmbed" bot/src/commands/task-list.ts && grep -q "resolveContext" bot/src/commands/task-add.ts && cd bot && npx tsc --noEmit</automated>
  </verify>
  <done>Les 3 handlers existent, importent les helpers, defer la reponse, gerent les erreurs de resolution de contexte, et passent `tsc --noEmit`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Mettre a jour le dispatcher commands/index.ts pour router vers les vrais handlers</name>
  <files>bot/src/commands/index.ts</files>
  <action>
Remplacer le stub `handleTaskInteraction` (livre par 04-01) par un vrai dispatcher qui route selon `interaction.options.getSubcommand()`.

Conserver l'export `taskCommand` (definitions SlashCommandBuilder) — il est utilise par `register.ts` pour publier les commandes chez Discord. Ne PAS modifier les definitions des sous-commandes (titre, priorite, categorie, statut, id, choices, requireds).

Nouveau contenu de `bot/src/commands/index.ts` (remplacer integralement) :

```typescript
// bot/src/commands/index.ts
import {
  SlashCommandBuilder,
  type ChatInputCommandInteraction,
} from "discord.js";
import { handleTaskAdd } from "./task-add.js";
import { handleTaskList } from "./task-list.js";
import { handleTaskDone } from "./task-done.js";

export const taskCommand = new SlashCommandBuilder()
  .setName("task")
  .setDescription("Gerer les taches du workspace lie a ce serveur Discord")
  .addSubcommand((sub) =>
    sub
      .setName("add")
      .setDescription("Creer une nouvelle tache")
      .addStringOption((opt) =>
        opt.setName("titre").setDescription("Titre de la tache").setRequired(true),
      )
      .addStringOption((opt) =>
        opt
          .setName("priorite")
          .setDescription("Priorite de la tache")
          .addChoices(
            { name: "low", value: "low" },
            { name: "medium", value: "medium" },
            { name: "high", value: "high" },
            { name: "urgent", value: "urgent" },
          ),
      )
      .addStringOption((opt) =>
        opt.setName("categorie").setDescription("Nom d'une categorie existante"),
      ),
  )
  .addSubcommand((sub) =>
    sub
      .setName("list")
      .setDescription("Lister les taches en cours")
      .addStringOption((opt) =>
        opt
          .setName("statut")
          .setDescription("Filtrer par statut")
          .addChoices(
            { name: "todo", value: "todo" },
            { name: "in_progress", value: "in_progress" },
          ),
      ),
  )
  .addSubcommand((sub) =>
    sub
      .setName("done")
      .setDescription("Marquer une tache comme terminee")
      .addStringOption((opt) =>
        opt
          .setName("id")
          .setDescription("ID (ou prefixe) de la tache")
          .setRequired(true),
      ),
  );

export const allCommands = [taskCommand];

/**
 * Dispatcher principal — appele depuis index.ts sur chaque interaction /task.
 * Route vers le bon handler selon la sous-commande.
 */
export async function handleTaskInteraction(
  interaction: ChatInputCommandInteraction,
): Promise<void> {
  const sub = interaction.options.getSubcommand();
  switch (sub) {
    case "add":
      return handleTaskAdd(interaction);
    case "list":
      return handleTaskList(interaction);
    case "done":
      return handleTaskDone(interaction);
    default:
      await interaction.reply({
        content: `Sous-commande inconnue : ${sub}`,
        ephemeral: true,
      });
  }
}
```

Verifier que `bot/src/index.ts` importe toujours `handleTaskInteraction` depuis `./commands/index.js` — aucune modification d'index.ts n'est requise.
  </action>
  <verify>
    <automated>grep -q 'import { handleTaskAdd }' bot/src/commands/index.ts && grep -q 'import { handleTaskList }' bot/src/commands/index.ts && grep -q 'import { handleTaskDone }' bot/src/commands/index.ts && grep -q 'case "add":' bot/src/commands/index.ts && grep -q 'case "list":' bot/src/commands/index.ts && grep -q 'case "done":' bot/src/commands/index.ts && ! grep -q "stub 04-01" bot/src/commands/index.ts && cd bot && npx tsc --noEmit</automated>
  </verify>
  <done>Le dispatcher route vers les 3 handlers reels, le stub "[stub 04-01]" est supprime, le `taskCommand` SlashCommandBuilder reste identique (pour ne pas casser register.ts), `tsc --noEmit` passe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord vers bot | Interactions signees par Discord (validees par discord.js) |
| bot vers DB | Bot lit/ecrit Prisma via le meme schema que l'app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | E (Elevation) | /task done — un user d'un autre workspace pourrait tenter de cloturer une tache | mitigate | Tous les `prisma.*` filtrent par `workspaceId: ctx.ctx.workspace.id`. Le workspace est resolu via `discordGuildId` du guild d'origine. Cross-workspace impossible par construction. |
| T-04-07 | S (Spoofing) | Un user Discord non lie pourrait essayer d'agir | mitigate | `resolveContext` echoue si `User.discordId` ne match pas. Le User Discord doit avoir explicitement renseigne son ID dans l'app (ce sera fait en plan 04-03 via UI settings). |
| T-04-08 | I (Information disclosure) | /task list pourrait fuiter des taches d'un autre workspace si guildId mal verifie | mitigate | Le `where: { workspaceId }` est dans CHAQUE query (findMany, count, update). Aucune query sans filtre workspaceId. |
| T-04-09 | T (Tampering) | Input utilisateur (titre, id, categorie) | mitigate | Title trim + bornage 1-200 chars. ID minimum 4 chars + prefixe scope au workspace. CategoryName resolu via `findUnique` strict (pas de like). Prisma protege contre l'injection SQL. |
| T-04-10 | R (Repudiation) | Pas de log d'action Discord | accept | Out-of-scope v1. Les operations passent par `console.error` en cas d'echec. Audit log = phase ulterieure. |
</threat_model>

<verification>
1. `cd bot && npx tsc --noEmit` passe sans erreur.
2. Le dispatcher route les 3 sous-commandes vers les vrais handlers (grep absence de "stub").
3. Tests manuels (apres deploy local) :
   - Avec un workspace dont `discordGuildId` est NULL : `/task add x` repond "serveur non lie".
   - Avec un user dont `discordId` est NULL : `/task add x` repond "compte non lie" + Discord ID.
   - Avec un binding complet : `/task add Test priorite:urgent` cree la tache et embed rouge.
   - `/task list` liste max 10 taches en `todo` ou `in_progress`.
   - `/task done <prefixe-8>` cloture la tache.
   - `/task done <prefixe-trop-court-ambigu>` repond message d'erreur "plusieurs correspondances".
4. Aucun appel Prisma dans les handlers ne doit etre sans `workspaceId` dans le `where` (audit grep).
</verification>

<success_criteria>
1. `/task add <titre>` cree une tache dans le workspace lie au guild Discord (success criteria #1 du phase goal).
2. `/task list` affiche les taches en cours sous forme d'embed Discord (success criteria #2).
3. `/task done <id>` marque une tache comme terminee (success criteria #3).
4. Tous les acces DB sont scopes par `workspaceId` (isolation forte).
5. Les messages d'erreur de resolution sont uniformes et indiquent l'action a faire.
6. Les embeds utilisent les couleurs decidees en CONTEXT.md (urgent/high/medium/low).
</success_criteria>

<output>
After completion, create `.planning/phases/04-discord-integration/04-02-SUMMARY.md` documentant :
- Les 3 handlers crees + leurs comportements (add/list/done)
- Le pattern resolveContext + ses 3 cas d'echec
- Les decisions d'isolation par workspaceId
- Note pour 04-03 : les notifications post-mutation ne sont pas encore declenchees ici. C'est le service Next.js qui les declenchera apres mutations cote app, et qui exposera l'UI de liaison user.discordId / workspace.discordGuildId / workspace.discordChannelId.
</output>
</content>
</invoke>