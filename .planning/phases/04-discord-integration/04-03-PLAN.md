---
phase: 04-discord-integration
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - bot/src/notify-server.ts
  - src/lib/discord-notify.ts
  - src/app/api/workspaces/[id]/tasks/route.ts
  - src/app/api/workspaces/[id]/tasks/[taskId]/route.ts
  - src/app/api/workspaces/[id]/discord/route.ts
  - src/app/api/users/me/discord/route.ts
  - src/app/workspace/[id]/settings/page.tsx
  - src/components/ui/WorkspaceDiscordSettings.tsx
  - src/components/ui/UserDiscordSettings.tsx
  - src/app/profile/page.tsx
autonomous: true
requirements:
  - FR-07
must_haves:
  truths:
    - "A la creation d'une tache via l'app, un message embed est poste dans le channel Discord configure du workspace"
    - "A la completion d'une tache (status -> done), un message embed est poste dans le channel Discord configure"
    - "Le bot route POST /notify vers le bon channel via Workspace.discordChannelId"
    - "Le proprietaire d'un workspace peut lier/delier un guild Discord et un channel via la page settings"
    - "Un utilisateur peut renseigner et supprimer son Discord ID depuis sa page profil"
    - "Si discordChannelId est NULL ou la liaison echoue, l'app continue sans planter (notification best-effort)"
    - "Les routes /api/users/me/discord et /api/workspaces/[id]/discord sont protegees par auth + requireMembership(OWNER pour workspace)"
  artifacts:
    - path: "bot/src/notify-server.ts"
      provides: "Endpoint POST /notify route vers le channel Discord du workspace"
      contains: "discordChannelId"
    - path: "src/lib/discord-notify.ts"
      provides: "Helper Next.js fetch POST vers http://discord-bot:8080/notify (best-effort)"
      contains: "fetch"
    - path: "src/app/api/workspaces/[id]/tasks/route.ts"
      provides: "POST mutated to call notifyDiscord apres creation"
      contains: "notifyDiscord"
    - path: "src/app/api/workspaces/[id]/tasks/[taskId]/route.ts"
      provides: "PATCH mutated to call notifyDiscord quand status passe a done"
      contains: "notifyDiscord"
    - path: "src/app/api/workspaces/[id]/discord/route.ts"
      provides: "API CRUD config Discord d'un workspace (PUT/DELETE)"
      min_lines: 40
    - path: "src/app/api/users/me/discord/route.ts"
      provides: "API CRUD discordId du user courant (PUT/DELETE)"
      min_lines: 30
    - path: "src/components/ui/WorkspaceDiscordSettings.tsx"
      provides: "UI client section Discord dans settings workspace"
      min_lines: 50
    - path: "src/components/ui/UserDiscordSettings.tsx"
      provides: "UI client section Discord ID dans page profil"
      min_lines: 30
    - path: "src/app/profile/page.tsx"
      provides: "Page profil utilisateur (server component) hebergeant UserDiscordSettings"
      min_lines: 20
  key_links:
    - from: "src/app/api/workspaces/[id]/tasks/route.ts"
      to: "src/lib/discord-notify.ts"
      via: "import notifyDiscord + appel apres create"
      pattern: "notifyDiscord"
    - from: "src/lib/discord-notify.ts"
      to: "bot/src/notify-server.ts"
      via: "fetch http://discord-bot:8080/notify"
      pattern: "discord-bot:8080"
    - from: "bot/src/notify-server.ts"
      to: "Discord channel"
      via: "client.channels.fetch(discordChannelId).send(embed)"
      pattern: "channels.fetch"
    - from: "src/app/workspace/[id]/settings/page.tsx"
      to: "src/components/ui/WorkspaceDiscordSettings.tsx"
      via: "import + render OWNER only"
      pattern: "WorkspaceDiscordSettings"
---

<objective>
Cabler le systeme de notifications Discord de bout en bout : (1) le bot route POST /notify vers le bon channel via Workspace.discordChannelId ; (2) l'app Next.js notifie le bot apres mutations tasks (creation et passage a done) ; (3) UI settings permet a un OWNER de lier/delier un workspace a un guild + channel Discord ; (4) page profil permet a un user de declarer son Discord ID.

Purpose: Apres ce plan, les success criteria #4, #5, #6 du phase goal sont remplis (notifications creation/completion + UI config channel par workspace). Combine aux plans 04-01 et 04-02, le scope FR-07 est entierement livre pour v1.

Output: Bot capable d'envoyer des messages dans des channels reels, app capable de declencher ces messages, UI capable de configurer les liaisons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/STATE.md
@.planning/phases/04-discord-integration/04-CONTEXT.md
@.planning/phases/04-discord-integration/04-01-SUMMARY.md
@.planning/phases/04-discord-integration/04-02-SUMMARY.md
@.planning/phases/02-auth-workspaces/02-PATTERNS.md
@.planning/phases/03-core-task-management/03-PATTERNS.md
@bot/src/notify-server.ts
@bot/src/index.ts
@bot/src/lib/embeds.ts
@src/app/api/workspaces/[id]/tasks/route.ts
@src/app/api/workspaces/[id]/tasks/[taskId]/route.ts
@src/app/workspace/[id]/settings/page.tsx
@src/lib/auth-helpers.ts
@src/auth.ts
@docker-compose.yml

<interfaces>
**Patterns API existants a respecter (Phase 2 et 3) :**
- `Response.json()` (PAS NextResponse) pour toutes les routes API
- `await params` pour les routes dynamiques
- `requireMembership(workspaceId, userId)` depuis `@/lib/auth-helpers`
- console.error format `[contexte:METHODE]`
- import alias `@/` (jamais `../../`)

**Endpoints existants impactes :**
- `POST /api/workspaces/[id]/tasks` cree une tache. Doit, apres creation reussie, fire-and-forget une notification Discord (type "task.created").
- `PATCH /api/workspaces/[id]/tasks/[taskId]` modifie une tache. Doit, si la transition est `oldStatus !== "done" && newStatus === "done"`, fire-and-forget une notification Discord (type "task.completed").

**Schema des messages /notify (contrat bot ↔ app) :**
```typescript
type NotifyPayload =
  | {
      type: "task.created";
      workspaceId: string;
      task: {
        id: string;
        title: string;
        priority: "low" | "medium" | "high" | "urgent";
        status: "todo" | "in_progress" | "done" | "cancelled";
        category: { name: string } | null;
        dueDate: string | null; // ISO string
      };
      actor: { name: string | null };
    }
  | {
      type: "task.completed";
      workspaceId: string;
      task: { id: string; title: string; priority: "low"|"medium"|"high"|"urgent"; status: "done"; category: { name: string } | null; dueDate: string | null };
      actor: { name: string | null };
    };
```

**URL interne du bot (reseau Docker) :**
- En production / dans le compose : `http://discord-bot:8080/notify` (DNS Docker via nom de service)
- En dev local sans Docker : `http://localhost:8080/notify`
- Variable d'env cote app : `DISCORD_BOT_NOTIFY_URL` (defaults a `http://discord-bot:8080/notify`)

**Helpers d'embeds :**
Le bot reutilise `bot/src/lib/embeds.ts::buildTaskActionEmbed` (livre par 04-02) pour construire les embeds des notifications. Le payload arrive deja dans le bon shape `TaskLike` modulo conversion `dueDate string -> Date`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Cabler notify-server.ts pour router vers le bon channel Discord</name>
  <files>bot/src/notify-server.ts</files>
  <action>
Remplacer integralement le contenu stub (livre par 04-01) par une implementation qui :
1. Parse et valide le payload selon le contrat `NotifyPayload`.
2. Resoud le `Workspace` via `workspaceId` -> recupere `discordChannelId`.
3. Si `discordChannelId` est NULL, log et repond 204 (rien a faire — pas une erreur).
4. Sinon, fetch le channel Discord via `client.channels.fetch(discordChannelId)`, verifie qu'il est text-based, et envoie l'embed.
5. Reutilise `buildTaskActionEmbed` de `bot/src/lib/embeds.ts`.

```typescript
// bot/src/notify-server.ts
import { createServer, type IncomingMessage, type ServerResponse } from "node:http";
import type { Client, TextBasedChannel } from "discord.js";
import { prisma } from "./prisma.js";
import { buildTaskActionEmbed } from "./lib/embeds.js";

interface NotifyTask {
  id: string;
  title: string;
  priority: "low" | "medium" | "high" | "urgent";
  status: "todo" | "in_progress" | "done" | "cancelled";
  category: { name: string } | null;
  dueDate: string | null;
}

interface NotifyPayload {
  type: "task.created" | "task.completed";
  workspaceId: string;
  task: NotifyTask;
  actor: { name: string | null };
}

function isValidPayload(data: unknown): data is NotifyPayload {
  if (!data || typeof data !== "object") return false;
  const d = data as Record<string, unknown>;
  if (d.type !== "task.created" && d.type !== "task.completed") return false;
  if (typeof d.workspaceId !== "string") return false;
  if (!d.task || typeof d.task !== "object") return false;
  const t = d.task as Record<string, unknown>;
  return typeof t.id === "string" && typeof t.title === "string";
}

async function readJson(req: IncomingMessage): Promise<unknown> {
  return new Promise((resolve, reject) => {
    let body = "";
    req.on("data", (chunk) => { body += chunk; });
    req.on("end", () => {
      try { resolve(body ? JSON.parse(body) : {}); }
      catch (e) { reject(e); }
    });
    req.on("error", reject);
  });
}

async function dispatchNotification(client: Client, payload: NotifyPayload): Promise<{ status: number; body: unknown }> {
  const workspace = await prisma.workspace.findUnique({
    where: { id: payload.workspaceId },
    select: { id: true, name: true, discordChannelId: true },
  });

  if (!workspace) {
    return { status: 404, body: { error: "Workspace introuvable" } };
  }

  if (!workspace.discordChannelId) {
    console.log(`[notify] workspace ${workspace.id} sans channel Discord — skip`);
    return { status: 204, body: null };
  }

  let channel: TextBasedChannel | null = null;
  try {
    const fetched = await client.channels.fetch(workspace.discordChannelId);
    if (fetched && fetched.isTextBased() && "send" in fetched) {
      channel = fetched as TextBasedChannel;
    }
  } catch (err) {
    console.error(`[notify] impossible de fetch le channel ${workspace.discordChannelId}:`, err);
    return { status: 502, body: { error: "Channel inaccessible" } };
  }

  if (!channel) {
    return { status: 422, body: { error: "Channel non textuel ou inaccessible" } };
  }

  const taskWithDate = {
    ...payload.task,
    dueDate: payload.task.dueDate ? new Date(payload.task.dueDate) : null,
  };

  const titleByType: Record<NotifyPayload["type"], string> = {
    "task.created": "Nouvelle tache",
    "task.completed": "Tache terminee",
  };

  const embed = buildTaskActionEmbed({
    title: titleByType[payload.type],
    task: taskWithDate,
    workspaceId: workspace.id,
    workspaceName: workspace.name,
  });

  const actorLine = payload.actor.name ? `Par : ${payload.actor.name}` : null;

  if ("send" in channel && typeof channel.send === "function") {
    await channel.send({
      content: actorLine ?? undefined,
      embeds: [embed],
    });
  }

  return { status: 200, body: { status: "sent" } };
}

export function startNotifyServer(client: Client): void {
  const port = Number(process.env.NOTIFY_PORT ?? 8080);

  const server = createServer(async (req: IncomingMessage, res: ServerResponse) => {
    if (req.method === "GET" && req.url === "/health") {
      res.writeHead(200, { "Content-Type": "application/json" });
      res.end(JSON.stringify({ status: "ok", botReady: client.isReady() }));
      return;
    }

    if (req.method === "POST" && req.url === "/notify") {
      try {
        const data = await readJson(req);
        if (!isValidPayload(data)) {
          res.writeHead(400, { "Content-Type": "application/json" });
          res.end(JSON.stringify({ error: "Payload invalide" }));
          return;
        }

        if (!client.isReady()) {
          res.writeHead(503, { "Content-Type": "application/json" });
          res.end(JSON.stringify({ error: "Bot non pret" }));
          return;
        }

        const { status, body } = await dispatchNotification(client, data);
        res.writeHead(status, { "Content-Type": "application/json" });
        res.end(body === null ? "" : JSON.stringify(body));
      } catch (err) {
        console.error("[notify] erreur dispatch:", err);
        res.writeHead(500, { "Content-Type": "application/json" });
        res.end(JSON.stringify({ error: "Erreur interne" }));
      }
      return;
    }

    res.writeHead(404, { "Content-Type": "application/json" });
    res.end(JSON.stringify({ error: "Not found" }));
  });

  server.listen(port, "0.0.0.0", () => {
    console.log(`[notify] Server listening on :${port}`);
  });
}
```

Notes :
- `client.channels.fetch` peut renvoyer plusieurs types — on garde uniquement les channels textuels (`isTextBased()`).
- Si `discordChannelId` est defini mais le channel n'existe plus / le bot n'a pas les permissions : 502 (le caller cote app loggue mais ne plante pas).
- Pas d'auth sur l'endpoint en v1 — la mitigation est reseau (T-04-01 du plan 04-01).
  </action>
  <verify>
    <automated>grep -q "discordChannelId" bot/src/notify-server.ts && grep -q "buildTaskActionEmbed" bot/src/notify-server.ts && grep -q "client.channels.fetch" bot/src/notify-server.ts && grep -q "isValidPayload" bot/src/notify-server.ts && cd bot && npx tsc --noEmit</automated>
  </verify>
  <done>`bot/src/notify-server.ts` route les payloads vers le channel configure du workspace, retourne 204 si pas de channel, gere les erreurs Discord proprement, passe `tsc --noEmit`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Creer src/lib/discord-notify.ts (helper app -> bot) et brancher les mutations tasks</name>
  <files>src/lib/discord-notify.ts, src/app/api/workspaces/[id]/tasks/route.ts, src/app/api/workspaces/[id]/tasks/[taskId]/route.ts</files>
  <action>
**1. Creer `src/lib/discord-notify.ts` — helper fire-and-forget :**

```typescript
// src/lib/discord-notify.ts
// Helper d'appel du bot Discord depuis l'app Next.js (reseau Docker interne).
// Best-effort : log l'erreur mais ne propage pas — une notification Discord ratee
// ne doit JAMAIS faire echouer une mutation de tache.

const NOTIFY_URL = process.env.DISCORD_BOT_NOTIFY_URL ?? "http://discord-bot:8080/notify";
const NOTIFY_TIMEOUT_MS = 3000;

interface NotifyTaskShape {
  id: string;
  title: string;
  priority: "low" | "medium" | "high" | "urgent";
  status: "todo" | "in_progress" | "done" | "cancelled";
  category: { name: string } | null;
  dueDate: Date | string | null;
}

export interface NotifyDiscordInput {
  type: "task.created" | "task.completed";
  workspaceId: string;
  task: NotifyTaskShape;
  actor: { name: string | null };
}

export async function notifyDiscord(input: NotifyDiscordInput): Promise<void> {
  const payload = {
    type: input.type,
    workspaceId: input.workspaceId,
    task: {
      id: input.task.id,
      title: input.task.title,
      priority: input.task.priority,
      status: input.task.status,
      category: input.task.category,
      dueDate:
        input.task.dueDate === null
          ? null
          : typeof input.task.dueDate === "string"
            ? input.task.dueDate
            : input.task.dueDate.toISOString(),
    },
    actor: input.actor,
  };

  const controller = new AbortController();
  const timeout = setTimeout(() => controller.abort(), NOTIFY_TIMEOUT_MS);

  try {
    const res = await fetch(NOTIFY_URL, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(payload),
      signal: controller.signal,
    });
    if (!res.ok && res.status !== 204) {
      const body = await res.text().catch(() => "");
      console.warn(`[discord-notify] ${res.status} ${body}`);
    }
  } catch (err) {
    // Best-effort : le bot peut etre down, le channel peut etre delie, peu importe
    console.warn("[discord-notify] echec appel bot:", (err as Error).message);
  } finally {
    clearTimeout(timeout);
  }
}
```

**2. Modifier `src/app/api/workspaces/[id]/tasks/route.ts` — appeler notifyDiscord apres creation :**

Lire le fichier actuel, identifier le handler `POST` qui fait `prisma.task.create(...)`. Apres le `create` reussi, ajouter (avant le `return Response.json(...)`):

```typescript
import { notifyDiscord } from "@/lib/discord-notify";
// ...
// Apres prisma.task.create reussi (variable `task` ou `created`) :
void notifyDiscord({
  type: "task.created",
  workspaceId: id,
  task: {
    id: task.id,
    title: task.title,
    priority: task.priority,
    status: task.status,
    category: task.category ?? null, // assurer que category est inclus dans le select
    dueDate: task.dueDate,
  },
  actor: { name: session.user.name ?? null },
});
```

Si le `select`/`include` actuel du `prisma.task.create` n'inclut PAS `category: { select: { name: true } }`, l'ajouter (sans casser les autres champs). Le `void` devant l'appel signale qu'on ne `await` pas — la notification est fire-and-forget.

**3. Modifier `src/app/api/workspaces/[id]/tasks/[taskId]/route.ts` — appeler notifyDiscord sur transition vers done :**

Dans le handler `PATCH`, AVANT le `prisma.task.update`, recuperer le statut actuel pour detecter la transition :

```typescript
import { notifyDiscord } from "@/lib/discord-notify";
// ...
const before = await prisma.task.findUnique({
  where: { id: taskId, workspaceId: id },
  select: { status: true },
});
if (!before) {
  return Response.json({ error: "Tache introuvable" }, { status: 404 });
}

const updated = await prisma.task.update({
  where: { id: taskId, workspaceId: id },
  data: { /* champs actuels */ },
  select: {
    id: true,
    title: true,
    priority: true,
    status: true,
    dueDate: true,
    category: { select: { name: true } },
    // + tous les autres champs deja selectionnes
  },
});

const transitionedToDone = before.status !== "done" && updated.status === "done";
if (transitionedToDone) {
  void notifyDiscord({
    type: "task.completed",
    workspaceId: id,
    task: {
      id: updated.id,
      title: updated.title,
      priority: updated.priority,
      status: updated.status,
      category: updated.category ?? null,
      dueDate: updated.dueDate,
    },
    actor: { name: session.user.name ?? null },
  });
}
```

**Regles importantes :**
- Ne PAS `await notifyDiscord()` — c'est du fire-and-forget. Utiliser `void` ou `.catch(() => {})`.
- Conserver TOUS les champs actuellement retournes au client par les handlers (ne rien retirer du `select`/`include`).
- Si le handler PATCH actuel utilise `findFirst`+`update` ou un autre pattern, adapter pour conserver l'idee : recuperer status avant + status apres.
- Conserver le pattern `Response.json()` et `await params`.
- Le `category: { select: { name: true } }` doit etre ajoute au select s'il n'y est pas.
  </action>
  <verify>
    <automated>test -f src/lib/discord-notify.ts && grep -q "DISCORD_BOT_NOTIFY_URL" src/lib/discord-notify.ts && grep -q "notifyDiscord" src/app/api/workspaces/\[id\]/tasks/route.ts && grep -q "notifyDiscord" src/app/api/workspaces/\[id\]/tasks/\[taskId\]/route.ts && grep -q "task.created" src/app/api/workspaces/\[id\]/tasks/route.ts && grep -q "task.completed" src/app/api/workspaces/\[id\]/tasks/\[taskId\]/route.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Helper `notifyDiscord` cree, importe et appele en fire-and-forget dans les 2 routes mutated, transition vers done detectee correctement, `tsc --noEmit` passe pour l'app Next.js.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: API routes config Discord (workspace + user me)</name>
  <files>src/app/api/workspaces/[id]/discord/route.ts, src/app/api/users/me/discord/route.ts</files>
  <action>
**1. `src/app/api/workspaces/[id]/discord/route.ts` — config guild + channel d'un workspace :**

Patterns repris : auth, await params, requireMembership, OWNER only (pour modifier), Response.json, console.error `[discord:METHODE]`.

```typescript
// src/app/api/workspaces/[id]/discord/route.ts
import { auth } from "@/auth";
import { prisma } from "@/lib/prisma";
import { requireMembership } from "@/lib/auth-helpers";

const SNOWFLAKE_REGEX = /^\d{17,20}$/;

export async function PUT(
  request: Request,
  { params }: { params: Promise<{ id: string }> },
) {
  const session = await auth();
  if (!session?.user?.id) {
    return Response.json({ error: "Non authentifie" }, { status: 401 });
  }

  try {
    const { id } = await params;
    const member = await requireMembership(id, session.user.id);
    if (!member || member.role !== "OWNER") {
      return Response.json({ error: "Acces refuse" }, { status: 403 });
    }

    const body = await request.json();
    const { discordGuildId, discordChannelId } = body as {
      discordGuildId?: string | null;
      discordChannelId?: string | null;
    };

    // Validation : si fourni, doit etre un snowflake Discord (digits, 17-20 chars)
    if (discordGuildId != null && discordGuildId !== "" && !SNOWFLAKE_REGEX.test(discordGuildId)) {
      return Response.json({ error: "Guild ID invalide (snowflake attendu)" }, { status: 400 });
    }
    if (discordChannelId != null && discordChannelId !== "" && !SNOWFLAKE_REGEX.test(discordChannelId)) {
      return Response.json({ error: "Channel ID invalide (snowflake attendu)" }, { status: 400 });
    }

    // Verifier l'unicite du guildId si fourni (un guild = un seul workspace)
    if (discordGuildId) {
      const conflict = await prisma.workspace.findUnique({
        where: { discordGuildId },
        select: { id: true },
      });
      if (conflict && conflict.id !== id) {
        return Response.json(
          { error: "Ce serveur Discord est deja lie a un autre workspace" },
          { status: 409 },
        );
      }
    }

    const workspace = await prisma.workspace.update({
      where: { id },
      data: {
        discordGuildId: discordGuildId === "" ? null : discordGuildId,
        discordChannelId: discordChannelId === "" ? null : discordChannelId,
      },
      select: { id: true, discordGuildId: true, discordChannelId: true },
    });

    return Response.json({ data: workspace });
  } catch (error) {
    console.error("[discord:PUT]", error);
    return Response.json({ error: "Erreur interne" }, { status: 500 });
  }
}

export async function DELETE(
  _request: Request,
  { params }: { params: Promise<{ id: string }> },
) {
  const session = await auth();
  if (!session?.user?.id) {
    return Response.json({ error: "Non authentifie" }, { status: 401 });
  }

  try {
    const { id } = await params;
    const member = await requireMembership(id, session.user.id);
    if (!member || member.role !== "OWNER") {
      return Response.json({ error: "Acces refuse" }, { status: 403 });
    }

    await prisma.workspace.update({
      where: { id },
      data: { discordGuildId: null, discordChannelId: null },
    });

    return Response.json({ success: true });
  } catch (error) {
    console.error("[discord:DELETE]", error);
    return Response.json({ error: "Erreur interne" }, { status: 500 });
  }
}
```

**2. `src/app/api/users/me/discord/route.ts` — declaration discordId du user courant :**

```typescript
// src/app/api/users/me/discord/route.ts
import { auth } from "@/auth";
import { prisma } from "@/lib/prisma";

const SNOWFLAKE_REGEX = /^\d{17,20}$/;

export async function PUT(request: Request) {
  const session = await auth();
  if (!session?.user?.id) {
    return Response.json({ error: "Non authentifie" }, { status: 401 });
  }

  try {
    const body = await request.json();
    const { discordId } = body as { discordId?: string };

    if (!discordId || !SNOWFLAKE_REGEX.test(discordId)) {
      return Response.json({ error: "Discord ID invalide" }, { status: 400 });
    }

    // Conflit : un autre user a deja ce discordId
    const conflict = await prisma.user.findUnique({
      where: { discordId },
      select: { id: true },
    });
    if (conflict && conflict.id !== session.user.id) {
      return Response.json(
        { error: "Ce Discord ID est deja lie a un autre compte" },
        { status: 409 },
      );
    }

    const user = await prisma.user.update({
      where: { id: session.user.id },
      data: { discordId },
      select: { id: true, discordId: true },
    });

    return Response.json({ data: user });
  } catch (error) {
    console.error("[user-discord:PUT]", error);
    return Response.json({ error: "Erreur interne" }, { status: 500 });
  }
}

export async function DELETE() {
  const session = await auth();
  if (!session?.user?.id) {
    return Response.json({ error: "Non authentifie" }, { status: 401 });
  }

  try {
    await prisma.user.update({
      where: { id: session.user.id },
      data: { discordId: null },
    });
    return Response.json({ success: true });
  } catch (error) {
    console.error("[user-discord:DELETE]", error);
    return Response.json({ error: "Erreur interne" }, { status: 500 });
  }
}
```

Notes :
- Snowflake Discord : 17 a 20 chiffres (Twitter Snowflake encode en base 10, longueur croissante avec le temps).
- Pas de POST — seulement PUT (idempotent : un user a 0 ou 1 discordId).
- Pas de GET dedie : la valeur courante est rendue par la page server qui consomme l'UI.
  </action>
  <verify>
    <automated>test -f src/app/api/workspaces/\[id\]/discord/route.ts && test -f src/app/api/users/me/discord/route.ts && grep -q "requireMembership" src/app/api/workspaces/\[id\]/discord/route.ts && grep -q 'role !== "OWNER"' src/app/api/workspaces/\[id\]/discord/route.ts && grep -q "SNOWFLAKE_REGEX" src/app/api/workspaces/\[id\]/discord/route.ts && grep -q "SNOWFLAKE_REGEX" src/app/api/users/me/discord/route.ts && grep -q "Response.json" src/app/api/workspaces/\[id\]/discord/route.ts && npx tsc --noEmit</automated>
  </verify>
  <done>2 routes API protegees, validation snowflake en place, conflits unique gerables (409), gestion DELETE pour delier, pattern Phase 2/3 respecte.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: UI client (WorkspaceDiscordSettings + UserDiscordSettings) + integration dans les pages</name>
  <files>src/components/ui/WorkspaceDiscordSettings.tsx, src/components/ui/UserDiscordSettings.tsx, src/app/workspace/[id]/settings/page.tsx, src/app/profile/page.tsx</files>
  <action>
**1. `src/components/ui/WorkspaceDiscordSettings.tsx` — section client liaison Discord du workspace :**

Pattern repris de `src/components/ui/WorkspaceSettingsForm.tsx` (Tailwind, useState, fetch, router.refresh).

```typescript
// src/components/ui/WorkspaceDiscordSettings.tsx
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";

interface Props {
  workspaceId: string;
  initialGuildId: string | null;
  initialChannelId: string | null;
}

export function WorkspaceDiscordSettings({
  workspaceId,
  initialGuildId,
  initialChannelId,
}: Props) {
  const [guildId, setGuildId] = useState(initialGuildId ?? "");
  const [channelId, setChannelId] = useState(initialChannelId ?? "");
  const [loading, setLoading] = useState(false);
  const [unlinkLoading, setUnlinkLoading] = useState(false);
  const [error, setError] = useState("");
  const [success, setSuccess] = useState(false);
  const router = useRouter();

  async function handleSave(e: React.FormEvent) {
    e.preventDefault();
    setLoading(true);
    setError("");
    setSuccess(false);
    try {
      const res = await fetch(`/api/workspaces/${workspaceId}/discord`, {
        method: "PUT",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          discordGuildId: guildId.trim() || null,
          discordChannelId: channelId.trim() || null,
        }),
      });
      const data = await res.json();
      if (!res.ok) {
        setError(data.error ?? "Erreur lors de l'enregistrement");
        return;
      }
      setSuccess(true);
      router.refresh();
    } catch {
      setError("Erreur reseau");
    } finally {
      setLoading(false);
    }
  }

  async function handleUnlink() {
    if (!window.confirm("Delier ce serveur Discord du workspace ?")) return;
    setUnlinkLoading(true);
    setError("");
    try {
      const res = await fetch(`/api/workspaces/${workspaceId}/discord`, {
        method: "DELETE",
      });
      if (!res.ok) {
        const data = await res.json();
        setError(data.error ?? "Erreur lors de la suppression");
        return;
      }
      setGuildId("");
      setChannelId("");
      router.refresh();
    } catch {
      setError("Erreur reseau");
    } finally {
      setUnlinkLoading(false);
    }
  }

  const inputClass =
    "w-full px-3 py-2 rounded-lg border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 text-gray-900 dark:text-gray-100 placeholder-gray-400 dark:placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-blue-500";

  return (
    <section className="bg-white dark:bg-gray-900 rounded-xl border border-gray-200 dark:border-gray-800 p-6">
      <h2 className="text-lg font-semibold text-gray-900 dark:text-gray-100 mb-1">
        Integration Discord
      </h2>
      <p className="text-sm text-gray-500 dark:text-gray-400 mb-4">
        Liez ce workspace a un serveur Discord pour utiliser les slash commands /task
        et recevoir les notifications de tache dans un channel.
      </p>
      <form onSubmit={handleSave} className="space-y-4">
        <div>
          <label className="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
            ID du serveur Discord (Guild ID)
          </label>
          <input
            type="text"
            value={guildId}
            onChange={(e) => setGuildId(e.target.value)}
            placeholder="ex: 1098765432109876543"
            pattern="\d{17,20}"
            className={inputClass}
          />
          <p className="text-xs text-gray-500 dark:text-gray-400 mt-1">
            Mode developpeur Discord -&gt; clic droit sur le serveur -&gt; Copier l'ID.
          </p>
        </div>
        <div>
          <label className="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
            ID du channel de notifications
          </label>
          <input
            type="text"
            value={channelId}
            onChange={(e) => setChannelId(e.target.value)}
            placeholder="ex: 1098765432109876544"
            pattern="\d{17,20}"
            className={inputClass}
          />
          <p className="text-xs text-gray-500 dark:text-gray-400 mt-1">
            Le channel ou seront postees les notifications. Le bot doit y avoir acces.
          </p>
        </div>
        {error && <p className="text-sm text-red-600 dark:text-red-400">{error}</p>}
        {success && (
          <p className="text-sm text-green-600 dark:text-green-400">
            Configuration mise a jour.
          </p>
        )}
        <div className="flex gap-3">
          <button
            type="submit"
            disabled={loading}
            className="px-4 py-2 text-sm rounded-lg bg-blue-600 hover:bg-blue-700 text-white font-medium disabled:opacity-50 disabled:cursor-not-allowed transition-colors"
          >
            {loading ? "Enregistrement..." : "Enregistrer"}
          </button>
          {(initialGuildId || initialChannelId) && (
            <button
              type="button"
              onClick={handleUnlink}
              disabled={unlinkLoading}
              className="px-4 py-2 text-sm rounded-lg text-red-600 dark:text-red-400 hover:bg-red-50 dark:hover:bg-red-950 disabled:opacity-50 transition-colors"
            >
              {unlinkLoading ? "Suppression..." : "Delier"}
            </button>
          )}
        </div>
      </form>
    </section>
  );
}
```

**2. `src/components/ui/UserDiscordSettings.tsx` — section client declaration discordId du user :**

```typescript
// src/components/ui/UserDiscordSettings.tsx
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";

interface Props {
  initialDiscordId: string | null;
}

export function UserDiscordSettings({ initialDiscordId }: Props) {
  const [discordId, setDiscordId] = useState(initialDiscordId ?? "");
  const [loading, setLoading] = useState(false);
  const [unlinkLoading, setUnlinkLoading] = useState(false);
  const [error, setError] = useState("");
  const [success, setSuccess] = useState(false);
  const router = useRouter();

  async function handleSave(e: React.FormEvent) {
    e.preventDefault();
    setLoading(true);
    setError("");
    setSuccess(false);
    try {
      const res = await fetch("/api/users/me/discord", {
        method: "PUT",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ discordId: discordId.trim() }),
      });
      const data = await res.json();
      if (!res.ok) {
        setError(data.error ?? "Erreur lors de l'enregistrement");
        return;
      }
      setSuccess(true);
      router.refresh();
    } catch {
      setError("Erreur reseau");
    } finally {
      setLoading(false);
    }
  }

  async function handleUnlink() {
    if (!window.confirm("Supprimer la liaison Discord ?")) return;
    setUnlinkLoading(true);
    setError("");
    try {
      const res = await fetch("/api/users/me/discord", { method: "DELETE" });
      if (!res.ok) {
        const data = await res.json();
        setError(data.error ?? "Erreur");
        return;
      }
      setDiscordId("");
      router.refresh();
    } catch {
      setError("Erreur reseau");
    } finally {
      setUnlinkLoading(false);
    }
  }

  return (
    <section className="bg-white dark:bg-gray-900 rounded-xl border border-gray-200 dark:border-gray-800 p-6">
      <h2 className="text-lg font-semibold text-gray-900 dark:text-gray-100 mb-1">
        Compte Discord
      </h2>
      <p className="text-sm text-gray-500 dark:text-gray-400 mb-4">
        Renseignez votre Discord ID pour pouvoir utiliser les slash commands /task
        depuis Discord.
      </p>
      <form onSubmit={handleSave} className="space-y-4">
        <input
          type="text"
          value={discordId}
          onChange={(e) => setDiscordId(e.target.value)}
          placeholder="ex: 198765432101234567"
          pattern="\d{17,20}"
          className="w-full px-3 py-2 rounded-lg border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 text-gray-900 dark:text-gray-100 placeholder-gray-400 dark:placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-blue-500"
        />
        <p className="text-xs text-gray-500 dark:text-gray-400">
          Mode developpeur Discord -&gt; Parametres -&gt; Avance -&gt; Mode developpeur,
          puis clic droit sur votre nom -&gt; Copier l'ID.
        </p>
        {error && <p className="text-sm text-red-600 dark:text-red-400">{error}</p>}
        {success && (
          <p className="text-sm text-green-600 dark:text-green-400">
            Discord ID enregistre.
          </p>
        )}
        <div className="flex gap-3">
          <button
            type="submit"
            disabled={loading || !discordId.trim()}
            className="px-4 py-2 text-sm rounded-lg bg-blue-600 hover:bg-blue-700 text-white font-medium disabled:opacity-50 disabled:cursor-not-allowed transition-colors"
          >
            {loading ? "Enregistrement..." : "Enregistrer"}
          </button>
          {initialDiscordId && (
            <button
              type="button"
              onClick={handleUnlink}
              disabled={unlinkLoading}
              className="px-4 py-2 text-sm rounded-lg text-red-600 dark:text-red-400 hover:bg-red-50 dark:hover:bg-red-950 disabled:opacity-50 transition-colors"
            >
              {unlinkLoading ? "Suppression..." : "Delier"}
            </button>
          )}
        </div>
      </form>
    </section>
  );
}
```

**3. Modifier `src/app/workspace/[id]/settings/page.tsx` — ajouter la section Discord apres la section nom :**

Etendre le `select` de `prisma.workspaceMember.findUnique` pour inclure `discordGuildId` et `discordChannelId` du workspace, puis rendre `<WorkspaceDiscordSettings>` dans la page :

- Importer `WorkspaceDiscordSettings` depuis `@/components/ui/WorkspaceDiscordSettings`.
- Modifier l'`include`/`select` du fetch pour ramener `workspace.discordGuildId` et `workspace.discordChannelId`.
- Apres `<WorkspaceSettingsForm>`, rendre `<WorkspaceDiscordSettings workspaceId={id} initialGuildId={...} initialChannelId={...} />`.
- Conserver toute la logique existante (auth, redirect, notFound si pas OWNER).

**4. Creer `src/app/profile/page.tsx` — page profil server component :**

```typescript
// src/app/profile/page.tsx
import { auth } from "@/auth";
import { redirect } from "next/navigation";
import { prisma } from "@/lib/prisma";
import { Header } from "@/components/ui/Header";
import { UserDiscordSettings } from "@/components/ui/UserDiscordSettings";
import Link from "next/link";

export default async function ProfilePage() {
  const session = await auth();
  if (!session?.user?.id) redirect("/auth/signin");

  const user = await prisma.user.findUnique({
    where: { id: session.user.id },
    select: { id: true, name: true, email: true, discordId: true },
  });

  if (!user) redirect("/auth/signin");

  return (
    <div className="min-h-screen bg-gray-50 dark:bg-gray-950">
      <Header user={session.user} />
      <main className="max-w-2xl mx-auto px-6 py-10">
        <nav className="text-sm text-gray-500 dark:text-gray-400 mb-6">
          <Link
            href="/dashboard"
            className="hover:text-blue-600 dark:hover:text-blue-400"
          >
            Mes workspaces
          </Link>
          {" / "}
          <span>Profil</span>
        </nav>
        <h1 className="text-2xl font-bold text-gray-900 dark:text-gray-100 mb-2">
          Mon profil
        </h1>
        <p className="text-sm text-gray-500 dark:text-gray-400 mb-8">
          {user.name} ({user.email})
        </p>
        <UserDiscordSettings initialDiscordId={user.discordId} />
      </main>
    </div>
  );
}
```

Notes :
- Pas besoin de modifier `proxy.ts` — la route `/profile` n'est pas dans les `publicPaths` donc elle est protegee par defaut.
- Header est deja un composant existant (Phase 2).
- Si Header n'a pas deja un lien vers `/profile`, c'est OK pour ce plan — le user peut acceder via URL directe ou ajouter un lien sera fait en Phase 6 (polish).
  </action>
  <verify>
    <automated>test -f src/components/ui/WorkspaceDiscordSettings.tsx && test -f src/components/ui/UserDiscordSettings.tsx && test -f src/app/profile/page.tsx && grep -q "WorkspaceDiscordSettings" src/app/workspace/\[id\]/settings/page.tsx && grep -q "UserDiscordSettings" src/app/profile/page.tsx && grep -q '/api/workspaces/${workspaceId}/discord' src/components/ui/WorkspaceDiscordSettings.tsx && grep -q '/api/users/me/discord' src/components/ui/UserDiscordSettings.tsx && grep -q "discordGuildId" src/app/workspace/\[id\]/settings/page.tsx && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>2 composants client crees, page profil creee, settings page etendue avec section Discord, fetch sur les bonnes URLs, `tsc --noEmit` et `lint` passent.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Mettre a jour docker-compose.yml et Dockerfile pour la variable DISCORD_BOT_NOTIFY_URL et NEXT_PUBLIC_APP_URL cote bot</name>
  <files>docker-compose.yml</files>
  <action>
Le service `web` doit connaitre `DISCORD_BOT_NOTIFY_URL` (avec un default raisonnable). Le service `discord-bot` doit connaitre `NEXT_PUBLIC_APP_URL` pour les URL des embeds (footer/lien).

**Modifier `docker-compose.yml` (Edit, ne pas reecrire) :**

1. Dans le service `web`, sous `environment:`, AJOUTER apres les variables existantes :
```yaml
      DISCORD_BOT_NOTIFY_URL: ${DISCORD_BOT_NOTIFY_URL:-http://discord-bot:8080/notify}
```

2. Dans le service `discord-bot`, sous `environment:`, AJOUTER (apres `NOTIFY_PORT`) :
```yaml
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL:-https://tasks.bantou.me}
```

3. Toujours dans le service `discord-bot`, faire que `web` soit dans la liste de demarrage logique : ce n'est PAS obligatoire (le bot tourne meme si web est down — il interroge la DB directement) donc ne pas ajouter `depends_on` croisee. Confirmer juste que les 2 services sont sur le meme network `coolify`.

Verifier que `.env.example` est coherent (la variable `NEXT_PUBLIC_APP_URL` y est deja, pas besoin de toucher).
  </action>
  <verify>
    <automated>grep -q "DISCORD_BOT_NOTIFY_URL: " docker-compose.yml && grep -q "discord-bot:8080/notify" docker-compose.yml && grep -A 30 "discord-bot:" docker-compose.yml | grep -q "NEXT_PUBLIC_APP_URL" && docker compose config --quiet 2>&1 | tee /tmp/compose-check.log && test ! -s /tmp/compose-check.log</automated>
  </verify>
  <done>Le service `web` a `DISCORD_BOT_NOTIFY_URL` (defaut `http://discord-bot:8080/notify`). Le service `discord-bot` a `NEXT_PUBLIC_APP_URL`. `docker compose config` valide.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Verification end-to-end de la chaine de notifications Discord</name>
  <what-built>
Les taches 1-5 ont cable la chaine complete :
- Bot route POST /notify vers le bon channel via `Workspace.discordChannelId`
- App appelle le bot en fire-and-forget apres `task.created` et `task.completed`
- UI permet a un OWNER de configurer guildId/channelId, et a un user de declarer son discordId
- Compose injecte les bonnes variables d'environnement

Cette etape valide manuellement la chaine bout-en-bout, ce qu'aucun test automatique ne peut faire (necessite un vrai serveur Discord, un vrai bot token, une vraie interaction).
  </what-built>
  <how-to-verify>
**Pre-requis (a faire AVANT de cocher la checklist) :**
1. Avoir un bot Discord cree sur https://discord.com/developers/applications
2. Avoir invite le bot sur un serveur Discord de test avec scope `bot applications.commands` et permissions "Envoyer des messages"
3. Renseigner dans `.env` (ou Coolify) : `DISCORD_TOKEN`, `DISCORD_CLIENT_ID`, optionnel `DISCORD_GUILD_ID`
4. Avoir lance `npx prisma db push --accept-data-loss` (deja fait en plan 04-01 normalement)
5. Avoir build et demarre les services : `docker compose up --build` (ou en dev local : `cd bot && npm install && npm run register && npm run dev` + `npm run dev` cote app)

**Sequence de verification :**

1. **Health bot :**
   ```bash
   docker compose exec web wget -qO- http://discord-bot:8080/health
   ```
   Doit retourner `{"status":"ok","botReady":true}`.

2. **Liaison user (UI) :**
   - Visiter https://tasks.bantou.me/profile (ou http://localhost:3000/profile en dev)
   - Saisir votre Discord ID (recuperer via clic droit sur votre nom dans Discord avec mode developpeur)
   - Cliquer "Enregistrer" -&gt; message "Discord ID enregistre."

3. **Liaison workspace (UI) :**
   - Visiter `/workspace/<id>/settings` (en tant qu'OWNER)
   - Section "Integration Discord" : saisir le Guild ID du serveur de test et le Channel ID du channel ou recevoir les notifications
   - Cliquer "Enregistrer" -&gt; message "Configuration mise a jour."

4. **Test slash command (Discord) :**
   - Dans le serveur Discord, taper `/task add titre:Test depuis Discord priorite:urgent`
   - Reponse ephemere : embed rouge "Tache creee" avec le titre
   - **ATTENDU :** un embed parait AUSSI dans le channel de notifications configure (la commande `/task add` cree la tache via le bot directement, mais elle ne notifie PAS — voir note ci-dessous)

   **Note :** Les notifications sont declenchees par les routes API Next.js, pas par le bot quand il agit lui-meme. Pour tester proprement, faire les etapes 5-6.

5. **Test notification "task.created" via l'app :**
   - Dans l'app, aller sur `/workspace/<id>/list`, ouvrir le modal "Nouvelle tache", saisir un titre, enregistrer
   - **ATTENDU :** dans le channel Discord configure, un embed parait avec titre "Nouvelle tache", couleur selon priorite, "Par : votre nom"

6. **Test notification "task.completed" via l'app :**
   - Sur la meme tache, changer le statut a "done" (drag dans le Kanban ou modifier dans le modal)
   - **ATTENDU :** dans le channel Discord, un embed parait avec titre "Tache terminee", footer mentionnant le workspace + l'ID court

7. **Test resilience (channel mal configure) :**
   - Dans `/workspace/<id>/settings`, mettre un Channel ID invalide (ex: `999999999999999999`)
   - Creer une nouvelle tache via l'app
   - **ATTENDU :** la tache est creee normalement, AUCUNE erreur visible cote utilisateur. Les logs cote app affichent `[discord-notify]` warning. L'app NE PLANTE PAS.

8. **Test bot down :**
   - Stopper le bot : `docker compose stop discord-bot`
   - Creer une tache via l'app
   - **ATTENDU :** la tache est creee, l'app log un warning, et continue.
   - Redemarrer : `docker compose start discord-bot`

**Si une etape echoue :**
- Verifier `docker compose logs discord-bot` pour les erreurs Discord
- Verifier `docker compose logs web` pour les erreurs `[discord-notify]`
- Verifier que le bot a bien la permission "Envoyer des messages" dans le channel cible
- Verifier que les commandes ont bien ete enregistrees : `docker compose exec discord-bot npx tsx src/register.ts` (ou en dev `cd bot && npm run register`)
  </how-to-verify>
  <resume-signal>
Repondre :
- "validated" si les 8 etapes passent
- "partial: <description>" si certaines passent (ex: liaison OK mais notifications absentes)
- "blocked: <raison>" si pre-requis manquant (token, bot non invite, etc.)
- Decrire toute erreur observee dans les logs
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser vers app | UI form -&gt; API routes (auth NextAuth + requireMembership) |
| App vers bot | fetch http vers reseau Docker interne (non expose publiquement) |
| Bot vers Discord | Send message via gateway authentifie (token bot) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | E (Elevation) | PUT /api/workspaces/[id]/discord | mitigate | Auth check + `requireMembership` + `member.role !== "OWNER"` -&gt; 403. Pattern Phase 2 strictement applique. |
| T-04-12 | T (Tampering) | PUT /api/users/me/discord — un user pourrait spoofer un autre Discord ID | mitigate | `findUnique({ where: { discordId } })` + check de conflit -&gt; 409 si pris. Pas de moyen pour un user de "voler" un Discord ID deja lie. |
| T-04-13 | I (Information disclosure) | endpoint /notify sans auth | accept (mitigation reseau) | Pas de port mapping public (cf T-04-01 plan 04-01), reseau Docker interne. v1 : suffisant. v2 : ajouter `BOT_NOTIFY_SECRET` partage si besoin. |
| T-04-14 | D (DoS) | notification fire-and-forget pourrait bloquer la mutation tache si bot lent | mitigate | `AbortController` avec timeout 3s dans `notifyDiscord`. Erreur `catch`-ee silencieusement. |
| T-04-15 | T (Tampering) | Snowflake invalide injecte cote API | mitigate | `SNOWFLAKE_REGEX = /^\d{17,20}$/` valide cote serveur dans les 2 routes (workspace discord, user me discord). Pattern HTML cote client est defense en profondeur. |
| T-04-16 | I (Information disclosure) | Bot poste dans un mauvais channel si channelId mal configure | mitigate | Le bot fetch `channels.fetch` avec le channelId du WORKSPACE (pas celui d'un autre). Si l'OWNER met un mauvais channelId, c'est sa responsabilite — pas une fuite cross-workspace. |
| T-04-17 | E (Elevation) | Un user MEMBER (non OWNER) ne doit pas pouvoir lier un guild | mitigate | API renvoie 403 si role !== OWNER. Cote UI : la page /workspace/[id]/settings est deja protegee `if (!membership || membership.role !== "OWNER") notFound();` (Phase 2). |
</threat_model>

<verification>
**Verification automatique (en fin de plan) :**
1. `cd bot && npx tsc --noEmit` passe.
2. `npx tsc --noEmit` cote racine (Next.js) passe.
3. `npm run lint` passe.
4. `docker compose config --quiet` ne renvoie aucune erreur.
5. Grep absence : aucun appel `await notifyDiscord` (doit etre fire-and-forget). `grep -E "await\s+notifyDiscord" src/app/api/` retourne 0 lignes.
6. Toutes les nouvelles routes API utilisent `Response.json` (pas `NextResponse.json`).

**Verification manuelle (checkpoint task 6) :**
1. Liaison user via /profile fonctionne.
2. Liaison workspace via /settings fonctionne (OWNER seulement).
3. Slash commands declenchent embeds (deja teste en 04-02).
4. Creation tache via app -&gt; embed dans channel Discord.
5. Passage tache a `done` via app -&gt; embed dans channel Discord.
6. Bot down ne casse pas l'app.
</verification>

<success_criteria>
1. Notification envoyee dans le channel configure a la creation d'une tache (success criteria #4 du phase goal). 
2. Notification envoyee dans le channel configure a la completion d'une tache (success criteria #5).
3. UI settings : associer un channel Discord a un workspace fonctionne (success criteria #6).
4. Le user peut lier son Discord ID via la page profil (pre-requis pour /task add etc.).
5. Une notification ratee (bot down, channel invalide) ne fait pas echouer la mutation tache.
6. La route /notify n'est PAS exposee publiquement (verifie en 04-01).
7. Tous les patterns Phase 2/3 sont respectes (Response.json, await params, requireMembership, [contexte:METHODE] logs).
</success_criteria>

<output>
After completion, create `.planning/phases/04-discord-integration/04-03-SUMMARY.md` documentant :
- Le contrat NotifyPayload entre app et bot
- Le flux complet : user clique "Done" -&gt; PATCH /api/workspaces/[id]/tasks/[taskId] -&gt; notifyDiscord (fire-and-forget) -&gt; bot recoit /notify -&gt; channels.fetch -&gt; channel.send(embed)
- Les 2 nouvelles routes API (workspaces/[id]/discord, users/me/discord) avec leurs methodes et proprietes
- Les 2 nouveaux composants client + la nouvelle page profile
- Les variables d'environnement ajoutees (DISCORD_BOT_NOTIFY_URL cote web, NEXT_PUBLIC_APP_URL cote bot)
- Decisions de resilience (timeout 3s, fire-and-forget, no auth sur /notify v1)
- Note pour Phase 6 (Polish) : possibilite d'ajouter un lien "Profil" dans Header, possibilite d'OAuth Discord pour eviter saisie manuelle de discordId (deja note comme deferred dans CONTEXT.md).
</output>
</content>
</invoke>