---
phase: 05-gitlab-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/app/api/webhooks/gitlab/[workspaceId]/route.ts
autonomous: true
requirements: [FR-08, NFR-02]

must_haves:
  truths:
    - "Schema Prisma contient gitlabProjectId/gitlabBaseUrl/gitlabWebhookSecret sur Workspace"
    - "Schema Prisma contient gitlabIssueIid/gitlabIssueUrl sur Task"
    - "npx prisma db push --accept-data-loss exécuté avec succès"
    - "Endpoint POST /api/webhooks/gitlab/[workspaceId] retourne 401 si X-Gitlab-Token manquant ou invalide"
    - "Endpoint reçoit issue GitLab event open → crée la tâche avec titre, description, labels→tags, priority dérivée"
    - "Endpoint reçoit issue GitLab event close → marque la tâche existante comme done"
    - "Endpoint reçoit issue GitLab event reopen → marque la tâche existante comme todo"
    - "Tâche créée avec gitlabIssueIid déjà existant → 200 + skip (idempotence, pas de doublon)"
    - "Après création de tâche, notifyDiscord('task.created') est appelé en fire-and-forget"
    - "Comparaison X-Gitlab-Token utilise crypto.timingSafeEqual (pas ===)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Workspace.gitlabProjectId/gitlabBaseUrl/gitlabWebhookSecret + Task.gitlabIssueIid/gitlabIssueUrl"
      contains: "gitlabProjectId"
    - path: "src/app/api/webhooks/gitlab/[workspaceId]/route.ts"
      provides: "Endpoint webhook GitLab"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/webhooks/gitlab/[workspaceId]/route.ts"
      to: "prisma.workspace.findUnique → gitlabWebhookSecret"
      via: "Validation X-Gitlab-Token timing-safe"
      pattern: "timingSafeEqual"
    - from: "src/app/api/webhooks/gitlab/[workspaceId]/route.ts"
      to: "prisma.task.create / prisma.task.update"
      via: "Mapping issue → tâche selon object_attributes.action"
      pattern: "gitlabIssueIid"
    - from: "src/app/api/webhooks/gitlab/[workspaceId]/route.ts"
      to: "notifyDiscord('task.created')"
      via: "void (fire-and-forget) après prisma.task.create"
      pattern: "void notifyDiscord"
---

<objective>
Étendre le schema Prisma avec les champs GitLab (Workspace + Task), pousser le schema en base via `prisma db push`, puis implémenter l'endpoint webhook GitLab `/api/webhooks/gitlab/[workspaceId]` qui valide X-Gitlab-Token, mappe les issues GitLab vers des tâches (open/close/reopen), garantit l'idempotence, et déclenche la notification Discord en fire-and-forget.

Purpose: Réaliser FR-08 (intégration GitLab) côté backend — le webhook est le point d'entrée unique des événements GitLab. NFR-02 (sécurité) impose la validation du token avec comparaison constant-time.
Output: Schema Prisma étendu + base poussée + endpoint webhook fonctionnel et sécurisé.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-gitlab-integration/05-CONTEXT.md
@AGENTS.md
@CLAUDE.md
@prisma/schema.prisma
@src/lib/auth-helpers.ts
@src/lib/discord-notify.ts
@src/app/api/workspaces/[id]/tasks/route.ts

<interfaces>
<!-- Contrats existants à utiliser. Ne PAS ré-explorer. -->

From src/lib/discord-notify.ts (helper fire-and-forget existant) :
```typescript
export interface NotifyDiscordInput {
  type: "task.created" | "task.completed";
  workspaceId: string;
  task: {
    id: string;
    title: string;
    priority: "low" | "medium" | "high" | "urgent";
    status: "todo" | "in_progress" | "done" | "cancelled";
    category: { name: string } | null;
    dueDate: Date | string | null;
  };
  actor: { name: string | null };
}
export async function notifyDiscord(input: NotifyDiscordInput): Promise<void>;
```
**Usage** : `void notifyDiscord({...})` — JAMAIS `await`.

From prisma/schema.prisma (modèles existants concernés) :
```prisma
model Workspace {
  id               String   @id @default(cuid())
  name             String
  description      String?
  discordGuildId   String?  @unique
  discordChannelId String?
  // ... + relations
  @@map("workspaces")
}

model Task {
  id          String     @id @default(cuid())
  title       String
  description String?    @db.Text
  status      TaskStatus @default(todo)
  priority    Priority   @default(medium)
  dueDate     DateTime?
  workspaceId  String
  categoryId   String?
  assigneeId   String?
  createdById  String
  // ... + relations
  @@map("tasks")
}

enum TaskStatus { todo  in_progress  done  cancelled }
enum Priority   { low  medium  high  urgent }
```

Format payload GitLab webhook (Issue Hook) — extrait pertinent v1 :
```json
{
  "object_kind": "issue",
  "event_type": "issue",
  "user": { "id": 1, "name": "Admin", "username": "root", "email": "admin@example.com" },
  "project": { "id": 42, "path_with_namespace": "mygroup/myrepo", "web_url": "https://gitlab.com/mygroup/myrepo" },
  "object_attributes": {
    "id": 301,
    "iid": 23,
    "title": "Issue title",
    "description": "Issue description (markdown)",
    "state": "opened",
    "action": "open",
    "url": "https://gitlab.com/mygroup/myrepo/-/issues/23",
    "labels": [{ "title": "bug" }, { "title": "high" }]
  },
  "labels": [{ "title": "bug" }, { "title": "high" }],
  "assignees": [{ "name": "John", "username": "john", "email": "john@example.com" }]
}
```
Note : `object_attributes.action` peut être `"open" | "close" | "reopen" | "update"`. Les `labels` peuvent être au niveau racine ou dans `object_attributes`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Étendre le schema Prisma avec les champs GitLab</name>
  <files>prisma/schema.prisma</files>
  <action>
Modifier `prisma/schema.prisma` :

1. Dans `model Workspace`, ajouter 3 champs optionnels APRÈS `discordChannelId` et AVANT `createdAt` :
```prisma
gitlabProjectId     String?
gitlabBaseUrl       String?
gitlabWebhookSecret String?
```

2. Dans `model Task`, ajouter 2 champs optionnels APRÈS `dueDate` et AVANT `createdAt` :
```prisma
gitlabIssueIid Int?
gitlabIssueUrl String?
```

Ne pas ajouter d'index (peu de webhooks attendus, pas nécessaire en v1). Ne PAS rendre `gitlabIssueIid` `@unique` globalement — la même IID peut exister dans deux projets GitLab différents (donc deux workspaces différents). L'unicité fonctionnelle (IID + workspaceId) est gérée par la requête d'idempotence dans le handler webhook.

Ne pas toucher aux autres modèles. Ne pas modifier les enums. Ne pas changer les `@@map`.

Lancer `npx prisma format` après modification pour normaliser le formatage.

Lancer `npx prisma generate` pour régénérer le client TypeScript (nécessaire avant Task 3).
  </action>
  <verify>
    <automated>grep -c "gitlabProjectId\|gitlabBaseUrl\|gitlabWebhookSecret\|gitlabIssueIid\|gitlabIssueUrl" prisma/schema.prisma | grep -q "^5$" && npx prisma generate 2>&1 | grep -q "Generated"</automated>
  </verify>
  <done>5 nouveaux champs présents dans schema.prisma, `npx prisma generate` réussit, `npx tsc --noEmit` passe sans erreur côté Prisma client.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: Pousser le schema en base via prisma db push</name>
  <files>prisma/schema.prisma</files>
  <action>
**Tâche bloquante** — les tâches suivantes (et tout le reste de la phase) dépendent de la base étant à jour avec les colonnes gitlab*.

Exécuter :
```bash
npx prisma db push --accept-data-loss
```

Le flag `--accept-data-loss` est requis car `prisma db push` est conservateur sur les bases avec données existantes — ici les nouveaux champs sont tous optionnels (`String?`, `Int?`), donc aucune donnée existante ne sera perdue, mais Prisma demande confirmation.

Si la commande échoue à cause de `localhost:5432` inaccessible (note connue dans STATE.md), documenter l'échec et marquer comme blocked-deferred-to-coolify : ajouter un commentaire en fin de console output. Le code (Task 3) doit malgré tout TypeCheck via `prisma generate` qui a déjà été lancé en Task 1.

**Comportement attendu :**
- Si DB accessible : `Database is now in sync with your Prisma schema.` → done.
- Si DB inaccessible : log explicite + on continue (Coolify exécutera la migration au déploiement). NE PAS marquer la phase en échec pour cette raison.

Si `prisma db push` réussit, ré-exécuter `npx prisma generate` pour cohérence.
  </action>
  <verify>
    <automated>npx prisma db push --accept-data-loss 2>&1 | tee /tmp/db-push.log; grep -E "in sync|Database is now in sync|P1001|Can't reach database" /tmp/db-push.log</automated>
  </verify>
  <done>Soit `db push` a réussi (sync OK), soit la DB locale est inaccessible (P1001) et la migration est explicitement déférée au déploiement Coolify (logged). Dans les deux cas, `npx prisma generate` a réussi en Task 1 et le code TypeScript compile.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Implémenter l'endpoint webhook GitLab</name>
  <files>src/app/api/webhooks/gitlab/[workspaceId]/route.ts</files>
  <behavior>
    - Validation token : header X-Gitlab-Token absent → 401 (logged "missing token", sans exposer le secret).
    - Validation token : workspace.gitlabWebhookSecret null → 401 (workspace non configuré).
    - Validation token : token reçu ne match pas (timingSafeEqual) → 401 (logged "invalid token").
    - object_kind != "issue" (ex: merge_request) → 200 + skip (best-effort, pas une erreur).
    - object_attributes.action === "open" : tâche avec ce gitlabIssueIid existe déjà dans ce workspace → 200 + skip (idempotence, pas de doublon).
    - object_attributes.action === "open" : nouvelle issue → tâche créée avec title, description, priority dérivée des labels, tags upsertés, gitlabIssueIid, gitlabIssueUrl, assigneeId via match email best-effort. Puis void notifyDiscord("task.created").
    - object_attributes.action === "close" : tâche existante (matchée par gitlabIssueIid + workspaceId) passe à status="done".
    - object_attributes.action === "reopen" : tâche existante passe à status="todo".
    - object_attributes.action === "update" v1 : skip (200, défléré).
    - Body JSON malformé → 400.
    - Aucune session NextAuth requise — webhook = endpoint public protégé par token uniquement (NE PAS appeler requireMembership).
  </behavior>
  <action>
Créer `src/app/api/webhooks/gitlab/[workspaceId]/route.ts`. Le fichier doit exporter UNIQUEMENT `POST` (pas de GET, pas d'OPTIONS).

**Imports** :
```typescript
import { prisma } from "@/lib/prisma";
import { notifyDiscord } from "@/lib/discord-notify";
import { timingSafeEqual } from "node:crypto";
import type { Priority, TaskStatus } from "@prisma/client";
```

**Constantes** :
```typescript
const PRIORITY_LABELS = new Set(["low", "medium", "high", "urgent"]);
```

**Helper de comparaison constant-time** (au-dessus de `POST`) :
```typescript
function tokensEqual(received: string, expected: string): boolean {
  // timingSafeEqual exige des Buffers de même longueur — sinon comparaison fictive
  const a = Buffer.from(received, "utf8");
  const b = Buffer.from(expected, "utf8");
  if (a.length !== b.length) {
    // Comparer un buffer avec lui-même pour garder un coût constant — puis return false
    timingSafeEqual(a, a);
    return false;
  }
  return timingSafeEqual(a, b);
}
```

**Handler POST** — pseudo-structure complète :

```typescript
export async function POST(
  request: Request,
  { params }: { params: Promise<{ workspaceId: string }> }
) {
  try {
    const { workspaceId } = await params;

    // 1. Charger le workspace + son secret
    const workspace = await prisma.workspace.findUnique({
      where: { id: workspaceId },
      select: {
        id: true,
        gitlabWebhookSecret: true,
        discordChannelId: true,
      },
    });
    if (!workspace || !workspace.gitlabWebhookSecret) {
      console.warn(`[gitlab-webhook] workspace ${workspaceId} sans secret configuré`);
      return Response.json({ error: "Webhook non configuré" }, { status: 401 });
    }

    // 2. Valider X-Gitlab-Token (constant-time)
    const receivedToken = request.headers.get("x-gitlab-token") ?? "";
    if (!receivedToken || !tokensEqual(receivedToken, workspace.gitlabWebhookSecret)) {
      console.warn(`[gitlab-webhook] token invalide pour workspace ${workspaceId}`);
      return Response.json({ error: "Token invalide" }, { status: 401 });
    }

    // 3. Parser le body
    const rawBody = await request.text();
    let payload: GitLabIssueWebhookPayload;
    try {
      payload = JSON.parse(rawBody);
    } catch {
      return Response.json({ error: "JSON invalide" }, { status: 400 });
    }

    // 4. Filter : on ne gère que les issues v1
    if (payload.object_kind !== "issue") {
      return Response.json({ ok: true, skipped: "not an issue event" });
    }

    const attrs = payload.object_attributes;
    if (!attrs || typeof attrs.iid !== "number") {
      return Response.json({ error: "object_attributes manquants" }, { status: 400 });
    }

    // 5. Router selon action
    const action = attrs.action;

    // OPEN → idempotent create
    if (action === "open") {
      const existing = await prisma.task.findFirst({
        where: { workspaceId, gitlabIssueIid: attrs.iid },
        select: { id: true },
      });
      if (existing) {
        return Response.json({ ok: true, skipped: "already exists", taskId: existing.id });
      }

      // Récupérer un user "système" pour createdById : on prend l'OWNER du workspace
      const owner = await prisma.workspaceMember.findFirst({
        where: { workspaceId, role: "OWNER" },
        select: { userId: true },
      });
      if (!owner) {
        return Response.json({ error: "Workspace sans OWNER" }, { status: 500 });
      }

      // Labels GitLab → priority + tags
      const labelTitles = (payload.labels ?? attrs.labels ?? []).map((l) => l.title);
      const priorityFromLabels = labelTitles.find((t) =>
        PRIORITY_LABELS.has(t.toLowerCase())
      );
      const priority: Priority = (priorityFromLabels?.toLowerCase() as Priority) ?? "medium";
      const nonPriorityLabels = labelTitles.filter(
        (t) => !PRIORITY_LABELS.has(t.toLowerCase())
      );

      // Upsert tags (un par un, pour respecter @@unique([name, workspaceId]))
      const tagIds: string[] = [];
      for (const labelName of nonPriorityLabels) {
        const tag = await prisma.tag.upsert({
          where: { name_workspaceId: { name: labelName, workspaceId } },
          create: { name: labelName, workspaceId },
          update: {},
          select: { id: true },
        });
        tagIds.push(tag.id);
      }

      // Assignee best-effort (match par email)
      const assigneeEmail = payload.assignees?.[0]?.email;
      let assigneeId: string | null = null;
      if (assigneeEmail) {
        const member = await prisma.user.findUnique({
          where: { email: assigneeEmail },
          select: { id: true },
        });
        // Vérifier que c'est bien un membre du workspace
        if (member) {
          const isMember = await prisma.workspaceMember.findUnique({
            where: { userId_workspaceId: { userId: member.id, workspaceId } },
            select: { id: true },
          });
          if (isMember) assigneeId = member.id;
        }
      }

      const task = await prisma.task.create({
        data: {
          title: attrs.title.slice(0, 255),
          description: attrs.description ?? null,
          status: "todo",
          priority,
          workspaceId,
          createdById: owner.userId,
          assigneeId,
          gitlabIssueIid: attrs.iid,
          gitlabIssueUrl: attrs.url ?? null,
          ...(tagIds.length > 0
            ? { tags: { create: tagIds.map((tagId) => ({ tagId })) } }
            : {}),
        },
        include: {
          category: { select: { id: true, name: true, color: true } },
          tags: { include: { tag: { select: { id: true, name: true } } } },
        },
      });

      // Fire-and-forget : notification Discord (NE PAS await)
      void notifyDiscord({
        type: "task.created",
        workspaceId,
        task: {
          id: task.id,
          title: task.title,
          priority: task.priority,
          status: task.status,
          category: task.category ? { name: task.category.name } : null,
          dueDate: task.dueDate,
        },
        actor: { name: payload.user?.name ?? "GitLab" },
      });

      return Response.json({ ok: true, taskId: task.id, action: "created" }, { status: 201 });
    }

    // CLOSE → marque la tâche existante comme done
    if (action === "close") {
      const existing = await prisma.task.findFirst({
        where: { workspaceId, gitlabIssueIid: attrs.iid },
        select: { id: true, status: true },
      });
      if (!existing) {
        return Response.json({ ok: true, skipped: "task not found" });
      }
      if (existing.status === "done") {
        return Response.json({ ok: true, skipped: "already done" });
      }
      await prisma.task.update({
        where: { id: existing.id },
        data: { status: "done" satisfies TaskStatus },
      });
      return Response.json({ ok: true, taskId: existing.id, action: "closed" });
    }

    // REOPEN → marque comme todo
    if (action === "reopen") {
      const existing = await prisma.task.findFirst({
        where: { workspaceId, gitlabIssueIid: attrs.iid },
        select: { id: true },
      });
      if (!existing) {
        return Response.json({ ok: true, skipped: "task not found" });
      }
      await prisma.task.update({
        where: { id: existing.id },
        data: { status: "todo" satisfies TaskStatus },
      });
      return Response.json({ ok: true, taskId: existing.id, action: "reopened" });
    }

    // UPDATE et autres : déféré v2
    return Response.json({ ok: true, skipped: `action ${action} not handled v1` });
  } catch (error) {
    console.error("[gitlab-webhook:POST]", error);
    return Response.json({ error: "Erreur interne" }, { status: 500 });
  }
}
```

**Types locaux** (à mettre en haut du fichier après les imports) :
```typescript
interface GitLabLabel { title: string }
interface GitLabAssignee { name?: string; username?: string; email?: string }
interface GitLabIssueAttrs {
  iid: number;
  title: string;
  description?: string | null;
  state?: string;
  action?: "open" | "close" | "reopen" | "update";
  url?: string;
  labels?: GitLabLabel[];
}
interface GitLabIssueWebhookPayload {
  object_kind: string;
  event_type?: string;
  user?: { id: number; name?: string; username?: string; email?: string };
  project?: { id: number; path_with_namespace?: string; web_url?: string };
  object_attributes?: GitLabIssueAttrs;
  labels?: GitLabLabel[];
  assignees?: GitLabAssignee[];
}
```

**Patterns à respecter (rappel)** :
- `Response.json()` partout (jamais `NextResponse`).
- `await params` pour la route dynamique.
- AUCUN `requireMembership` ni `auth()` — webhook public protégé par token.
- `void notifyDiscord(...)` — JAMAIS `await` (fire-and-forget).
- `console.warn`/`console.error` ne logguent JAMAIS le secret (`receivedToken` ni `gitlabWebhookSecret`).
- Pas de `any` (TypeScript strict).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "webhooks/gitlab|error TS" || echo "TS OK"; test -f src/app/api/webhooks/gitlab/[workspaceId]/route.ts && grep -q "timingSafeEqual" src/app/api/webhooks/gitlab/[workspaceId]/route.ts && grep -q "void notifyDiscord" src/app/api/webhooks/gitlab/[workspaceId]/route.ts && grep -q "x-gitlab-token" src/app/api/webhooks/gitlab/[workspaceId]/route.ts && ! grep -q "requireMembership\|auth()" src/app/api/webhooks/gitlab/[workspaceId]/route.ts</automated>
  </verify>
  <done>Fichier route.ts créé, exporte POST, utilise timingSafeEqual pour la validation token, appelle notifyDiscord en fire-and-forget après création, gère open/close/reopen/skip, idempotence vérifiée par findFirst({gitlabIssueIid, workspaceId}) avant create. `npx tsc --noEmit` passe. `npm run build` réussit (la nouvelle route apparaît dans la liste).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitLab → app (webhook) | Source non authentifiée par session. Auth via X-Gitlab-Token uniquement. |
| webhook handler → Prisma | Trusted (interne, scope par workspaceId). |
| webhook handler → bot Discord | Best-effort fire-and-forget (déjà géré par discord-notify.ts). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | webhook endpoint (X-Gitlab-Token) | mitigate | `timingSafeEqual` pour comparaison constant-time du token. Workspace sans secret configuré → 401 immédiat. |
| T-05-02 | Tampering | payload JSON arbitraire | mitigate | Validation explicite : `object_kind === "issue"`, `typeof iid === "number"`, types stricts (pas de `any`). Body JSON malformé → 400. |
| T-05-03 | Information Disclosure | logs serveur du token | mitigate | `console.warn` log uniquement le `workspaceId` et un message générique ; jamais `receivedToken` ni `gitlabWebhookSecret`. |
| T-05-04 | Denial of Service | flood de requêtes webhook | accept | Rate limiting déféré v2 (peu de webhooks attendus en pratique). Mitigation infra niveau Coolify/Traefik si nécessaire. |
| T-05-05 | Elevation of Privilege | cross-workspace pollution | mitigate | Toutes les queries Prisma scopées par `workspaceId` du path param. Le token est unique par workspace → impossible de poller un autre workspace. |
| T-05-06 | Repudiation | doublons via webhook ré-émis | mitigate | Idempotence : `findFirst({ workspaceId, gitlabIssueIid })` avant `create` → 200 + skip si déjà présent. |
| T-05-07 | Tampering | assignee email spoofing | accept | Le best-effort match par email n'élève pas de privilèges (l'utilisateur doit déjà être membre du workspace, vérifié par `workspaceMember.findUnique`). Si pas membre → `assigneeId = null`. |
</threat_model>

<verification>
**Vérifications automatiques** :
1. `npx tsc --noEmit` — aucune erreur TypeScript.
2. `npm run build` — succès, la route `POST /api/webhooks/gitlab/[workspaceId]` apparaît dans la liste.
3. `grep -c "gitlabProjectId\|gitlabBaseUrl\|gitlabWebhookSecret\|gitlabIssueIid\|gitlabIssueUrl" prisma/schema.prisma` → 5.
4. `npx prisma generate` — réussit.
5. `! grep -q "requireMembership\|auth()" src/app/api/webhooks/gitlab/[workspaceId]/route.ts` — confirme que le webhook n'utilise PAS l'auth user.
6. `grep -q "timingSafeEqual" src/app/api/webhooks/gitlab/[workspaceId]/route.ts` — comparaison constant-time présente.
7. `grep -q "void notifyDiscord" src/app/api/webhooks/gitlab/[workspaceId]/route.ts` — fire-and-forget respecté.
8. Audit grep `await\s+notifyDiscord` dans la route → 0 match.

**Smoke test optionnel (manuel, en dev local)** :
```bash
# 401 attendu : pas de token
curl -i -X POST http://localhost:3000/api/webhooks/gitlab/<workspaceId> \
  -H "Content-Type: application/json" -d '{"object_kind":"issue"}'

# 401 attendu : mauvais token
curl -i -X POST http://localhost:3000/api/webhooks/gitlab/<workspaceId> \
  -H "Content-Type: application/json" -H "X-Gitlab-Token: wrong" \
  -d '{"object_kind":"issue"}'

# 201 attendu : payload issue open valide (avec un secret valide configuré en base)
curl -i -X POST http://localhost:3000/api/webhooks/gitlab/<workspaceId> \
  -H "Content-Type: application/json" -H "X-Gitlab-Token: <secret>" \
  -d '{"object_kind":"issue","object_attributes":{"iid":1,"title":"Test","action":"open","url":"https://gitlab.com/x/y/-/issues/1"}}'
```
Note : le smoke test nécessite un workspace avec `gitlabWebhookSecret` set en base. Il sera typiquement effectué après plan 05-02 qui livre l'UI de configuration.
</verification>

<success_criteria>
1. Schema Prisma contient gitlabProjectId/gitlabBaseUrl/gitlabWebhookSecret sur Workspace, gitlabIssueIid/gitlabIssueUrl sur Task — vérifiable par grep.
2. `npx prisma db push --accept-data-loss` exécuté (ou marqué blocked-localhost si la DB n'est pas accessible — sera repris au déploiement Coolify).
3. Endpoint POST /api/webhooks/gitlab/[workspaceId] existe, refuse 401 sans/mauvais token, crée la tâche sur action=open, marque done sur close, todo sur reopen.
4. Idempotence : second webhook avec même gitlabIssueIid → 200 skipped, pas de doublon en base.
5. notifyDiscord appelé en fire-and-forget après création (pas de await, code continue immédiatement).
6. `npm run build` réussit, `npx tsc --noEmit` passe.
7. Aucun `requireMembership` ni `auth()` dans la route webhook (vérifié par grep).
8. Aucun secret loggé en clair dans les console.warn/error.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/05-gitlab-integration/05-01-SUMMARY.md` avec :
- Champs Prisma ajoutés (Workspace + Task)
- État de `prisma db push` (réussi en local OU déféré Coolify)
- Structure du handler webhook (open/close/reopen)
- Décisions clés : timingSafeEqual, idempotence par findFirst, createdById = OWNER du workspace, assignee best-effort par email
- Threat register status (T-05-01..07)
- Vérifications passées
- Notes pour plan 05-02 (UI settings + lien retour)
</output>
