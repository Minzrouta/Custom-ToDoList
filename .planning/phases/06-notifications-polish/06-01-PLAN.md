---
phase: 06-notifications-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/notifications.ts
  - src/app/api/notifications/route.ts
  - src/app/api/notifications/[id]/route.ts
  - src/app/api/notifications/mark-all-read/route.ts
  - src/app/api/workspaces/[id]/tasks/route.ts
  - src/app/api/workspaces/[id]/tasks/[taskId]/route.ts
autonomous: false
requirements: [FR-09]
must_haves:
  truths:
    - "Le modèle Prisma Notification existe en base avec enum NotificationType"
    - "GET /api/notifications retourne les notifs de l'user authentifié, triées DESC par date"
    - "GET /api/notifications?unread=true filtre les notifs non lues uniquement"
    - "PATCH /api/notifications/[id] marque une notif comme lue (readAt = now)"
    - "POST /api/notifications/mark-all-read marque toutes les notifs de l'user comme lues"
    - "Quand l'assigneeId d'une tâche est défini/changé (POST + PATCH tasks), une notif task_assigned est créée pour le nouvel assignee (sauf si c'est l'actor)"
    - "Quand une tâche passe à status=done via PATCH, une notif task_completed est créée pour l'assignee (sauf si c'est l'actor)"
    - "L'appel de notification est fire-and-forget (void) — une erreur de notif ne casse jamais la mutation"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Modèle Notification + enum NotificationType"
      contains: "model Notification"
    - path: "src/lib/notifications.ts"
      provides: "Helpers createNotification, notifyAssignment, notifyCompletion"
      exports: ["createNotification", "notifyAssignment", "notifyCompletion"]
    - path: "src/app/api/notifications/route.ts"
      provides: "GET liste notifs de l'user"
      exports: ["GET"]
    - path: "src/app/api/notifications/[id]/route.ts"
      provides: "PATCH marquer comme lu"
      exports: ["PATCH"]
    - path: "src/app/api/notifications/mark-all-read/route.ts"
      provides: "POST mark all as read"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/workspaces/[id]/tasks/route.ts"
      to: "src/lib/notifications.ts"
      via: "void notifyAssignment(...)"
      pattern: "void notifyAssignment"
    - from: "src/app/api/workspaces/[id]/tasks/[taskId]/route.ts"
      to: "src/lib/notifications.ts"
      via: "void notifyAssignment(...) + void notifyCompletion(...)"
      pattern: "void notify(Assignment|Completion)"
    - from: "src/lib/notifications.ts"
      to: "prisma.notification"
      via: "prisma.notification.create"
      pattern: "prisma\\.notification\\.create"
---

<objective>
Livrer le backend complet du système de notifications in-app : modèle Prisma Notification, helpers fire-and-forget, 3 routes API (liste, mark-read, mark-all-read), et intégration dans les routes tasks (POST création + PATCH assignment/completion).

Purpose: Base FR-09. Sans modèle DB et sans génération automatique côté API, le badge/dropdown du plan 06-02 n'aurait rien à afficher. Cette plan livre la donnée + l'API qui la sert.

Output: Schema Prisma étendu (poussé en DB via [BLOCKING]), 1 helper, 3 routes API, 2 fichiers existants modifiés pour émettre les notifs en fire-and-forget.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-notifications-polish/06-CONTEXT.md
@CLAUDE.md
@AGENTS.md
@prisma/schema.prisma
@src/lib/discord-notify.ts
@src/lib/auth-helpers.ts
@src/lib/prisma.ts
@src/app/api/workspaces/[id]/tasks/route.ts
@src/app/api/workspaces/[id]/tasks/[taskId]/route.ts

<interfaces>
<!-- Contrats issus du codebase existant — utiliser tels quels, pas d'exploration nécessaire. -->

From src/lib/prisma.ts:
```typescript
export const prisma: PrismaClient;
```

From src/lib/auth-helpers.ts:
```typescript
export async function requireMembership(workspaceId: string, userId: string): Promise<WorkspaceMember | null>;
```

From @/auth (NextAuth v5):
```typescript
export async function auth(): Promise<Session | null>;
// session.user.id, session.user.name, session.user.email, session.user.image
```

Pattern fire-and-forget (référence src/lib/discord-notify.ts) :
- Fonction async qui catch toute erreur en interne (console.warn, jamais throw)
- Appelée avec `void notifyXxx(...)` (jamais await) depuis les routes API
- Une erreur de notif ne bloque pas la mutation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Étendre schema Prisma — modèle Notification + enum NotificationType</name>
  <files>prisma/schema.prisma</files>
  <action>
    Lire AGENTS.md avant tout code (rappel des breaking changes Next.js).

    Modifier `prisma/schema.prisma` pour ajouter :

    1. Nouvel enum `NotificationType` (après l'enum `Priority`, ligne ~83) :
    ```prisma
    enum NotificationType {
      task_assigned
      task_due_soon
      task_completed
      task_mentioned
    }
    ```

    2. Nouveau modèle `Notification` (après le modèle `Comment`, ligne ~175, AVANT model Account) :
    ```prisma
    model Notification {
      id          String           @id @default(cuid())
      userId      String
      type        NotificationType
      title       String
      body        String?          @db.Text
      taskId      String?
      workspaceId String?
      readAt      DateTime?
      createdAt   DateTime         @default(now())

      user      User       @relation(fields: [userId], references: [id], onDelete: Cascade)
      task      Task?      @relation(fields: [taskId], references: [id], onDelete: Cascade)
      workspace Workspace? @relation(fields: [workspaceId], references: [id], onDelete: Cascade)

      @@index([userId, readAt, createdAt(sort: Desc)])
      @@map("notifications")
    }
    ```

    3. Ajouter la relation inverse `notifications Notification[]` dans le modèle `User` (après `comments     Comment[]` ligne 26) :
    ```prisma
    notifications Notification[]
    ```

    4. Ajouter la relation inverse `notifications Notification[]` dans le modèle `Workspace` (après `tags       Tag[]` ligne 46) :
    ```prisma
    notifications Notification[]
    ```

    5. Ajouter la relation inverse `notifications Notification[]` dans le modèle `Task` (après `comments  Comment[]` ligne 135) :
    ```prisma
    notifications Notification[]
    ```

    Convention : `onDelete: Cascade` partout (cohérent avec le reste du schéma). Index composite `(userId, readAt, createdAt DESC)` pour optimiser la requête badge (compte unread) ET la requête dropdown (top 10 trié desc).

    NE PAS modifier les autres modèles. NE PAS retoucher les enums existants.
  </action>
  <verify>
    <automated>cd /home/ubuntu/Custom-ToDoList && npx prisma validate 2>&1 | grep -i "schema is valid"</automated>
  </verify>
  <done>
    - `npx prisma validate` retourne "schema is valid"
    - `grep -c "model Notification" prisma/schema.prisma` retourne 1
    - `grep -c "enum NotificationType" prisma/schema.prisma` retourne 1
    - `grep -c "notifications Notification\[\]" prisma/schema.prisma` retourne 3 (User, Workspace, Task)
  </done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Pousser le schema en base + régénérer client Prisma</name>
  <files>prisma/schema.prisma (no-op, juste exécution)</files>
  <action>
    **[BLOCKING]** — Cette étape doit réussir avant d'écrire le code TypeScript qui dépend du nouveau modèle Prisma. Sans `prisma generate`, le type `Notification` n'existe pas dans `@prisma/client` et TypeScript échouera à compiler les helpers et routes.

    Exécuter, dans cet ordre, depuis le répertoire racine du projet :

    1. `npx prisma db push --accept-data-loss`
       - Si DATABASE_URL en local est inaccessible (localhost:5432) OU postgres pas démarré : noter l'échec et exécuter UNIQUEMENT `npx prisma generate` (qui suffit pour TypeScript). Documenter dans le SUMMARY que `db push` est déféré au déploiement Coolify (même pattern que phase 05-01).
       - Si OK : la table `notifications` est créée + l'index composite.

    2. `npx prisma generate`
       - Régénère le client Prisma avec les nouveaux types `Notification`, `NotificationType`.
       - Doit toujours réussir (pas de dépendance DB).

    NE PAS créer de migration formelle (`prisma migrate dev`) — ce projet utilise `db push` exclusivement (décision phases précédentes, cf STATE.md).

    NE PAS continuer aux tasks suivantes si `prisma generate` échoue.
  </action>
  <verify>
    <automated>cd /home/ubuntu/Custom-ToDoList && npx prisma generate 2>&1 | grep -iE "(generated|success)"</automated>
  </verify>
  <done>
    - `npx prisma generate` réussit
    - Le type `Notification` est exporté par `@prisma/client` (vérifiable via `grep -rl "Notification" node_modules/.prisma/client/index.d.ts` retourne au moins 1 résultat)
    - Si `db push` a échoué (DB locale inaccessible), c'est documenté dans le SUMMARY comme déféré au déploiement Coolify
  </done>
</task>

<task type="auto">
  <name>Task 3: Créer src/lib/notifications.ts — helpers fire-and-forget</name>
  <files>src/lib/notifications.ts</files>
  <action>
    Créer un nouveau fichier `src/lib/notifications.ts` qui expose 3 fonctions selon le pattern de `src/lib/discord-notify.ts` (best-effort, jamais throw).

    Différence clé vs discord-notify.ts : ici on écrit directement en DB via Prisma (pas d'appel HTTP). On garde le pattern fire-and-forget : try/catch interne + console.warn, jamais propager.

    ```typescript
    // src/lib/notifications.ts
    // Helpers de génération de notifications in-app.
    // Best-effort : log les erreurs mais ne propage jamais — une notif ratée
    // ne doit JAMAIS faire échouer une mutation de tâche.

    import { prisma } from "@/lib/prisma";
    import { NotificationType } from "@prisma/client";

    interface CreateNotificationInput {
      userId: string;
      type: NotificationType;
      title: string;
      body?: string | null;
      taskId?: string | null;
      workspaceId?: string | null;
    }

    /**
     * Crée une notification en DB. Best-effort : log + swallow toute erreur.
     * À appeler en fire-and-forget : `void createNotification({...})`.
     */
    export async function createNotification(
      input: CreateNotificationInput,
    ): Promise<void> {
      try {
        await prisma.notification.create({
          data: {
            userId: input.userId,
            type: input.type,
            title: input.title,
            body: input.body ?? null,
            taskId: input.taskId ?? null,
            workspaceId: input.workspaceId ?? null,
          },
        });
      } catch (err) {
        console.warn(
          "[notifications:create] échec création notif:",
          (err as Error).message,
        );
      }
    }

    interface NotifyAssignmentInput {
      taskId: string;
      taskTitle: string;
      workspaceId: string;
      oldAssigneeId: string | null;
      newAssigneeId: string | null;
      actorId: string;
      actorName: string | null;
    }

    /**
     * Notifie le nouvel assignee qu'une tâche lui a été assignée.
     * No-op si :
     *  - newAssigneeId est null (désassignation)
     *  - newAssigneeId === oldAssigneeId (pas de changement)
     *  - newAssigneeId === actorId (l'user s'auto-assigne, pas de notif inutile)
     */
    export async function notifyAssignment(
      input: NotifyAssignmentInput,
    ): Promise<void> {
      if (!input.newAssigneeId) return;
      if (input.newAssigneeId === input.oldAssigneeId) return;
      if (input.newAssigneeId === input.actorId) return;

      const actor = input.actorName ?? "Quelqu'un";
      await createNotification({
        userId: input.newAssigneeId,
        type: "task_assigned",
        title: `${actor} vous a assigné une tâche`,
        body: input.taskTitle,
        taskId: input.taskId,
        workspaceId: input.workspaceId,
      });
    }

    interface NotifyCompletionInput {
      taskId: string;
      taskTitle: string;
      workspaceId: string;
      assigneeId: string | null;
      actorId: string;
      actorName: string | null;
    }

    /**
     * Notifie l'assignee qu'une tâche qui lui était assignée a été complétée
     * par quelqu'un d'autre. No-op si :
     *  - assigneeId est null (tâche non assignée)
     *  - assigneeId === actorId (l'assignee a complété sa propre tâche)
     */
    export async function notifyCompletion(
      input: NotifyCompletionInput,
    ): Promise<void> {
      if (!input.assigneeId) return;
      if (input.assigneeId === input.actorId) return;

      const actor = input.actorName ?? "Quelqu'un";
      await createNotification({
        userId: input.assigneeId,
        type: "task_completed",
        title: `${actor} a terminé une tâche qui vous était assignée`,
        body: input.taskTitle,
        taskId: input.taskId,
        workspaceId: input.workspaceId,
      });
    }
    ```

    Pattern critique : aucune des 3 fonctions ne `throw`. Toute erreur est capturée par le try/catch de `createNotification`. Les fonctions `notifyAssignment` / `notifyCompletion` appellent `createNotification` qui catch elle-même.

    NE PAS ajouter de mécanisme de retry (KISS, fire-and-forget). NE PAS calculer `task_due_soon` ici (computed at fetch time dans le GET liste — décidé en CONTEXT). NE PAS implémenter `task_mentioned` (déféré v2).
  </action>
  <verify>
    <automated>cd /home/ubuntu/Custom-ToDoList && npx tsc --noEmit 2>&1 | grep -v "^$" | head -20 ; test -f src/lib/notifications.ts && grep -c "export async function" src/lib/notifications.ts</automated>
  </verify>
  <done>
    - Fichier `src/lib/notifications.ts` existe
    - Exporte 3 fonctions : `createNotification`, `notifyAssignment`, `notifyCompletion`
    - `npx tsc --noEmit` ne reporte pas d'erreur sur ce fichier
    - Aucun `throw` dans le code (uniquement `console.warn` dans le catch)
  </done>
</task>

<task type="auto">
  <name>Task 4: Créer les 3 routes API notifications (GET liste, PATCH read, POST mark-all-read)</name>
  <files>src/app/api/notifications/route.ts, src/app/api/notifications/[id]/route.ts, src/app/api/notifications/mark-all-read/route.ts</files>
  <action>
    Créer 3 fichiers de route API. Respecter strictement les patterns existants :
    - `Response.json()` (PAS `NextResponse`)
    - `import { auth } from "@/auth"` pour la session
    - `await params` pour les routes dynamiques
    - Try/catch avec `console.error("[notifs:METHOD]", error)` puis 500
    - PAS de `requireMembership` (les notifs sont scopées par `userId === session.user.id`, pas par workspace)

    **Fichier 1 : `src/app/api/notifications/route.ts`** — GET liste paginée
    ```typescript
    // src/app/api/notifications/route.ts
    import { auth } from "@/auth";
    import { prisma } from "@/lib/prisma";

    // GET /api/notifications?unread=true&limit=20
    // Retourne les notifs de l'user authentifié, triées DESC par createdAt.
    export async function GET(request: Request) {
      const session = await auth();
      if (!session?.user?.id) {
        return Response.json({ error: "Non authentifié" }, { status: 401 });
      }

      try {
        const { searchParams } = new URL(request.url);
        const unreadOnly = searchParams.get("unread") === "true";
        const limitRaw = parseInt(searchParams.get("limit") ?? "20", 10);
        const limit = Number.isFinite(limitRaw) && limitRaw > 0 && limitRaw <= 100 ? limitRaw : 20;

        const where = {
          userId: session.user.id,
          ...(unreadOnly ? { readAt: null } : {}),
        };

        const [items, unreadCount] = await Promise.all([
          prisma.notification.findMany({
            where,
            orderBy: { createdAt: "desc" },
            take: limit,
            include: {
              task: { select: { id: true, title: true, workspaceId: true } },
              workspace: { select: { id: true, name: true } },
            },
          }),
          prisma.notification.count({
            where: { userId: session.user.id, readAt: null },
          }),
        ]);

        return Response.json({ data: { items, unreadCount } });
      } catch (error) {
        console.error("[notifs:GET]", error);
        return Response.json({ error: "Erreur interne" }, { status: 500 });
      }
    }
    ```

    **Fichier 2 : `src/app/api/notifications/[id]/route.ts`** — PATCH mark-read
    ```typescript
    // src/app/api/notifications/[id]/route.ts
    import { auth } from "@/auth";
    import { prisma } from "@/lib/prisma";

    // PATCH /api/notifications/[id] — marque une notif comme lue
    export async function PATCH(
      _request: Request,
      { params }: { params: Promise<{ id: string }> },
    ) {
      const session = await auth();
      if (!session?.user?.id) {
        return Response.json({ error: "Non authentifié" }, { status: 401 });
      }

      try {
        const { id } = await params;

        // Vérifier que la notif appartient bien à l'user (sécurité)
        const existing = await prisma.notification.findUnique({
          where: { id },
          select: { userId: true, readAt: true },
        });
        if (!existing) {
          return Response.json({ error: "Notification introuvable" }, { status: 404 });
        }
        if (existing.userId !== session.user.id) {
          return Response.json({ error: "Accès refusé" }, { status: 403 });
        }

        // Idempotent : si déjà lue, on renvoie le record actuel sans update
        if (existing.readAt) {
          const notif = await prisma.notification.findUnique({ where: { id } });
          return Response.json({ data: notif });
        }

        const notif = await prisma.notification.update({
          where: { id },
          data: { readAt: new Date() },
        });

        return Response.json({ data: notif });
      } catch (error) {
        console.error("[notif:PATCH]", error);
        return Response.json({ error: "Erreur interne" }, { status: 500 });
      }
    }
    ```

    **Fichier 3 : `src/app/api/notifications/mark-all-read/route.ts`** — POST mark-all
    ```typescript
    // src/app/api/notifications/mark-all-read/route.ts
    import { auth } from "@/auth";
    import { prisma } from "@/lib/prisma";

    // POST /api/notifications/mark-all-read — marque toutes les notifs de l'user comme lues
    export async function POST() {
      const session = await auth();
      if (!session?.user?.id) {
        return Response.json({ error: "Non authentifié" }, { status: 401 });
      }

      try {
        const result = await prisma.notification.updateMany({
          where: { userId: session.user.id, readAt: null },
          data: { readAt: new Date() },
        });
        return Response.json({ success: true, count: result.count });
      } catch (error) {
        console.error("[notifs:mark-all-read]", error);
        return Response.json({ error: "Erreur interne" }, { status: 500 });
      }
    }
    ```

    NE PAS exposer le champ `userId` côté response (les notifs renvoyées contiennent userId mais c'est OK : c'est forcément l'user actuel, vérifié par `where: { userId: session.user.id }`).

    NE PAS implémenter DELETE — pas demandé en v1 (déféré).
  </action>
  <verify>
    <automated>cd /home/ubuntu/Custom-ToDoList && test -f src/app/api/notifications/route.ts && test -f src/app/api/notifications/[id]/route.ts && test -f src/app/api/notifications/mark-all-read/route.ts && npx tsc --noEmit 2>&1 | grep -E "src/app/api/notifications" | head -5</automated>
  </verify>
  <done>
    - Les 3 fichiers existent
    - `npx tsc --noEmit` ne reporte aucune erreur sur les 3 fichiers
    - Chaque route utilise `Response.json()` (pas NextResponse) — vérifiable via `grep -L "NextResponse" src/app/api/notifications/**/*.ts` doit retourner les 3 fichiers (aucun n'utilise NextResponse)
    - Chaque route a `auth()` check + try/catch + console.error pattern
  </done>
</task>

<task type="auto">
  <name>Task 5: Émettre les notifs depuis les routes tasks (POST création + PATCH update)</name>
  <files>src/app/api/workspaces/[id]/tasks/route.ts, src/app/api/workspaces/[id]/tasks/[taskId]/route.ts</files>
  <action>
    Modifier les 2 routes tasks existantes pour émettre les notifs en fire-and-forget (pattern identique à `notifyDiscord` déjà en place).

    **Fichier 1 : `src/app/api/workspaces/[id]/tasks/route.ts`** (POST création)

    1. Ajouter l'import en haut, à côté de `notifyDiscord` :
    ```typescript
    import { notifyAssignment } from "@/lib/notifications";
    ```

    2. Dans la fonction `POST`, APRÈS le bloc `void notifyDiscord({...})` (ligne ~126) et AVANT le `return Response.json({ data: task }, { status: 201 });`, ajouter :
    ```typescript
    // Fire-and-forget : notifier le nouvel assignee si tâche assignée à la création.
    void notifyAssignment({
      taskId: task.id,
      taskTitle: task.title,
      workspaceId: id,
      oldAssigneeId: null,
      newAssigneeId: task.assigneeId,
      actorId: session.user.id,
      actorName: session.user.name ?? null,
    });
    ```

    Le helper gère lui-même les cas no-op (newAssigneeId null, self-assignment).

    **Fichier 2 : `src/app/api/workspaces/[id]/tasks/[taskId]/route.ts`** (PATCH update)

    1. Ajouter l'import en haut, à côté de `notifyDiscord` :
    ```typescript
    import { notifyAssignment, notifyCompletion } from "@/lib/notifications";
    ```

    2. Modifier le `select` du `findUnique` `existing` (ligne ~72-75) pour récupérer aussi `assigneeId` (nécessaire pour comparer ancienne/nouvelle valeur ET pour la notif completion) :
    ```typescript
    const existing = await prisma.task.findUnique({
      where: { id: taskId, workspaceId: id },
      select: { id: true, status: true, assigneeId: true },
    });
    ```

    3. APRÈS le bloc `if (transitionedToDone) { void notifyDiscord({...}); }` (ligne ~125) et AVANT le `return Response.json({ data: task });`, ajouter :
    ```typescript
    // Fire-and-forget : notifier le nouvel assignee si l'assignation a changé.
    if (task.assigneeId !== existing.assigneeId) {
      void notifyAssignment({
        taskId: task.id,
        taskTitle: task.title,
        workspaceId: id,
        oldAssigneeId: existing.assigneeId,
        newAssigneeId: task.assigneeId,
        actorId: session.user.id,
        actorName: session.user.name ?? null,
      });
    }

    // Fire-and-forget : si transition vers done, notifier l'assignee
    // (cas où quelqu'un d'autre que l'assignee a complété la tâche).
    if (transitionedToDone) {
      void notifyCompletion({
        taskId: task.id,
        taskTitle: task.title,
        workspaceId: id,
        assigneeId: task.assigneeId,
        actorId: session.user.id,
        actorName: session.user.name ?? null,
      });
    }
    ```

    Pattern critique : `void` devant chaque appel — JAMAIS `await`. Une notif ratée ne doit pas bloquer la réponse de l'API tasks (cf NFR-01 — API < 200ms).

    NE PAS toucher au reste de la logique des routes (Discord notify, validation, update Prisma, etc.). NE PAS introduire d'await sur les helpers de notification.
  </action>
  <verify>
    <automated>cd /home/ubuntu/Custom-ToDoList && grep -c "void notifyAssignment" src/app/api/workspaces/\[id\]/tasks/route.ts && grep -c "void notifyAssignment\|void notifyCompletion" src/app/api/workspaces/\[id\]/tasks/\[taskId\]/route.ts && npx tsc --noEmit 2>&1 | grep "src/app/api/workspaces" | head -5</automated>
  </verify>
  <done>
    - `grep "void notifyAssignment" src/app/api/workspaces/[id]/tasks/route.ts` retourne 1 match
    - `grep "void notify" src/app/api/workspaces/[id]/tasks/[taskId]/route.ts` retourne 2 matches (Assignment + Completion)
    - `grep "await notify" src/app/api/workspaces/` retourne 0 (aucun await — toujours fire-and-forget)
    - `npx tsc --noEmit` ne reporte aucune erreur sur ces 2 fichiers
    - Les imports de `notifyAssignment` et `notifyCompletion` sont présents en haut des fichiers concernés
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Vérification humaine — flux end-to-end notification d'assignation</name>
  <what-built>
    - Modèle Prisma `Notification` + enum `NotificationType` poussé en DB (ou différé Coolify si DB locale KO)
    - 3 routes API : GET /api/notifications, PATCH /api/notifications/[id], POST /api/notifications/mark-all-read
    - Helpers `notifyAssignment` et `notifyCompletion` (fire-and-forget, jamais throw)
    - Hooks dans POST tasks + PATCH tasks qui appellent les helpers en `void`
  </what-built>
  <how-to-verify>
    Cette vérification est **optionnelle** si la DB locale n'est pas accessible (cas déjà connu — phases précédentes). Si DB OK :

    1. Démarrer le serveur : `npm run dev` (assure-toi que `npx prisma generate` a été exécuté et que `prisma db push --accept-data-loss` a réussi)
    2. Se connecter via OAuth (Google ou GitHub)
    3. Ouvrir un workspace avec au moins 2 membres (sinon inviter un second user)
    4. Créer une tâche en l'assignant à l'AUTRE user (pas soi-même)
    5. Se déconnecter, se reconnecter en tant que l'autre user
    6. `curl -s -b cookies.txt http://localhost:3000/api/notifications` (depuis un navigateur connecté, copier l'URL dans la barre)
       - Doit retourner `{ data: { items: [{ type: "task_assigned", title: "X vous a assigné une tâche", body: "<titre tâche>" }], unreadCount: 1 } }`
    7. Marquer la notif lue : `PATCH /api/notifications/<id>` (via DevTools Network ou un client HTTP)
    8. Re-fetch GET /api/notifications : `unreadCount` doit être 0 et `readAt` non null sur la notif

    Si DB locale KO : juste valider que `npx tsc --noEmit` passe sans erreur et que les fichiers existent. Documenter que la vérif fonctionnelle se fera après déploiement Coolify.
  </how-to-verify>
  <resume-signal>Tape "approved" si OK (ou si DB locale KO mais TS clean), ou décris les problèmes rencontrés</resume-signal>
</task>

</tasks>

<verification>
- `npx prisma validate` retourne "schema is valid"
- `npx prisma generate` réussit (table physique créée si DB accessible)
- `npx tsc --noEmit` passe sans erreur sur les fichiers modifiés/créés
- `grep -c "model Notification" prisma/schema.prisma` retourne 1
- `grep -c "export async function" src/lib/notifications.ts` retourne 3
- Aucun `NextResponse` dans `src/app/api/notifications/` (pattern projet : `Response.json()`)
- Aucun `await notify(Assignment|Completion)` dans les routes tasks (toujours `void`)
- Les helpers ne propagent jamais d'erreur (pas de `throw`, uniquement `console.warn` en catch)
</verification>

<success_criteria>
1. Modèle Prisma Notification + enum NotificationType présents dans schema.prisma et type généré dans @prisma/client
2. Les 3 routes API notifications répondent selon la spec (GET liste avec unreadCount, PATCH marque lue, POST mark-all-read)
3. Création d'une tâche assignée à un autre user → notif task_assigned créée pour cet user
4. PATCH d'une tâche qui change l'assignee → notif task_assigned pour le nouvel assignee
5. PATCH qui transitionne vers status=done → notif task_completed pour l'assignee (sauf self-completion)
6. Aucune mutation tâche ne fail à cause d'une erreur dans les helpers de notification (fire-and-forget garanti)
</success_criteria>

<output>
Après complétion, créer `.planning/phases/06-notifications-polish/06-01-SUMMARY.md` qui documente :
- Le schema appliqué (table notifications + index)
- Si `db push` a été déféré (cas DB locale inaccessible)
- Les 3 routes API et leur format de réponse
- Les helpers et leur logique no-op (self-assignment, assigneeId null, etc.)
- Les hooks dans les routes tasks (lignes ajoutées)
</output>
