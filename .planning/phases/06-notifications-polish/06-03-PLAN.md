---
phase: 06-notifications-polish
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - README.md
  - .env.example
  - src/components/ui/InviteModal.tsx
  - src/components/ui/CreateWorkspaceModal.tsx
  - src/components/ui/DashboardActions.tsx
  - src/components/ui/WorkspaceActions.tsx
autonomous: false
requirements: [NFR-01, NFR-04]
must_haves:
  truths:
    - "README.md contient une section 'Déploiement Coolify' avec instructions complètes"
    - "README.md liste toutes les variables d'environnement nécessaires"
    - "README.md a une section 'Architecture' qui décrit Next.js + Postgres + Discord bot"
    - "README.md référence le système GSD (.planning/) pour le contexte projet"
    - ".env.example contient toutes les variables réellement utilisées par le projet"
    - "Aucun bouton icon-only du Header n'est sans aria-label (déjà fait en 06-02)"
    - "Les boutons d'action des modals (CreateWorkspace, Invite) ont un aria-label si icon-only"
    - "npm run build réussit sans warning bloquant"
  artifacts:
    - path: "README.md"
      provides: "Documentation projet complète"
      min_lines: 80
      contains: "Déploiement Coolify"
    - path: ".env.example"
      provides: "Template des variables d'env"
      contains: "DISCORD_TOKEN"
  key_links:
    - from: "README.md"
      to: ".env.example"
      via: "Reference dans la section 'Variables d'environnement'"
      pattern: "\\.env\\.example"
    - from: "README.md"
      to: "docker-compose.yml + Dockerfile"
      via: "Reference dans la section 'Déploiement Coolify'"
      pattern: "docker-compose|Coolify"
---

<objective>
Finition de l'app v1.0 : README.md complet (description, stack, env vars, dev local, déploiement Coolify, architecture), .env.example exhaustif (toutes les variables réellement utilisées), audit a11y léger sur les composants restants (boutons icon-only sans label), vérification finale `npm run build` clean.

Purpose: Sans README à jour, l'app n'est pas "production-ready" même si elle fonctionne. NFR-01/NFR-04 exigent que la documentation déploiement soit complète et que l'a11y de base soit respectée. C'est la dernière mile avant v1.0 shippable.

Output: README.md remplacé (boilerplate Next.js → doc projet complète), .env.example étendu, 3-4 fichiers UI patchés pour a11y, build clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-notifications-polish/06-CONTEXT.md
@.planning/phases/06-notifications-polish/06-01-PLAN.md
@.planning/phases/06-notifications-polish/06-02-PLAN.md
@CLAUDE.md
@AGENTS.md
@README.md
@.env.example
@docker-compose.yml
@package.json

<interfaces>
<!-- Variables d'environnement réellement référencées dans le code -->
<!-- Recensement basé sur docker-compose.yml + grep process.env -->

Variables côté web (Next.js) :
- DATABASE_URL — connexion PostgreSQL Prisma (obligatoire)
- NEXTAUTH_URL — URL publique de l'app pour NextAuth (obligatoire)
- NEXTAUTH_SECRET — secret NextAuth, openssl rand -base64 32 (obligatoire)
- AUTH_GOOGLE_ID + AUTH_GOOGLE_SECRET — OAuth Google (obligatoire si Google activé)
- AUTH_GITHUB_ID + AUTH_GITHUB_SECRET — OAuth GitHub (obligatoire si GitHub activé)
- NEXT_PUBLIC_APP_URL — baked au build, URL publique (obligatoire en prod)
- DISCORD_BOT_NOTIFY_URL — URL interne du bot (par défaut http://discord-bot:8080/notify)

Variables côté discord-bot :
- DATABASE_URL — partagé avec web
- DISCORD_TOKEN — token du bot Discord (obligatoire si bot activé)
- DISCORD_CLIENT_ID — pour le slash command register (obligatoire)
- APP_URL / NEXT_PUBLIC_APP_URL — pour générer les liens dans les embeds
- NOTIFY_PORT — port HTTP interne du service notify (défaut 8080)

Variables Postgres (docker-compose database service) :
- POSTGRES_DB — nom de la base (défaut custom_todolist)
- POSTGRES_USER — défaut postgres
- POSTGRES_PASSWORD — obligatoire (pas de défaut)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Réécrire README.md — documentation projet complète</name>
  <files>README.md</files>
  <action>
    Lire AGENTS.md AVANT tout code. Remplacer intégralement le contenu de `README.md` (actuellement le boilerplate create-next-app) par une vraie doc projet.

    Structure obligatoire (sections, dans cet ordre) :

    ````markdown
    # Custom-ToDoList

    Application web de gestion de tâches multi-utilisateur, auto-hébergée. Auth OAuth, workspaces partagés, vues Kanban/Liste, intégrations Discord et GitLab, notifications in-app.

    Déployée en production sur [tasks.bantou.me](https://tasks.bantou.me) via Coolify.

    ## Fonctionnalités

    - **Auth OAuth** : connexion via Google ou GitHub (NextAuth.js v5)
    - **Workspaces multi-user** : Boulot / École / Perso par défaut + workspaces custom, invitation par email
    - **Gestion de tâches complète** : titre, description (markdown), statut (todo / in_progress / done / cancelled), priorité (low / medium / high / urgent), catégorie, tags, sous-tâches, commentaires, assignee, due date
    - **Vues** : Kanban (drag & drop) + Liste (filtres par statut, priorité, catégorie, tag, assignee)
    - **Intégration Discord** : bot avec slash commands (`/task add`, `/task list`, `/task done`), notifications dans un channel par workspace
    - **Intégration GitLab** : webhook entrant — une issue GitLab créée déclenche une tâche dans l'app
    - **Notifications in-app** : badge + dropdown header, marquer comme lu, déclenchées par assignation et complétion
    - **Recherche globale** : titre + description, scopée aux workspaces de l'utilisateur
    - **Dark mode** : toggle dans le header, persisté via next-themes
    - **Page profil** : éditer nom et avatar, lier un compte Discord

    ## Stack technique

    - **Front + API** : Next.js 16 (App Router, React 19, TypeScript strict)
    - **Styles** : Tailwind CSS v4 (CSS-first, dark mode via class)
    - **ORM** : Prisma 5 + PostgreSQL 16
    - **Auth** : NextAuth.js v5 (beta) avec Prisma adapter
    - **Drag & drop** : @dnd-kit/core + @dnd-kit/sortable
    - **Bot Discord** : discord.js v14 (service Docker séparé)
    - **Reverse proxy / SSL** : Traefik (via Coolify)
    - **Conteneurisation** : Docker Compose (web + database + discord-bot)

    ## Variables d'environnement

    Voir `.env.example` pour le template complet. Variables critiques :

    | Variable | Service | Description |
    |---|---|---|
    | `DATABASE_URL` | web + bot | Connexion PostgreSQL Prisma |
    | `NEXTAUTH_URL` | web | URL publique pour NextAuth (callback OAuth) |
    | `NEXTAUTH_SECRET` | web | Secret NextAuth (`openssl rand -base64 32`) |
    | `AUTH_GOOGLE_ID` / `AUTH_GOOGLE_SECRET` | web | OAuth Google (Google Cloud Console) |
    | `AUTH_GITHUB_ID` / `AUTH_GITHUB_SECRET` | web | OAuth GitHub (GitHub Developer Settings) |
    | `NEXT_PUBLIC_APP_URL` | web (build) | URL publique baked au build (ex: `https://tasks.bantou.me`) |
    | `DISCORD_TOKEN` | bot | Token du bot Discord (Discord Developer Portal) |
    | `DISCORD_CLIENT_ID` | bot | Application ID Discord (pour `register-commands`) |
    | `DISCORD_BOT_NOTIFY_URL` | web | URL interne du bot (défaut `http://discord-bot:8080/notify`) |
    | `POSTGRES_DB` / `POSTGRES_USER` / `POSTGRES_PASSWORD` | database | Credentials PostgreSQL |

    > **Important Coolify** : `NEXT_PUBLIC_*` sont baked au build — passer en `build.args` ET en `environment` dans `docker-compose.yml` (déjà fait). Ne JAMAIS committer un `.env` réel (le `.gitignore` l'exclut).

    ## Développement local

    Prérequis : Node.js 20+, PostgreSQL 16 local ou Docker.

    ```bash
    # 1. Cloner et installer
    git clone <repo>
    cd Custom-ToDoList
    npm install

    # 2. Configurer .env
    cp .env.example .env
    # → renseigne DATABASE_URL, NEXTAUTH_SECRET, AUTH_GOOGLE_*, etc.

    # 3. Pousser le schéma en DB locale
    npx prisma generate
    npx prisma db push

    # 4. Démarrer le serveur dev
    npm run dev
    # → http://localhost:3000
    ```

    Pour tester le bot Discord en local : voir `bot/README.md` (si présent) ou `docker compose up discord-bot`.

    ## Déploiement Coolify

    Le projet est déployé sur un VPS via [Coolify](https://coolify.io) qui orchestre Traefik (SSL Let's Encrypt automatique) et Docker Compose.

    ### Étapes

    1. **Créer un projet Coolify** dans le dashboard (`https://coolify.bantou.me`).
    2. **Ajouter une application** de type "Docker Compose" :
       - Source : repo Git (GitHub / GitLab)
       - Compose file : `docker-compose.yml` (à la racine du repo)
    3. **Configurer le domaine** :
       - Service `web` → `https://tasks.bantou.me` (ou autre subdomain)
       - Coolify injecte automatiquement les labels Traefik et génère le certificat SSL.
    4. **Configurer les variables d'environnement** dans Coolify (UI Settings → Environment) :
       - Toutes les variables listées dans `.env.example` (et le tableau ci-dessus)
       - Marquer comme "Build-time variable" celles préfixées `NEXT_PUBLIC_*`
    5. **Configurer les OAuth callbacks** dans Google Cloud Console + GitHub Developer Settings :
       - Google : `https://tasks.bantou.me/api/auth/callback/google`
       - GitHub : `https://tasks.bantou.me/api/auth/callback/github`
    6. **Déployer** depuis Coolify (ou via webhook GitHub push).
    7. **Pousser le schéma en prod** la première fois : SSH sur le container web et lancer `npx prisma db push --accept-data-loss`, OU exécuter via Coolify console.
    8. **Enregistrer les slash commands Discord** : SSH sur le container `discord-bot` et lancer `npm run register` (ou commande équivalente dans `bot/package.json`).

    ### Gotchas Coolify

    - **`env_file: .env` interdit** : le fichier n'existe pas sur le VPS, ça casse le démarrage du compose. Passer les variables uniquement via `environment:` (déjà fait).
    - **`NEXT_PUBLIC_*` baked au build** : si tu modifies `NEXT_PUBLIC_APP_URL` après le premier déploiement, redéployer pour reconstruire l'image.
    - **Réseau Traefik** : le label `traefik.docker.network=coolify` est indispensable sur le service `web` (déjà présent dans `docker-compose.yml`).
    - **`ubuntu` pas dans groupe docker** : pour les opérations locales sur le VPS, utiliser `sudo docker` ou l'API Coolify.

    ## Architecture

    ```
    ┌────────────────────────────────────────────────────────┐
    │                     Traefik (Coolify)                   │
    │            tasks.bantou.me → :3000 (web)                │
    └────────────────────┬───────────────────────────────────┘
                         │
                ┌────────▼─────────┐         ┌──────────────────┐
                │   web (Next.js)  │◄────────│  discord-bot     │
                │                  │  HTTP   │  (discord.js)    │
                │  - App Router    │ :8080   │  - slash cmds    │
                │  - API routes    │         │  - notify HTTP   │
                │  - Server Compo. │         └──────────────────┘
                └────────┬─────────┘                    │
                         │                              │
                         │      Prisma     ┌────────────▼───────┐
                         └────────────────►│  PostgreSQL 16     │
                                           │  (volume Docker)   │
                                           └────────────────────┘
    ```

    - Le service `web` rend l'UI et expose toutes les routes API (`/api/...`).
    - Le service `discord-bot` est un client Discord persistent + serveur HTTP interne (port 8080) qui reçoit les notifications fire-and-forget depuis `web` (`POST /notify`).
    - Les deux services partagent la même base PostgreSQL via `DATABASE_URL`.
    - Le bot connaît la même schema Prisma (mêmes `Workspace`, `Task`, etc.) — pas d'API REST entre les deux, accès DB direct (KISS).

    ## Méthodologie : Get Shit Done (GSD)

    Ce projet a été développé avec la méthodologie GSD :
    - Le contexte produit, les décisions et la roadmap vivent dans `.planning/`
    - Chaque phase a un dossier `.planning/phases/XX-name/` avec son `PLAN.md` (instructions exécutables pour Claude Code) et son `SUMMARY.md` (récap après livraison)
    - `.planning/STATE.md` tient l'état d'avancement
    - `.planning/ROADMAP.md` liste les phases et leurs requirements

    Lire `.planning/PROJECT.md` pour la vision produit complète.

    ## Scripts npm

    ```bash
    npm run dev      # serveur dev Next.js (Turbopack)
    npm run build    # build production
    npm start        # démarre le build production
    npm run lint     # ESLint
    ```

    ## Roadmap

    v1.0 (livré) :
    - ✅ Phase 1 : Bootstrap & infra
    - ✅ Phase 2 : Auth & workspaces
    - ✅ Phase 3 : Core task management
    - ✅ Phase 4 : Intégration Discord
    - ✅ Phase 5 : Intégration GitLab
    - ✅ Phase 6 : Notifications & polish

    Déféré v2 (cf `.planning/phases/06-notifications-polish/06-CONTEXT.md`) :
    - Notifications temps réel (WebSocket / SSE)
    - Upload avatar direct (UploadThing / S3)
    - Mentions @user dans description/commentaires
    - Email notifications
    - Récurrence de tâches
    - App mobile

    ## Licence

    Privé — projet perso de [Baptiste](https://github.com/<your-handle>).
    ````

    Notes critiques :
    - Le diagramme ASCII doit rester aligné en monospace.
    - Le tableau des variables doit lister exactement ce qui est dans `.env.example` (cohérence).
    - Ne PAS inclure de section "Tests" — pas de tests automatisés dans le projet (assume).
    - Le lien `https://github.com/<your-handle>` reste un placeholder explicite (l'user le remplacera lui-même si besoin).

    NE PAS conserver le boilerplate Next.js. NE PAS inviter à déployer sur Vercel (le projet vise Coolify exclusivement).
  </action>
  <verify>
    <automated>cd /home/ubuntu/Custom-ToDoList && test -f README.md && grep -c "Déploiement Coolify" README.md && grep -c "DATABASE_URL\|NEXTAUTH_SECRET\|DISCORD_TOKEN" README.md && wc -l README.md | awk '{print $1}'</automated>
  </verify>
  <done>
    - README.md fait au moins 80 lignes (vs ~37 avant)
    - Contient une section "Déploiement Coolify"
    - Liste au moins DATABASE_URL, NEXTAUTH_SECRET, DISCORD_TOKEN
    - Contient un diagramme d'architecture
    - Référence `.planning/` pour le contexte GSD
    - Ne contient plus "Deploy on Vercel" (boilerplate supprimé)
  </done>
</task>

<task type="auto">
  <name>Task 2: Étendre .env.example + audit a11y boutons icon-only</name>
  <files>.env.example, src/components/ui/InviteModal.tsx, src/components/ui/CreateWorkspaceModal.tsx, src/components/ui/DashboardActions.tsx, src/components/ui/WorkspaceActions.tsx</files>
  <action>
    **1. Remplacer `.env.example`** par une version exhaustive listant TOUTES les variables réellement utilisées par le code (recensement déjà fait dans `<interfaces>`) :

    ```bash
    # .env.example — Copier en .env pour le dev local
    # En production (Coolify), ces variables sont injectées via l'UI Coolify.
    # NE JAMAIS commit un .env réel (gitignored).

    # ─── Base de données ─────────────────────────────────────────
    DATABASE_URL="postgresql://postgres:password@localhost:5432/custom_todolist"

    # Pour Docker Compose database service :
    POSTGRES_DB="custom_todolist"
    POSTGRES_USER="postgres"
    POSTGRES_PASSWORD="changeme"

    # ─── NextAuth.js ─────────────────────────────────────────────
    # URL publique de l'app (pour les callbacks OAuth)
    NEXTAUTH_URL="http://localhost:3000"
    # Secret : openssl rand -base64 32
    NEXTAUTH_SECRET="changeme-generate-with-openssl-rand-base64-32"

    # OAuth Google — https://console.cloud.google.com → Credentials
    AUTH_GOOGLE_ID=""
    AUTH_GOOGLE_SECRET=""

    # OAuth GitHub — https://github.com/settings/developers
    AUTH_GITHUB_ID=""
    AUTH_GITHUB_SECRET=""

    # ─── URLs publiques (baked au build pour Next.js) ────────────
    # En prod Coolify : passer aussi dans build.args du compose
    NEXT_PUBLIC_APP_URL="http://localhost:3000"

    # ─── Discord bot ─────────────────────────────────────────────
    # Token du bot — https://discord.com/developers/applications
    DISCORD_TOKEN=""
    # Application ID (pour register-commands)
    DISCORD_CLIENT_ID=""
    # URL interne du service notify (par défaut OK en docker compose)
    DISCORD_BOT_NOTIFY_URL="http://discord-bot:8080/notify"
    # Port HTTP interne du bot (défaut 8080)
    NOTIFY_PORT="8080"
    ```

    **2. Audit a11y — ajouter `aria-label` aux boutons icon-only restants** :

    Lire chacun des 4 fichiers et **ajouter `aria-label`** uniquement si le bouton est icon-only (sans texte visible) ET sans label déjà présent.

    a) `src/components/ui/InviteModal.tsx`
    b) `src/components/ui/CreateWorkspaceModal.tsx`
    c) `src/components/ui/DashboardActions.tsx`
    d) `src/components/ui/WorkspaceActions.tsx`

    Pour chacun :
    - Si un `<button>` n'a que des enfants SVG/Icône (pas de texte) → ajouter `aria-label="…"` décrivant l'action.
    - Si un `<button>` a déjà du texte visible OU un `aria-label` → laisser tel quel.
    - Si un SVG décoratif est présent à côté d'un texte → ajouter `aria-hidden="true"` au SVG (évite la lecture redondante par le lecteur d'écran).

    Si AUCUN bouton icon-only n'est trouvé dans ces 4 fichiers (cas probable — les modals ont en général des boutons texte "Annuler" / "Enregistrer"), c'est OK : juste consigner dans le SUMMARY "audit a11y : aucun bouton icon-only à corriger dans les 4 fichiers".

    NE PAS modifier la logique métier de ces composants. NE PAS toucher au styling Tailwind. NE PAS ajouter de nouveaux composants.
  </action>
  <verify>
    <automated>cd /home/ubuntu/Custom-ToDoList && grep -c "DISCORD_TOKEN\|AUTH_GOOGLE_ID\|POSTGRES_PASSWORD" .env.example</automated>
  </verify>
  <done>
    - `.env.example` liste DATABASE_URL, NEXTAUTH_SECRET, AUTH_GOOGLE_ID, AUTH_GITHUB_ID, DISCORD_TOKEN, DISCORD_CLIENT_ID, POSTGRES_PASSWORD, NEXT_PUBLIC_APP_URL au minimum
    - Les 4 fichiers UI ont été inspectés (audit consigné dans le SUMMARY, qu'il y ait eu modif ou non)
    - Aucun bouton icon-only sans label restant dans les 4 fichiers audités
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Build final + smoke test production-ready</name>
  <what-built>
    - README.md complet (description, stack, env vars, dev local, déploiement Coolify, architecture, GSD)
    - .env.example exhaustif (toutes les vars réellement utilisées)
    - Audit a11y des modals + actions (consigné dans SUMMARY)
  </what-built>
  <how-to-verify>
    Exécuter en local et vérifier :

    1. **Build clean** :
       ```bash
       cd /home/ubuntu/Custom-ToDoList
       npm run build
       ```
       - Doit terminer sans erreur (warnings tolérés s'ils existaient déjà avant ce plan).
       - Aucun nouveau warning introduit par les phases 06-01/02/03.

    2. **Lint** :
       ```bash
       npm run lint
       ```
       - Aucun nouveau warning ESLint.

    3. **README lisible** : ouvrir le README dans un viewer markdown (GitHub, VS Code preview).
       - Sections présentes : Fonctionnalités, Stack, Variables d'env (tableau), Dev local, Déploiement Coolify, Architecture, GSD, Scripts, Roadmap.
       - Diagramme ASCII correctement aligné.

    4. **.env.example complet** : ouvrir `.env.example`.
       - Toutes les variables groupées par section avec commentaires explicatifs.
       - Les secrets sont vides (pas de valeur réelle leakée).

    5. **(Optionnel)** Lancer un dernier smoke test si DB locale OK :
       - `npm run dev` puis tester le parcours complet : signin → dashboard → workspace → créer tâche → assigner → notification apparaît → recherche → dark mode → profil édité.

    Si une étape échoue : diagnostiquer puis répare avant approval.
  </how-to-verify>
  <resume-signal>Tape "approved" si build + lint passent et que la doc est complète, ou décris ce qui manque</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` termine sans erreur (Next.js prod build OK)
- `npm run lint` ne reporte aucun nouveau warning introduit par cette phase
- README.md ≥ 80 lignes, contient "Déploiement Coolify", contient un diagramme d'architecture
- .env.example liste toutes les variables réellement utilisées par le code (cross-check avec docker-compose.yml)
- Audit a11y consigné dans SUMMARY (boutons icon-only corrigés ou confirmation aucun à corriger)
</verification>

<success_criteria>
1. README.md est une vraie doc projet (pas le boilerplate create-next-app)
2. .env.example est exhaustif et reflète exactement les variables utilisées par le code
3. `npm run build` réussit (preuve que toute la phase 6 est compilable)
4. Audit a11y léger fait — pas de bouton icon-only sans label dans les modals et actions
5. L'app est dans un état "v1.0 shippable" — un nouveau dev peut cloner + déployer en suivant le README
</success_criteria>

<output>
Après complétion, créer `.planning/phases/06-notifications-polish/06-03-SUMMARY.md` qui documente :
- Le contenu du nouveau README (sections livrées)
- Les variables ajoutées à .env.example (delta vs avant)
- Le résultat de l'audit a11y (fichiers modifiés ou confirmation aucune modif nécessaire)
- Le statut `npm run build` et `npm run lint`
- Un mot sur le shippability v1.0 (tout est en place, prêt pour déploiement Coolify)
</output>
